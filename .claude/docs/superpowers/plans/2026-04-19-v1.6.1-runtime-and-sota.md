<!-- SPDX-License-Identifier: MIT -->
---
name: v1.6.1 plan — Runtime auto-discovery + Anthropic prompt-eng SOTA grounding
description: Implementation plan executing the v1.6.1 spec. Three phases - Phase A in-repo feat (R8 registration + 5 template patches + tests stub + plugin bump + master.md depth update), Phase B runtime validation requiring user-assisted sibling-project session spawn + evidence capture, Phase C post-merge Layer 0 sync + binding-rule pinning decision + chore(memory) tranche. 13 tasks total across 3 phases.
type: plan
version: v1.6.1
---

# v1.6.1 Plan — Runtime auto-discovery + Anthropic prompt-eng SOTA grounding

## Phases overview

- **Phase A** (in-repo feat, ~1h) — R8 registration + 5 template patches + SKILL.md sentence + plugin.json bump + master.md depth update + tests/ runbook stub + Layer A/B zero-ripple verification. All edits inside worktree ; committed in the feat branch.
- **Phase B** (runtime validation, ~30-45min, **user-assisted**) — spawn fresh Claude Code session in sibling project (read-only), type trigger phrase, observe skill-engine behavior, capture redacted transcript excerpt into `tests/runtime_auto_discovery_v1_6_1.md`. Commit as separate feat commit in same branch.
- **Phase C** (post-merge Layer 0 sync + chore, ~45min) — after PR squash-merge and tag, apply pépite sync + binding-rule pinning decision from evidence + sentinel file ; then chore(memory) tranche with honest re-rating, session trace, MEMORY pointer, forward resume.

Six-commit rhythm target : spec + spec-polish + plan + plan-polish + feat-core + feat-runtime. Seventh commit (chore) lands in chore-worktree.

## Pre-flight checks (already verified in session)

- [x] R1.1 open ritual completed (MEMORY, master, git-status, resume, INDEX, stack-TTL, worktree)
- [x] Worktree at `.claude/worktrees/feat_2026-04-19_v1_6_1_runtime_and_sota/`, branch `feat/v1.6.1-runtime-and-sota`
- [x] gh auth active on `myconciergerie-prog` (R2.3.1)
- [x] R8 SOTA entry committed (spec-polish commit bundled R8 ; acceptable — R8 is prerequisite for spec+plan patches and naturally lands with the first patch-mentioning commit)
- [x] Spec + spec-polish committed (2 commits, both reviewer P1s + P2s landed)

## Phase A — in-repo feat

### Task A1 — Register R8 entry in INDEX.md

File : `.claude/docs/superpowers/research/INDEX.md`
Action : insert one row for `anthropic-prompt-engineering_2026-04-19.md` above the `claude-code-plugin-structure_2026-04-19.md` row. Row format : `| [anthropic-prompt-engineering](sota/anthropic-prompt-engineering_2026-04-19.md) | sota | 2026-04-19 | 2026-04-26 | high | <one-line summary> |`.
Verify : `grep -F "anthropic-prompt-engineering_2026-04-19.md" .claude/docs/superpowers/research/INDEX.md` returns 1 match.

### Task A2 — Apply template patch C1 (sampling-params migration)

File : `skills/promptor/references/template.md`

Current target line in the `## Canonical template (verbatim XML)` Part A block :

```
        ## 2. PARTIE A : PARAMÈTRES D'INFERENCE & CALIBRAGE
        - Paramètres API recommandés : Température, Top_P, Top_K, `stop_sequences` spécifiques.
        - Spécificités d'intégration MCP : Configuration des serveurs d'outils requis pour la tâche.
```

Replace with model-gated block :

```
        ## 2. PARTIE A : PARAMÈTRES D'INFERENCE & CALIBRAGE
        - **Pour Claude Opus 4.7+** (requis) : omettre `temperature`, `top_p`, `top_k` (retournent 400 sur valeur non-default) ; piloter via `output_config.effort` (`low` / `medium` / `high` / `xhigh` / `max`, défaut `xhigh` pour coding/agentic) + `thinking: {type: "adaptive"}` (off par défaut) + `max_tokens` ≥ 64k en `xhigh`/`max`. Prefill assistant message = 400 (→ structured outputs).
        - **Pour Claude Opus 4.6 et antérieurs** (legacy) : `temperature`, `top_p`, `top_k`, `stop_sequences` restent configurables.
        - `stop_sequences` spécifiques à la tâche.
        - Spécificités d'intégration MCP : Configuration des serveurs d'outils requis pour la tâche.
```

Verify : `grep -F "Pour Claude Opus 4.7+" skills/promptor/references/template.md` returns 1 match.

### Task A3 — Apply template patch C2 (cache-breakpoint placement)

File : `skills/promptor/references/template.md`

Current target line inside the Part B XML payload section :

```
        ## 3. PARTIE B : PAYLOAD DU PROMPT (XML ARCHITECTURE)
        Génère le prompt final dans un bloc de code. L'architecture DOIT optimiser le KV cache (éléments statiques en premier) :
```

Append a new sub-directive line directly after the sentence :

```
        - **Placement `cache_control`** : pose le marqueur `{type: "ephemeral", ttl: "5m" | "1h"}` sur le dernier bloc qui reste identique d'une requête à l'autre (jamais sur un bloc contenant un timestamp ou input dynamique). **Minimum 4 096 tokens** pour qu'Opus 4.7 active le cache (sinon silencieusement no-op) — vérifie `usage.cache_creation_input_tokens ≠ 0` au premier appel. Max 4 breakpoints / requête. Ordre canonique : `tools → system → messages`.
```

Verify : `grep -F "Placement \`cache_control\`" skills/promptor/references/template.md` returns 1 match (or adjust grep for escape).

### Task A4 — Apply template patch C3 (MCP ServerName:tool_name form)

File : `skills/promptor/references/template.md`

In the same Part B section, add a constraint line directly after the C2 insertion :

```
        - **Références d'outils MCP** : dans `<mcp_tools_schemas>` et tout texte du prompt, utilise la forme pleinement qualifiée `ServerName:tool_name` (ex : `Supabase:execute_sql`, `GitHub:create_issue`, jamais `execute_sql` bare) — requis par Anthropic skill authoring pour éviter les collisions multi-serveur.
```

Verify : `grep -F "Références d'outils MCP" skills/promptor/references/template.md` returns 1 match.

### Task A5 — Apply template patch L1 (Phase 1 Q2 acquires effort/thinking/max_tokens)

File : `skills/promptor/references/template.md`

Current target is in TWO locations (both must be patched — 1:1 consistency):
1. The plain-text `## Phase 1 standby output (rendered by SKILL.md gate)` block
2. The `<output_template>` inside the verbatim XML `<phase_1_standby>` block

Current text in both locations :
```
2. Spécifications du modèle cible (ex: Claude Opus 4.7 1M, température,
   configuration MCP/Outils) ?
```

Replace with :
```
2. Spécifications du modèle cible (ex: Claude Opus 4.7 1M), niveau d'effort
   (low/medium/high/xhigh/max), adaptive thinking (on/off, `display` omitted/summarized),
   `max_tokens`, configuration MCP/Outils ?
```

Verify : `grep -cF "niveau d'effort" skills/promptor/references/template.md` returns 2 (once in each location).

### Task A6 — Apply template patch L2 (Phase 1 Q1 metrics task-adaptive)

File : `skills/promptor/references/template.md`

Current target is in the same TWO locations as L1 :

```
1. Objectif métier précis, métriques de succès (Precision/Recall, Latence) et
   contraintes de format de sortie (ex: JSON Schema) ?
```

Replace with :

```
1. Objectif métier précis, métriques de succès adaptées à la tâche
   (Precision/Recall pour classification/RAG, taux de réussite end-to-end
   pour agentique, latence, coût/1k tokens) et contraintes de format de
   sortie (ex: JSON Schema) ?
```

Verify : `grep -cF "métriques de succès adaptées à la tâche" skills/promptor/references/template.md` returns 2.

### Task A7 — Add SKILL.md gate sentence

File : `skills/promptor/SKILL.md`

Locate the end of the `## Two-phase operating mode (high-level)` section (the paragraph starting "The full canonical XML lives in `references/template.md`.").

Append one sentence after that paragraph :

```
**v1.6.1 gate additions (in `references/template.md`)** — Part A now gates sampling params by model (Opus 4.7+ uses `effort` + `thinking: adaptive`, not `temperature`). Part B adds explicit `cache_control` placement directive (4,096-token minimum on Opus 4.7) and mandatory `ServerName:tool_name` MCP reference form.
```

Frontmatter `description` UNCHANGED (trigger phrases pinned verbatim since v1.6.0). Verify : `grep -F "v1.6.1 gate additions" skills/promptor/SKILL.md` returns 1 match ; `head -5 skills/promptor/SKILL.md | grep -F "description:"` unchanged from v1.6.0.

### Task A8 — Bump plugin version

File : `.claude-plugin/plugin.json`
Action : `"version": "1.6.0"` → `"version": "1.6.1"`. No keyword change.
Verify : `grep -F '"version": "1.6.1"' .claude-plugin/plugin.json` returns 1 match.

### Task A9 — master.md pattern #1 depth update (no new ordinal)

File : `memory/master.md`

Locate the v1.6.0 fourth-data-point sentence in pattern #1 :

```
**v1.6.0 adds a fourth data-point**: `promptor` mirrors the Layer 0 pépite at `~/.claude/memory/layer0/pepite_promptor_template_anthropic_prompt_engineering.md` with the skill as canonical source of truth and the pépite as a synced cache for sessions outside Genesis-bootstrapped projects (inverting the read-direction of the prior three data-points where Layer 0 / specs were canonical and the skill was the implementation — here the marketplace-shipped skill is canonical, and the Layer 0 entry is the cache that propagates to non-Genesis sessions).
```

Append directly after that sentence (still inside the pattern #1 paragraph) :

```
**v1.6.1 extends this data-point in depth (no new ordinal)** — both surfaces (skill + synced-cache pépite) now carry the same SOTA-validated edits from `sota/anthropic-prompt-engineering_2026-04-19.md`, closing the 8.7/10 Best-at-date axis cap from the v1.6.0 honest rating and demonstrating that 1:1 mirror discipline survives external-canon rev-up without schema change.
```

Verify : `grep -F "v1.6.1 extends this data-point in depth" memory/master.md` returns 1 match.

### Task A10 — Create tests/runtime_auto_discovery_v1_6_1.md stub

File : `tests/runtime_auto_discovery_v1_6_1.md` (new file ; create `tests/` directory if absent).

Content : runbook template with sections :

```markdown
<!-- SPDX-License-Identifier: MIT -->
---
name: Runtime auto-discovery validation — v1.6.1
description: Reproducible runbook + evidence log for validating that the promptor skill surfaces in a fresh Claude Code session in a sibling project, on trigger-phrase input. Executed once in Phase B of v1.6.1 ship; future v1.6.x+ sessions can re-run the procedure to re-validate.
type: test
created: 2026-04-19
---

# Runtime auto-discovery — v1.6.1

## Procedure

1. **Spawn context**
   - Open a fresh Claude Code session in a sibling project cwd (any Genesis-bootstrapped repo on this machine except `project-genesis` itself to avoid in-repo discovery bias).
   - Candidates: Aurum (frozen read-only OK per scope-lock additive-only), Cyrano, Myconciergerie.
   - Ensure the Project Genesis plugin is installed at user scope or as a plugin — skill must be reachable by the fresh session.
   - No Edit/Write/commit during the validation session (read-only mode).

2. **Trigger**
   - Type one of the verbatim trigger phrases from the v1.6.0 binding rule :
     - "aide-moi à drafter un prompt pour claude opus 4.7 pour un agent extracteur"
     - OR "architecture un prompt production-grade pour [use case]"
     - OR "optimise ce prompt pour KV cache"

3. **Observe**
   - Did Claude auto-invoke the `promptor` skill? (check Skill tool invocation or offered-for-invocation message)
   - If invoked : what form — bare `promptor`, namespaced `project-genesis:promptor`, or other ?
   - If not invoked : was it offered as candidate ? Did Claude explicitly reference the skill ?

4. **Capture evidence**
   - Copy the relevant transcript excerpt (user prompt + Claude's first ~2 turns).
   - Redact project-specific identifiers (paths, API keys, user emails, project-internal naming) — preserve trigger phrase + skill reference.
   - Paste below in the Evidence section.

5. **Decision routing (AC#7)**
   - Bare `promptor` surfaces reliably → keep binding rule non-pinned, add runtime-validated note.
   - Namespaced form required → pin `project-genesis:promptor` in binding rule + SKILL.md.
   - Mixed → document both-surface precedence rule.

## Evidence

> (To be filled in Phase B execution.)

## Pinning decision

> (To be filled in Phase B execution. Routes to AC#7 Option A / B / C.)
```

Verify : `test -f tests/runtime_auto_discovery_v1_6_1.md && echo OK`.

### Task A11 — Zero-ripple verification (Layer A / Layer B diff probe)

Run four git-diff probes to verify zero edit outside the expected scope :

```bash
git diff main --stat skills/genesis-drop-zone/
git diff main --stat skills/genesis-protocol/
git diff main --stat skills/phase-minus-one/ skills/phase-5-5-auth-preflight/ skills/journal-system/ skills/session-post-processor/ skills/pepite-flagging/
git diff main --stat '.claude-plugin/' | grep -v '^1 file changed'  # should only show plugin.json
```

Expected: all four produce empty or only-expected-file output. Any leak = halt and investigate.

### Task A12 — Feat-core commit

Stage and commit the 7 files touched by A1–A10 in one commit :

- `.claude/docs/superpowers/research/INDEX.md`
- `skills/promptor/references/template.md`
- `skills/promptor/SKILL.md`
- `.claude-plugin/plugin.json`
- `memory/master.md`
- `tests/runtime_auto_discovery_v1_6_1.md`

Commit message :

```
feat(v1.6.1): SOTA-grounded template patches + runtime-validation runbook

C1 — Part A sampling params model-gated (Opus 4.7 removal → effort + thinking + max_tokens)
C2 — cache_control placement directive (4,096-token min on Opus 4.7)
C3 — MCP ServerName:tool_name mandatory reference form
L1 — Phase 1 Q2 acquires effort + thinking + max_tokens (not temperature)
L2 — Phase 1 Q1 metrics task-adaptive (not fixed Precision/Recall)

+ R8 INDEX registration for anthropic-prompt-engineering_2026-04-19.md
+ SKILL.md gate sentence pointing to new C-patches
+ master.md pattern #1 fourth-data-point depth update (no new ordinal)
+ tests/runtime_auto_discovery_v1_6_1.md runbook stub for Phase B
+ plugin.json 1.6.0 → 1.6.1

Zero Layer A / Layer B ripple verified via 4 git-diff probes.
```

## Phase B — Runtime validation (user-assisted)

### Task B1 — Spawn fresh Claude Code session

**Blocker on user action.** Session halts here after Phase A commit is pushed to branch. Claude provides the user with :

```
I need you to spawn a fresh Claude Code session in a sibling project for the v1.6.1
runtime auto-discovery check. Here's the procedure:

1. Open a new PowerShell window (or new Claude Code session in your IDE).
2. cd to ONE of : C:/Dev/Claude_cowork/aurum-ai/ (read-only), C:/Dev/Claude_cowork/cyrano/,
   C:/Dev/Claude_cowork/myconciergerie/.
3. Start `claude --dangerously-skip-permissions` in that cwd.
4. In that session, type ONE of these three phrases verbatim, then wait for Claude's
   first response turn (don't interact after) :
   - "aide-moi à drafter un prompt pour claude opus 4.7 pour un agent extracteur"
   - "architecture un prompt production-grade pour un agent de coordination"
   - "optimise ce prompt pour KV cache"
5. Copy the entire first response (verbatim). Redact any sensitive path, key, or
   identifier. Paste it back to ME in this session.
6. Close the sibling session without committing anything (do not run Edit / Write).
```

### Task B2 — Evidence capture and redaction

When user pastes the transcript excerpt, Claude :
1. Verifies it contains a trigger phrase match.
2. Identifies whether `promptor` was auto-invoked, offered, mentioned, or absent.
3. Asks user to confirm redaction choices if any identifiers slipped through.
4. Writes the redacted evidence into `tests/runtime_auto_discovery_v1_6_1.md` § Evidence.
5. Fills `tests/runtime_auto_discovery_v1_6_1.md` § Pinning decision based on observation.

### Task B3 — Feat-runtime commit

Stage `tests/runtime_auto_discovery_v1_6_1.md` (the filled version) and commit :

```
feat(v1.6.1): runtime auto-discovery evidence + pinning decision

Evidence captured from fresh Claude Code session in <sibling-project> cwd on 2026-04-19.
Trigger phrase : "<quote>"
Skill surfacing : <bare | namespaced | both | absent>
Pinning decision : AC#7 Option <A | B | C>

<N-line evidence excerpt with identifiers redacted.>
```

## Phase C — PR + merge + tag (post-Phase-B)

### Task C1 — Apply plan polish from plan-reviewer pass

After plan-reviewer agent runs, apply P1 + P2 fixes in a "plan polish" commit.

### Task C2 — R2.3.1 pre-flight + push branch

```bash
gh auth status | head -3  # verify active = myconciergerie-prog
git push -u origin feat/v1.6.1-runtime-and-sota
```

### Task C3 — Code-quality reviewer pass (optional, if time permits)

Run `code-reviewer` subagent on the diff against main. Bundle any surgical P1 fixes as one additional commit in the feat branch (a7b0740-style pattern from v1.6.0) — honest rating deduction if bundled.

### Task C4 — Open PR and merge

```bash
gh pr create --title "v1.6.1 — runtime auto-discovery + Anthropic prompt-eng SOTA grounding" --body "$(cat <<'EOF'
## Summary
- Closes v1.6.0 honest axis caps (Best-at-date 8.7 and Pain-driven 9.2) via R8 grounding + runtime validation.
- 3 SOTA-surfaced template patches (C1 sampling / C2 cache-breakpoint / C3 MCP naming) + 2 polish edits (L1 Q2 effort / L2 task-adaptive metrics).
- Runtime auto-discovery evidence captured from fresh sibling-project session.
- Binding-rule pinning decision resolved empirically (AC#7).

## Test plan
- [ ] R8 entry reachable at `research/sota/anthropic-prompt-engineering_2026-04-19.md` (326+ lines)
- [ ] `grep -cF "niveau d'effort" skills/promptor/references/template.md` == 2
- [ ] `grep -F "ServerName:tool_name" skills/promptor/references/template.md` == 1
- [ ] `grep -F "v1.6.1 extends this data-point in depth" memory/master.md` == 1
- [ ] `tests/runtime_auto_discovery_v1_6_1.md` § Evidence filled
- [ ] Zero Layer A / Layer B ripple (4 git-diff probes empty)
EOF
)"
```

Wait for any CI / reviewer ; squash-merge ; capture merge commit SHA + PR number.

### Task C5 — Tag

```bash
git fetch origin && git checkout main && git pull --ff-only
git tag -a v1.6.1 -m "v1.6.1 — runtime auto-discovery + Anthropic prompt-eng SOTA grounding"
git push origin v1.6.1
```

## Phase D — Layer 0 sync (post-merge, NOT in repo)

### Task D1 — Pépite sync with C/L patches

File : `~/.claude/memory/layer0/pepite_promptor_template_anthropic_prompt_engineering.md`
Action : apply the same C1+C2+C3+L1+L2 template edits verbatim. Bump synced-cache marker from "Last sync: v1.6.0 @ 2026-04-19" to "Last sync: v1.6.1 @ 2026-04-19".
Verify : `grep -F "Last sync: v1.6.1" /c/Users/conta/.claude/memory/layer0/pepite_promptor_template_anthropic_prompt_engineering.md && echo OK`.

### Task D2 — Binding-rule pinning decision landing (AC#7)

File : `~/.claude/memory/layer0/feedback_invoke_promptor_for_production_anthropic_prompts.md`
Action : update § "The rule" per Phase B outcome (AC#7 Option A, B, or C).
Verify : `grep -F "runtime validated 2026-04-19" /c/Users/conta/.claude/memory/layer0/feedback_invoke_promptor_for_production_anthropic_prompts.md && echo OK` (or alternate marker per chosen Option).

### Task D3 — Phase D sentinel file

Write `~/.claude/memory/layer0/_v1_6_1_layer0_sync_DONE_2026-04-19.md` with a short marker note :

```markdown
Phase D Layer 0 sync for v1.6.1 completed 2026-04-19.

- pepite_promptor...md : synced-cache marker bumped to v1.6.1 + 5 template patches (C1/C2/C3/L1/L2) applied.
- feedback_invoke_promptor...md : § "The rule" updated with runtime-validation note + pinning decision.
```

## Phase E — Chore(memory) tranche

### Task E1 — Create chore worktree

```bash
git worktree add .claude/worktrees/chore_2026-04-19_v1_6_1_session -b chore/v1.6.1-memory main
cd .claude/worktrees/chore_2026-04-19_v1_6_1_session
```

### Task E2 — CHANGELOG honest re-rating

File : `CHANGELOG.md` (worktree root)
Action : prepend `[1.6.1]` entry above `[1.6.0]` with :
- Added / Changed / Removed sections.
- 5-axis honest self-rating table (post-feat re-evaluation).
- Running-average delta line ("v1.6.0 = 8.88 → v1.6.1 = ?").
- Streak ≥ 9.0 counter updated.
- Note : if bundled code-quality reviewer fix in Phase C3, note the Self-contained axis deduction honestly.

### Task E3 — Session trace

File : `memory/project/session_v1_6_1_runtime_and_sota.md` (new)
Action : follow the pattern established by `session_v1_6_0_promptor_skill.md` — What shipped / Why / Three brainstorming questions resolved / Reviewer P1+P2 summary / Self-rating honest / Cross-skill-pattern data-points added / Hypothesis outcomes / Out-of-scope / What's next / PR + tag state.

### Task E4 — MEMORY.md pointer

File : `memory/MEMORY.md`
Action : append one `- [Session v1.6.1 ...](project/session_v1_6_1_runtime_and_sota.md)` line under `## Project`.

### Task E5 — Forward resume

File : `.claude/docs/superpowers/resume/2026-04-19_v1_6_1_to_<next>.md` (new)
Action : pattern from `2026-04-19_v1_6_0_to_v1_6_1_or_v1_5_2.md`. Candidates for next ship :
- v1.5.2 runtime dogfood (queued from v1.5.1 resume — now 2 cycles deep)
- v1.5.3 Friction #3 retirement-trigger semantics
- v1.7.0 new-skill introduction (Aurum-unfreeze-adjacent candidates if lock lifts)

### Task E6 — Chore PR + merge

```bash
gh pr create --title "chore(memory): v1.6.1 — CHANGELOG honest + session trace + MEMORY + resume"
```

Squash-merge. Git fetch + fast-forward main. Write Phase E sentinel if needed for next-session verification.

## Idempotency discipline

Every Edit task (A5, A6, A9, D1, D2, E4) is append-style or locate-then-replace. Before each such Edit, grep for the post-edit marker :

```bash
# Example for A9
grep -F "v1.6.1 extends this data-point in depth" memory/master.md && echo "A9 already applied — SKIP" || echo "A9 pending — APPLY"
```

If the marker grep returns a hit, skip the Edit. This protects against double-application if the session resumes mid-plan.

## Ship-gate checklist (Phase A final)

Before opening the PR :

- [ ] All 12 Phase A tasks verified with grep / test commands (use the Verify sub-tasks above)
- [ ] Zero Layer A / Layer B ripple (Task A11)
- [ ] Phase B evidence captured and committed
- [ ] Plan-reviewer pass clean (no open P1)
- [ ] R8 entry still fresh (expires 2026-04-26, today is 2026-04-19 — green for at least 7 days)
- [ ] gh auth active = myconciergerie-prog (R2.3.1)
- [ ] No untracked files in worktree besides `.claude/settings.local.json` and `.playwright-mcp/`

## Reviewer-driven fix policy

If spec-reviewer or code-quality-reviewer flags a P1 at any stage, follow the v1.6.0 a7b0740 pattern :
- Apply as a separate commit in the same feat branch.
- Commit message explicitly names the reviewer pass and the P1 finding number.
- Accept the Self-contained-axis deduction in the honest rating if the fix is not strictly in the original scope (v1.6.0 precedent : 9.5 → 9.3 on bundled-scope).
