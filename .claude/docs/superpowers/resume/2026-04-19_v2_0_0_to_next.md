<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-19 v2.0.0 → next
description: Handoff from v2.0.0 first MAJOR bump (drop subprocess Citations + new phase-auth-preflight skill + halt-card retirement + Anthropic-first BYOAI staging + genesis.myconciergerie.fr v3 vision concretization). Streak ≥ 9.0 advances to 2 (v1.6.3 9.30 + v2.0.0 9.20). Running avg 8.93 over 21 tagged versions. Tag chain at 21 versions (... v1.6.3 → v2.0.0). Next candidates : v2.0.1 F6 marketplace.description fix / v2.1.0 runtime evidence (live test of remediation card on fresh project) / v3.0 web mode kickoff (genesis.myconciergerie.fr scaffold).
type: resume
previous_session: v2.0.0 max-subscription-drop-subprocess
next_action: v2.0.1 F6 cosmetic OR v2.1.0 runtime evidence OR v3.0 web mode kickoff
---

# Resume — 2026-04-19 v2.0.0 → next

## What v2.0.0 shipped

**Tag v2.0.0** (PR #49 squash-merged as `55c0f68`). **First MAJOR bump in Genesis history.** Architectural shift drops v1.4.0 subprocess Citations API + anthropic SDK + ANTHROPIC_API_KEY dependency. New 9th skill `phase-auth-preflight` runs `claude auth status` JSON probe as Phase 0.0 (Layer A) / Step 0.0 (Layer B) pre-flight. v1.5.0 halt-with-remediation card retired.

**15 commits in feat tranche** : 4 spec/spec-polish/spec-extend ×2 + 2 plan/plan-polish + 1 collateral marketplace.json fix + 8 SD-1 to SD-8 implementation.

**Self-rating 9.20/10 honest** — streak ≥ 9.0 advances to 2 (v1.6.3 9.30 + v2.0.0 9.20). Running avg 8.93 (+0.01).

## Current state

- **Repo** : main at `55c0f68`. Tag chain 21 versions (… v1.6.3 → **v2.0.0**).
- **Worktrees retained** : `feat_2026-04-19_v2_max_subscription_design/` + `chore_2026-04-19_v2_0_0_session/` + prior forensic.
- **User state** : marketplace install confirmed at v1.6.3 from v1.6.3 session (D pré-flight pre-update). Will need `claude plugin update project-genesis@project-genesis-marketplace` to bump local install to v2.0.0 next session.
- **R8 cache** : sota entries TTL OK ; stack `claude-code-plugin-structure_2026-04-19.md` + `claude-code-session-jsonl-format_2026-04-19.md` expire 2026-04-20 (refresh next session if touched). `anthropic-python_2026-04-18.md` MOVED to archive in v2.0.0.
- **V3 vision captured** in `memory/master.md § "What v3 vision is"` : genesis.myconciergerie.fr concrete domain + 7 dev disciplines + BYOAI staging table + Lovable-style hosted SaaS model.

## Next session candidates

### Candidate A — v2.0.1 F6 marketplace.description PATCH (cosmetic)

`claude plugin validate` warns `metadata.description: No marketplace description provided`. CLI v2.1.x wants the description under a `metadata` key (not at root, where we removed it in v2 collateral fix `0f69522`). Mechanical : add `metadata.description` field to `marketplace.json` matching the description text. ~15 min ship.

**Pros** : closes the only outstanding warning ; trivial ; resets validate output to clean ✔.
**Cons** : extremely small surface, may not warrant a tag (could be bundled with B or C).

### Candidate B — v2.1.0 runtime evidence (live test on fresh project)

v2.0.0 shipped with documentary expectation for 4/5 phase-auth-preflight scenarios + 1 live test (authed-firstparty same as 9-skills probe). A real end-to-end runtime test would invoke `/genesis-drop-zone` in a fresh project folder (e.g. `C:/tmp/genesis-v2-test/`) and verify : (a) Phase 0.0 phase-auth-preflight runs first, (b) prints `✓ Auth Anthropic OK` line, (c) proceeds to Phase 0.1 welcome, (d) full drop-zone flow without API key requirement. Captures the v2 win as concrete evidence rather than projection.

**Pros** : closes the v1.5.0 → v1.6.x runtime-evidence-gap formally ; proves v2 architecture works end-to-end ; pain-driven axis would benefit from real-pain-relief observation.
**Cons** : ~2-3h work for evidence-only ship ; would tag as v2.0.1 PATCH or v2.1.0 MINOR depending on whether any code touches.

### Candidate C — v3.0 web mode kickoff (genesis.myconciergerie.fr scaffold)

Per v3 vision captured in master.md, kick off the v3 web mode infrastructure. Concrete starting points : (a) DNS reservation + subdomain config for `genesis.myconciergerie.fr` ; (b) initial Next.js / Vite scaffold for landing page with provider connector picker UI (Anthropic active + Gemini/OpenAI disabled per BYOAI staging) ; (c) initial Supabase project on VPS OVH per Layer 0 `infra_2026-04-18_supabase_vps_ovh_migration.md` ; (d) backend skeleton that vendors the CLI plugin skill code per v2 design discipline #5 (CLI = reference implementation, not parallel codebase).

**Pros** : moves toward the strategic v3 vision the user prioritized ; biggest blast-radius shift since v2 itself ; opens beta-tester pipeline.
**Cons** : substantial scope (~weeks of work) ; would need fresh brainstorming session for v3.0 spec ; NOT tagable as a single ship — would split into v3.0.0-alpha → v3.0.0-beta → v3.0.0 cycle.

### Candidate D — Aurum frozen scope lock review (deferred work)

Per `memory/project/aurum_frozen_scope_lock.md`, Aurum is frozen at `0b1de3d` until Genesis v1 ships. v2.0.0 is now shipped. The lock formally LIFTED at v1.0.0 ; the v1.x → v2.0.0 cycle was beyond the original lock scope. Next session could review whether to formally unlock + open Aurum v1 kickoff session.

**Pros** : honors the lock-scope agreement ; opens Aurum runtime work which has been pending since 2026-04-14.
**Cons** : Aurum work is a different project context ; would close the Genesis-as-active-focus chapter (v2.0.0 was a natural pause point already).

## Reco for next session

**Mon vote : Candidate A bundled with Candidate B (combined v2.1.0 ship)**. Reasons :
- A is trivial cleanup with zero risk
- B closes the runtime-evidence-gap formally with real-pain-relief observation
- Bundling them = single MINOR ship v2.1.0 with both warning-fix + live-evidence
- Defers C (web mode) for a properly-scoped v3 brainstorming session
- Defers D (Aurum) for explicit user-level "what's next strategically" decision

**Auto-critique** : If user wants to commit hard to v3 web mode now (as their input "à terme le modèle de Genesis sera sur le modèle de Lovable" + "ces idées sont à noter dès maintenant pour orienter le dev dans ce sens" suggests), Candidate C kickoff IS the right next session. The v2 vision capture work was foundational ; actually building it is the natural next step. In that case A+B can wait until first v3 milestone.

## Exact phrase for next session

```
mode autonome  Je reviens après v2.0.0 first MAJOR bump (drop subprocess Citations + phase-auth-preflight 9th skill + halt-card retirement + genesis.myconciergerie.fr v3 vision capture + BYOAI staging Anthropic-first + 7 dev disciplines load-bearing). 15 commits feat tranche, PR #49 squash 55c0f68, tag v2.0.0. 9.20 HONEST streak advances to 2 (v1.6.3 9.30 + v2.0.0 9.20), running avg 8.93. Lis resume v2.0.0→next + session trace. Candidats : (A) v2.0.1 F6 marketplace.description fix cosmetic ~15min / (B) v2.1.0 runtime evidence live test ~2-3h / (C) v3.0 web mode kickoff genesis.myconciergerie.fr (substantial, fresh brainstorming required) / (D) Aurum frozen scope lock review post-v2-ship. Propose reco + auto-critique. /using-superpowers
```

**PowerShell launcher** (one-line, copy-paste ready) :

```
cd C:/Dev/Claude_cowork/project-genesis; claude --dangerously-skip-permissions "mode autonome  Je reviens après v2.0.0 first MAJOR bump (drop subprocess Citations + phase-auth-preflight 9th skill + halt-card retirement + genesis.myconciergerie.fr v3 vision capture + BYOAI staging Anthropic-first + 7 dev disciplines load-bearing). 15 commits feat tranche, PR #49 squash 55c0f68, tag v2.0.0. 9.20 HONEST streak advances to 2 (v1.6.3 9.30 + v2.0.0 9.20), running avg 8.93. Lis resume v2.0.0→next + session trace. Candidats A v2.0.1 F6 cosmetic / B v2.1.0 runtime evidence / C v3.0 web mode kickoff / D Aurum unlock review. Propose reco + auto-critique. /using-superpowers"
```
