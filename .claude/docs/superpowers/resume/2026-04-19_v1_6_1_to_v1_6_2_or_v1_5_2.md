<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-19 v1.6.1 → v1.6.2 OR v1.5.2
description: Handoff from v1.6.1 runtime auto-discovery + SOTA grounding PATCH. Closes v1.6.0 Best-at-date axis cap fully (R8 canonical grounded) and Pain-driven axis cap partially (Layer 0 pépite surface runtime-validated ; Skill-tool plugin-installed path deferred). Self-rating 9.18/10 honest (vs projected 9.26 ; Self-contained dropped 0.3 on reviewer-driven P2 bundling + plan-writing discipline first-pass 6P1s). **Streak ≥ 9.0 advances to 3 consecutive** (v1.5.1 = 9.12, v1.6.0 = 9.02, v1.6.1 = 9.18). Running avg ≈8.90 (+0.02). Pattern #4 eighth data-point (orthogonal meta-skill PATCH zero-ripple). Candidates for next ship : v1.6.2 Skill-tool plugin-installed runtime evidence (~1-2h, closes H1 fully), v1.5.2 runtime dogfood (~2-3h, queued 2 cycles), v1.5.3 retirement semantics (~4-5h), v1.7.0 new skill (if candidate).
type: resume
previous_session: v1.6.1 runtime + SOTA
next_action: v1.6.2 Skill-tool runtime evidence OR v1.5.2 runtime dogfood OR other
---

# Resume — 2026-04-19 v1.6.1 → v1.6.2 OR v1.5.2

## What v1.6.1 shipped

**Tag v1.6.1** (PR #43 squash-merged as `2d634b9`). **First PATCH on v1.6.x line.** Closes the two priced-in axis caps of v1.6.0 :

- **Best-at-date 8.7 → 9.3** — 326+ line SOTA R8 entry `sota/anthropic-prompt-engineering_2026-04-19.md` (42+ citations, canonical Anthropic migration guide primary for Opus 4.7 sampling-removal claim).
- **Pain-driven 9.2 → 9.3** (partial) — runtime evidence from fresh Claude Code session in Aurum sibling cwd (Genesis plugin NOT installed) validated Layer 0 pépite + binding-rule surface fires cleanly on trigger phrases. Skill-tool plugin-installed path NOT exercised — deferred to v1.6.2+.

**8 commits in feat tranche** (squash-merged) :
- `6fbce7b` spec (+147 l) + `93a3db4` spec-polish (3 P1 + 4 P2 reviewer fixes, canonical Anthropic citation swap)
- `c61c952` plan (+468 l, 13 tasks across 5 phases) + `5c197a3` plan-polish (6 P1 + 5 P2 reviewer fixes — edit-target precision, shell-escape, zero-ripple probe rewrite, idempotency table expansion, pattern #4 task gap closed)
- `5cc6e83` feat-core (6 files +97/-10 — R8 INDEX + 5 template patches + SKILL.md gate + plugin.json bump + master.md depth updates + tests/ stub)
- `d59a09c` feat-runtime (tests/runtime_auto_discovery_v1_6_1.md filled with evidence run 1)
- `682f522` reviewer-driven P2 fix (R8 § "What this challenges" item 1 citation-order consistency, a7b0740-style bundled)

**Phase D Layer 0 sync (post-merge, NOT in repo)** :
- `~/.claude/memory/layer0/pepite_promptor_template_anthropic_prompt_engineering.md` — 5 template patches applied verbatim, synced-cache marker bumped `v1.6.0 @ 2026-04-19` → `v1.6.1 @ 2026-04-19`
- `~/.claude/memory/layer0/feedback_invoke_promptor_for_production_anthropic_prompts.md` — § "The rule" Skill-first preamble gains "runtime-validated 2026-04-19 v1.6.1 on Layer 0 pépite surface" marker. Pinning decision AC#7 Option A (non-pinned)
- Sentinel `_v1_6_1_layer0_sync_DONE_2026-04-19.md` written

**Self-rating 9.18/10 honest** (vs projected 9.26, Self-contained dropped 0.3) :

| Axis | Projected | Honest | Notes |
|---|---|---|---|
| Pain-driven | 9.4 | 9.3 | Partial H1 — Layer 0 pépite surface runtime-validated ; Skill-tool plugin-installed path deferred |
| Prose | 9.2 | 9.2 | Template patches integrate cleanly ; R9 respected |
| Best-at-date | 9.3 | 9.3 | R8 canonical Anthropic primary for load-bearing claim |
| Self-contained | 9.3 | **9.0** | Reviewer P2 bundling (−0.2 a7b0740-style) + plan first-pass 6P1s (−0.1) |
| Anti-Frankenstein | 9.1 | 9.1 | 5 patches 1:1 to R8 findings |
| **Mean** | **9.26** | **9.18** | **Streak ≥ 9.0 advances to 3 consecutive** |

Running average post-v1.6.1 honest ≈ **8.90/10** (+0.02 vs v1.6.0 running 8.88 ; 18 tagged ratings total).

## Phase D confirmation checklist

Phase D Layer 0 sync was completed in-session. Verify in next session :

```bash
test -f /c/Users/conta/.claude/memory/layer0/_v1_6_1_layer0_sync_DONE_2026-04-19.md && echo "PHASE D DONE"
grep -cF "v1.6.1 @ 2026-04-19." /c/Users/conta/.claude/memory/layer0/pepite_promptor_template_anthropic_prompt_engineering.md  # should be 1
grep -cF "Pour Claude Opus 4.7+" /c/Users/conta/.claude/memory/layer0/pepite_promptor_template_anthropic_prompt_engineering.md  # should be 1
grep -cF "runtime-validated 2026-04-19 v1.6.1" /c/Users/conta/.claude/memory/layer0/feedback_invoke_promptor_for_production_anthropic_prompts.md  # should be 1
```

If any check fails, re-run Phase D Tasks D1/D2/D3 (idempotency markers protect against double-application).

## Current state

- **Repo** : `myconciergerie-prog/project-genesis`. Main at `2d634b9`. Tag chain …v1.5.0 → v1.5.1 → v1.6.0 → **v1.6.1**. 18 tagged versions total.
- **Worktrees retained** : `.claude/worktrees/feat_2026-04-19_v1_6_1_runtime_and_sota/` (feat, R2.5 forensic) ; `.claude/worktrees/chore_2026-04-19_v1_6_1_session/` (chore, this session) ; prior v1.5.1 + v1.6.0 worktrees still on disk per R2.5.
- **gh auth** : two accounts. Active flip-back to `avelizy-cloud` reproduced mid-session — `gh auth switch -u myconciergerie-prog` alone was NOT sufficient for `gh pr create`. Resolution required `GH_TOKEN=$(gh auth token -u myconciergerie-prog) gh pr create` env override. Second data-point for R2.3.1 — candidate for Layer 0 amplification (encode GH_TOKEN prefix as mandatory pre-flight for write gh calls).
- **Skill count** : 8 (six Layer B + one Layer A `genesis-drop-zone` + one orthogonal meta-skill `promptor`). Privilege map unchanged from v1.6.0 (2 none-class : `journal-system`, `promptor` ; 6 with concentrated privileges). Template patches in `skills/promptor/references/template.md` : sampling params gated by model, cache_control placement explicit, MCP naming mandatory, Phase 1 questions updated.
- **Cross-skill-patterns** : 4 named ; pattern #4 now at **eight data-points** (eighth = v1.6.1 orthogonal meta-skill PATCH zero-ripple) ; pattern #1 at 4 data-points (v1.6.0 fourth + v1.6.1 depth update, no new ordinal).
- **Layer 0** : pépite synced-cache marker at v1.6.1 ; binding rule runtime-validated on pépite surface (Option A non-pinned). Skill-tool plugin-installed path runtime validation still pending.
- **R8 cache state** :
  - `anthropic-prompt-engineering_2026-04-19.md` (sota 7d) — expires 2026-04-26. **New entry, fresh.**
  - `anthropic-auth-and-oauth-status_2026-04-19.md` (sota 7d) — expires 2026-04-26.
  - `living-memory-and-supersession-patterns_2026-04-18.md` (sota 7d) — expires 2026-04-25.
  - `v2_promptor_fusion_landscape_2026-04-17.md` (sota 7d) — expires 2026-04-24.
  - `gh-cli-single-click-auth_2026-04-16.md` (sota 7d) — expires 2026-04-23.
  - `zero-friction-bootstrap-ux_2026-04-16.md` (sota 7d) — expires 2026-04-23.
  - **`claude-code-plugin-structure_2026-04-19.md` (stack 1d) — expires 2026-04-20** (today). Refresh if next session opens 2026-04-21+.
  - **`claude-code-session-jsonl-format_2026-04-19.md` (stack 1d) — expires 2026-04-20**. Same.

## Next session candidates

### v1.6.2 — Skill-tool plugin-installed auto-discovery runtime evidence

Closes **H1 full closure** that v1.6.1 left partial. Pain source : v1.6.1 Pain-driven axis honest 9.3 (not 9.4 projection) because runtime evidence only covered the Layer 0 pépite surface (non-Genesis sibling). Need a plugin-installed sibling session to observe the Skill-tool invocation form (bare `promptor` vs `project-genesis:promptor` namespaced vs mixed) and route AC#7 final pinning decision if the Option A current status needs amendment.

Scope :
1. Install the `project-genesis` plugin user-scope (`claude plugin add <repo>` or `--plugin-dir`-equivalent) in a sibling project that does NOT have it yet — Cyrano or Myconciergerie candidates (Aurum stays read-only).
2. Spawn fresh Claude Code session in the plugin-installed sibling cwd.
3. Type verbatim trigger phrase → observe Skill-tool invocation form. Document in `tests/runtime_auto_discovery_v1_6_1.md` § Evidence as run 2 (do not overwrite run 1 per runbook "Re-run guidance").
4. If invocation form is bare → confirm Option A status explicitly (append "runtime-validated 2026-04-19 v1.6.2 on Skill-tool plugin-installed path" marker).
5. If invocation requires namespaced form → route to Option B (pin `project-genesis:promptor` in binding rule + sync note in SKILL.md frontmatter description). Honest Pain-driven axis closes 9.3 → 9.4+ either way.
6. CHANGELOG re-rating — Pain-driven cap fully closed.

**~1-2h** (installation + runtime + evidence + commit + PR + chore). **Strongest follow-on for v1.6.x line closure.** Recommended.

### v1.5.2 — Runtime dogfood (not paper-trace) of Phase 0.4/0.5/archive/halt

Still queued from v1.5.1 resume (now 2 cycles deep). Scope : spawn separate Claude Code session in each `C:/tmp/<fixture>/` cwd, invoke `/genesis-drop-zone` naturally, observe dispatch. Strengthens Pain-driven axis on v1.5.x line.

**~2-3h** including session-spawning overhead. Natural continuation of v1.5.1 discipline.

### v1.5.3 — Friction #3 retirement-trigger semantics for Phase 0.4

Phase 0.4 Retirement class triggers whenever a previously-populated field becomes null-class in new drop. Could flood arbitration card on sparse drops. Design work : should Retirement fire only on explicitly-retired fields, not on omission?

**~4-5h** for design + spec + plan. Lower urgency than v1.6.2 full-closure or v1.5.2 runtime dogfood.

### v1.7.0 — New skill introduction (if user has clear candidate)

Lower priority until v1.6.x line is fully closed via v1.6.2 Skill-tool runtime evidence.

## Reco for next session

**Mon vote : v1.6.2 Skill-tool plugin-installed runtime evidence**, then v1.5.2 runtime dogfood. Why :

- v1.6.1 Pain-driven axis capped at 9.3 (not 9.4 projection) specifically because Skill-tool path was NOT exercised. v1.6.2 closes that gap surgically.
- Small scope (~1-2h) = lowest total time investment to reach full v1.6.x line closure.
- Pattern matches v1.6.1 "close current minor line before stacking" discipline (which itself matched v1.5.1 → v1.5.0 honesty correction).
- Unlocks binding-rule pinning final decision if empirical evidence routes to Option B/C instead of current Option A. If evidence confirms Option A for both surfaces, the rule stays cleaner (no namespace pin needed).
- v1.5.2 runtime dogfood is defensible but 2-3× longer and addresses a different axis. v1.5.x line is less load-bearing as of 2026-04-19 (11 ships since v1.5.0).

**Auto-critique** : v1.6.2 could be perceived as over-investing in runtime-validation-for-its-own-sake. Counter : the v1.6.1 honest 9.3 explicitly priced in one axis cap on the deferred Skill-tool path ; shipping v1.7.0 or v1.5.2 without closing that cap would repeat the v1.5.0 mistake at the smallest possible scale (claiming honest-rating while leaving a priced-in validation unticked). The v1.5.1 dogfood-first + v1.6.1 runtime-validation discipline both say : ground in evidence before iterating.

Counter-counter : if user priority is on v1.5.x line or new v1.7.0 skill, v1.6.2 can land as a background ≤1h polish ship between larger tracks without blocking.

## R2.3.1 GH_TOKEN pattern — empirical reinforcement

Mid-session `gh` active-account flipped back to `avelizy-cloud` even though `gh auth switch` had set `myconciergerie-prog` as active at session-open. `gh auth status` showed the correct account ; `gh api user --jq .login` returned the OTHER account ; `gh pr create` failed with `must be a collaborator`. Resolution : `TOKEN=$(gh auth token -u myconciergerie-prog) && GH_TOKEN="$TOKEN" gh pr create ...`.

This is the **second runtime data-point** confirming R2.3.1 and the Layer 0 `workflow_github_and_tooling.md` GH_TOKEN-env-override pattern. Candidate for Layer 0 amplification : encode the `GH_TOKEN` env prefix as **mandatory pre-flight before any write gh call**, not optional fallback.

## Exact phrase for next session

Open Claude Code in `C:/Dev/Claude_cowork/project-genesis/` and paste :

```
mode autonome  Je reviens après le ship v1.6.1 (runtime auto-discovery + Anthropic prompt-eng SOTA R8 grounding PATCH — 326+ line R8 entry canonical Anthropic primary pour claim sampling-removal 4.7, 5 template patches C1/C2/C3/L1/L2 mapped 1:1 aux R8 findings, runtime evidence sibling Aurum non-Genesis validant Layer 0 pépite surface, AC#7 Option A non-pinned with runtime-validation marker, 8 commits feat dont 682f522 reviewer-driven a7b0740-style, pattern #4 eighth data-point orthogonal meta-skill PATCH zero-ripple, pattern #1 depth update v1.6.0 fourth no-new-ordinal, self-rating 9.18 HONEST [Pain-driven 9.3 partial H1 Skill-tool plugin-installed path deferred, Prose 9.2, Best-at-date 9.3, Self-contained 9.0 -0.3 reviewer P2 bundling + plan-writing first-pass 6P1s, Anti-Frankenstein 9.1], running avg 8.90 +0.02, STREAK ≥ 9.0 = 3 consécutif). Lis le resume v1.6.1 → v1.6.2 OR v1.5.2 et le session trace v1.6.1. Candidats next ship : (1) v1.6.2 Skill-tool plugin-installed runtime evidence ~1-2h ferme H1 totalement, (2) v1.5.2 runtime dogfood not paper-trace ~2-3h queued 2 cycles, (3) v1.5.3 retirement-trigger semantics ~4-5h, (4) v1.7.0 new skill. Propose avec reco + auto-critique post deep stack check. /using-superpowers
```

**PowerShell launcher** (one-line) :

```
cd C:/Dev/Claude_cowork/project-genesis; claude --dangerously-skip-permissions "mode autonome  Je reviens après le ship v1.6.1 (runtime + SOTA PATCH — R8 canonical Anthropic + 5 template patches + runtime evidence Aurum sibling, AC#7 Option A non-pinned, 8 commits feat dont reviewer-driven 682f522, pattern #4 eighth data-point, pattern #1 depth update, self-rating 9.18 HONEST, running avg 8.90 +0.02, STREAK ≥ 9.0 = 3). Lis le resume v1.6.1 → v1.6.2 OR v1.5.2 et le session trace v1.6.1. Candidats: v1.6.2 Skill-tool runtime evidence / v1.5.2 runtime dogfood / v1.5.3 retirement semantics / v1.7.0 new skill. Propose reco + auto-critique post deep stack check. /using-superpowers"
```
