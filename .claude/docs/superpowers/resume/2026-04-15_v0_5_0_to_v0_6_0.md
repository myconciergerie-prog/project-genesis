<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-15 v0.5.0 → v0.6.0 next milestone
description: Handoff from the 2026-04-15 session that shipped v0.5.0 (session-post-processor skill with halt-on-leak gate, after an R8 research refresh) to the next session whose job is to either implement pepite-flagging (last independent skill stub) or the first executable Python module for session-post-processor, targeting v0.6.0 at 8.0/10 floor.
type: resume
previous_session: 2026-04-15 v0.5.0 Session post-processor skill
next_action: Pick pepite-flagging OR session-post-processor run.py implementation; create feat worktree with granular commits; tag v0.6.0 at 8.0/10 floor
---

# Resume prompt — 2026-04-15 v0.5.0 → v0.6.0

## Context — what the previous session did

The 2026-04-15 session (v0.5.0) picked up the v0.4.0 → v0.5.0 handoff and delivered, on top of an R8 research refresh:

1. **Temp-directory audit** — full sweep of `C:/tmp/`, both `~/.claude/projects/C--Dev-Claude-cowork-project-genesis*/` slugs (including a previously-unknown `-2026` slug from a prior project rename), git reflog / fsck / stash / branches / worktrees. **Confirmed no Claude Code JSONL trace of any v0.4.0 session** — the lost PowerShell-window work was manual shell / editor state, never committed, never reached Claude Code, unrecoverable by construction.
2. **R8 research refresh as prerequisite** — the existing `claude-code-session-jsonl-format_2026-04-14.md` entry expired today and was confidence `medium`. Sampled the real `a086701e.jsonl` on disk (174 records, v0.3 feat+chore session), verified the outer vs inner type taxonomy, wrote `claude-code-session-jsonl-format_2026-04-15.md` at confidence `high`, moved the old entry to `research/archive/`, updated `research/INDEX.md`. First application of the R8 mid-session refresh rule.
3. **Feat worktree** at `.claude/worktrees/feat_2026-04-15_session-post-processor-skill/` off `feat/2026-04-15_session-post-processor-skill`
4. **8 granular commits** inside the feat branch (research refresh → SKILL.md → jsonl-parser → redaction-patterns → markdown-emitter → install-manifest → verification → version bump). First application of the granular-commit discipline from the v0.4.0 PowerShell incident.
5. **6 new files** under `skills/session-post-processor/` totalling ~51 KB, every file SPDX-headered. Full list in `memory/project/session_v0_5_0_skill_session_post_processor.md`.
6. **plugin.json bumped** to `0.5.0`
7. **CHANGELOG v0.5.0 entry** with 5-axis self-rating averaging **8.4/10** (floor 8.0; below v0.4.0's 8.8 intentionally because surface is larger and Python 3.10+ is a genuine self-containment cost; running average across v0.2 → v0.5 = 8.25/10, on track for v1 target 8.5/10).
8. **PR #8** opened via `GH_TOKEN="$GH_TOKEN" gh pr create`, squash-merged at commit `49994fe` via `GH_TOKEN="$GH_TOKEN" gh pr merge 8 --squash` (no `--delete-branch` per R2.3)
9. **Tag `v0.5.0`** created on `49994fe` and pushed to origin
10. **Chore worktree** at `.claude/worktrees/chore_2026-04-15_v0_5_0-session-memory-resume/` off `chore/2026-04-15_v0_5_0-session-memory-resume` for the session memory entry + this resume prompt

## Current state at session handoff

- **Repo**: `C:\Dev\Claude_cowork\project-genesis\`
- **GitHub**: `myconciergerie-prog/project-genesis` (private)
- **main branch**: at `49994fe` (feat squash), then updated by the chore PR that ships this file
- **Latest tag**: `v0.5.0` on `49994fe`
- **Self-rating**: v0.5.0 = **8.4/10**. Target for v0.6.0 = **8.0/10 floor**. Target for v1.0.0 average = **8.5/10**
- **Running average**: (7.6 + 8.2 + 8.8 + 8.4) / 4 = **8.25/10** — on track for v1 target
- **Working tree**: expected clean at session open. Minor lag behind origin possible on the main fast-forward

## Anti-Frankenstein gate — still holding

Four skills shipped so far. **Two** remaining stubs:

- `pepite-flagging/` (independent — red-light discovery flagging with cross-project routing per `specs/v1_pepite_discovery_flagging.md`)
- `genesis-protocol/` (orchestrator — should land last, once every phase is implemented)

Plus a **new candidate** that arose from v0.5:

- `session-post-processor/run.py` — executable Python module implementing the pipeline spec frozen in v0.5.0. Not a new skill, but the first time a Genesis skill ships a runnable file instead of a spec-only Markdown. Gated on the three-run manual dogfood (run 1 = v0.5 session itself, runs 2 and 3 pending)

## The next concrete action — v0.6.0: pick one of two

### Option A — `pepite-flagging` skill

The last **independent skill stub**. Canonical spec is `.claude/docs/superpowers/specs/v1_pepite_discovery_flagging.md`. Red-light criteria for flagging cross-project discoveries during research, with explicit consent before propagation to sibling projects.

**Why now**: it is the last independent skill. Once shipped, only the orchestrator (`genesis-protocol`) remains and v1 ships.

**Complexity**: medium. Heuristic classification is softer than the session-post-processor's deterministic pipeline, so the rating ceiling is lower. Target 8.0/10 floor, realistic ceiling 8.3/10.

### Option B — `session-post-processor/run.py` implementation

Promote the v0.5.0 spec to an executable Python module. The spec is already frozen in the six skill files; `run.py` is a straightforward implementation pass using Python 3.10+ stdlib.

**Why now**: the v0.5.0 session shipped the spec but no executable. The first dogfood run of the skill needs a Python entry point, which is currently a blocker on the three-run gate. Without `run.py`, the three dogfood runs cannot happen and hook wiring stays deferred indefinitely.

**Complexity**: medium-to-large. The spec is detailed (6 files, ~51 KB) and the implementation must match exactly — a divergence between spec and implementation is a v0.5 regression. Target 8.0/10 floor, realistic ceiling 8.5/10 because a clean implementation matching a frozen spec lands high.

### Selection rubric for the next session

**Pick whichever pain point is most concrete at the time**. As of the v0.5.0 handoff:

- `pepite-flagging`'s pain is the cross-project research sharing rule in Layer 0 has no implementation — research entries stay project-local by default. This is a latent pain, not an acute one.
- `session-post-processor/run.py`'s pain is the skill itself is unusable without it — the three-run dogfood gate is blocked. This is an **acute** pain that compounds if deferred (every new session makes the "archive all past sessions" backlog larger).

**Suggested pick**: `session-post-processor/run.py` because the acute pain outweighs the latent one. `pepite-flagging` can slide to v0.7.0 without compound cost; `run.py` cannot.

If the user disagrees and wants `pepite-flagging` first, that is defensible — both paths land at v1.0 eventually, and picking the one the user has more energy for is a reasonable heuristic.

### Subtasks for the v0.6.0 session

1. **Sync main**: `git fetch origin && git pull --ff-only origin main`
2. **Pick the option** — apply the rubric above, or justify a different choice in the commit message
3. **Create feat worktree**: `feat/2026-04-XX_<milestone>` under `.claude/worktrees/`
4. **For Option B specifically** (session-post-processor run.py):
   - Read all 6 existing `skills/session-post-processor/*.md` + `install-manifest.yaml` as the frozen spec
   - Implement `run.py` using **only Python stdlib** (`json`, `re`, `os`, `pathlib`, `datetime`) — no pip installs, no `requests`, no `yaml` (frontmatter emitted as plain text)
   - Match the spec exactly: if the spec says "apply patterns in this order", the code applies them in that order; if the spec says "deleted on leak", the code deletes on leak
   - Run the skill on **this session's JSONL** as the first dogfood. The JSONL path will be `~/.claude/projects/C--Dev-Claude-cowork-project-genesis/<current-session-uuid>.jsonl` and the slug derivation must produce a file there
   - Verify the halt-on-leak gate **catches a deliberately-introduced test secret** before shipping — add a test vector that matches `github_pat_finegrained`, confirm the archive is deleted, remove the test vector, re-run, confirm clean archive
   - Emit the first real archive to `memory/project/sessions/2026-04-XX_<slug>.md` and **commit it as part of the PR** — the first dogfood is a durable artefact, not a throwaway
5. **For Option A specifically** (pepite-flagging skill):
   - Read `.claude/docs/superpowers/specs/v1_pepite_discovery_flagging.md` in full before writing any skill file
   - Structure follows the journal-system convention (speech-native, minimal consent surface, 1:1 mirror of the spec)
   - Expect 5-6 files: `SKILL.md`, `trigger-criteria.md` (the red-light rules), `pepite-format.md` (the frontmatter schema for `memory/pepites/*.md`), `cross-project-routing.md` (how to propagate to sibling project memory dirs), `install-manifest.yaml`, `verification.md`
6. **Update `CHANGELOG.md`** with a v0.6.0 entry + 5-axis self-rating, floor **8.0/10**. Below 8.0 is acceptable only with explicit justification in the notes section.
7. **Bump `plugin.json`** to `0.6.0`
8. **Granular commits inside the feat branch** — one per file for Option A, or one per logical unit (parser, redactor, emitter, verifier, dogfood archive) for Option B. Discipline is now the default, not an exception.
9. **Push, PR, squash merge, tag** — same flow as v0.4 and v0.5.
10. **Memory entry**: `memory/project/session_v0_6_0_<milestone>.md`
11. **Resume prompt** for v0.7.0

### Reinforced disciplines (cumulative)

- **R8 mid-session refresh** — if any R8 entry is expired or confidence `medium`, refresh before depending on it. First commit in the feat branch is the research refresh (not the first skill file).
- **Granular commits inside the feat branch** — one per file or one per logical unit, squashed at merge time. No downside.
- **Manual-only before hook wiring** — the session-post-processor skill still needs runs 2 and 3 before any `SessionEnd` wiring. v0.6 is run 2 if Option B is picked.
- **Halt-on-leak as single privileged operation** — the session-post-processor is the only Genesis skill so far that deletes files. Keep that privilege concentrated.

### Acceptance criteria for v0.6.0

Common to both options:

- Every new file has an SPDX header
- `CHANGELOG.md` updated with v0.6.0 entry and self-rating ≥ 8.0/10 (or explicit justification)
- `plugin.json` version = `0.6.0`
- PR cleanly squash-merged to origin main
- Git tag `v0.6.0` on origin
- Worktree retained per R2.5
- Granular commits inside the feat branch

Option-specific:

- **Option B (run.py)**: the skill successfully archives this session's own JSONL (or v0.6's own JSONL) as the first dogfood, the halt-on-leak gate catches a test secret, the archive is committed alongside the code, and `memory/project/sessions/INDEX.md` is updated
- **Option A (pepite-flagging)**: the skill's trigger criteria are 1:1 with `v1_pepite_discovery_flagging.md`, every new file SPDX-headered, install-manifest creates `memory/pepites/` + `INDEX.md` idempotently

### Things to NOT do in the v0.6.0 session

- **Do not implement both options** — one milestone per version, anti-Frankenstein discipline
- **Do not wire `SessionEnd` hooks** even if Option B is picked — run 2 is not run 3
- **Do not vendor external JSONL parsers** — stdlib only, the spec is clear on this
- **Do not rewrite any of the four shipped skills** — all stable
- **Do not drift `journal-system` from Layer 0** — 1:1 mirror discipline still applies
- **Do not skip the self-rating block** — R10.3 gate
- **If the user says `frankenstein`**, back out of the last proposal

## Open questions worth flagging before v0.6.0 starts

1. **Option A or B?** — the rubric suggests B (session-post-processor run.py) because the pain is acute. User may have a different preference. Ask at the start.
2. **Dogfood archive commit vs gitignore** — if Option B is picked, should the first dogfood archive be committed as a durable artefact, or gitignored as ephemeral personal data? **Suggested**: commit. The archive is redacted by construction, it is small (~30 KB per session), and the git history is the right place to keep it. If later sessions reveal the archive volume is too high, a subsequent version can gitignore them and keep only the INDEX.
3. **Slug-collision YELLOW warning** — should the v0.6 run.py already implement the multi-slug YELLOW check (v0.5 gap) or defer to v0.7+? Suggested: implement if it's < 20 lines, defer if it requires significant logic.
4. **Test vector injection mechanism** — for the halt-on-leak sanity check, the test vector must be injected without persisting to disk (otherwise it's a real leak). Suggested: inject into an in-memory record list before the emitter runs, confirm the gate fires before the write attempt, then remove and re-run.

These are not blockers — the next session decides on the fly.

## Exact phrase for the next session

Open Claude Code in `C:\Dev\Claude_cowork\project-genesis\` and say:

```
On reprend v0.6.0 Project Genesis. Lis le resume prompt le plus récent
dans .claude/docs/superpowers/resume/, puis choisis Option A
(pepite-flagging skill) ou Option B (session-post-processor/run.py
executable). Suggested: Option B — la pain est aiguë parce que le gate
à trois runs de dogfood est bloqué sans run.py. Target v0.6.0 à 8.0/10
floor. Commits granulaires dans le feat branch, premier dogfood commité
si Option B.
```

Claude Code then runs R1.1, creates the worktree, reads this file + the
previous session memory, confirms or overrides the option, and implements.

## References the next session will consume

- `memory/project/session_v0_5_0_skill_session_post_processor.md` — prior session memory with the temp audit, research refresh, and skill structure
- `.claude/docs/superpowers/research/stack/claude-code-session-jsonl-format_2026-04-15.md` — on-disk verified schema (TTL expires 2026-04-16 — refresh at session start if expired)
- `.claude/docs/superpowers/specs/v1_pepite_discovery_flagging.md` — canonical spec for Option A
- `skills/session-post-processor/*.md` + `install-manifest.yaml` — the frozen spec for Option B's run.py
- `skills/journal-system/` — structural model for Option A (speech-native skill, minimal consent surface)
- `skills/phase-5-5-auth-preflight/` + `skills/phase-minus-one/` — structural models for larger skills if Option A turns out to need them

## Farewell notes to the next session's Claude

v0.5.0 shipped at 8.4/10 with two **first-time disciplines** applied cleanly: granular commits inside the feat branch (8 commits before squash), and R8 mid-session refresh (research first, skill second). Both proved cheap and defensive. Keep them as defaults going forward.

The running average is 8.25/10 with two milestones to go. The v1 target of 8.5/10 is achievable if at least one of the remaining two lands above 8.5. `session-post-processor/run.py` has the best chance because it implements a frozen spec — a clean match is the straightest path to a high rating. `pepite-flagging` has a softer ceiling because its subject matter (heuristic classification) is inherently less deterministic.

Either option is valid. Pick based on current pain, not elegance.

The recursive loop continues. Good luck.
