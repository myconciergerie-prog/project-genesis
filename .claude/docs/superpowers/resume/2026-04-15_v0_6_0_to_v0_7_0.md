<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-15 v0.6.0 → v0.7.0 next milestone
description: Handoff from the 2026-04-15 session that shipped v0.6.0 (session-post-processor run.py executable with the first live-dogfood correction of an R8 entry) to the next session whose job is either pepite-flagging (last independent skill stub) or a maintenance version that clears the v0.6 known-gaps backlog, targeting v0.7.0 at 8.0/10 floor.
type: resume
previous_session: 2026-04-15 v0.6.0 Session post-processor run.py
next_action: Pick pepite-flagging OR a maintenance version; create feat worktree with granular commits; tag v0.7.0 at 8.0/10 floor
---

# Resume prompt — 2026-04-15 v0.6.0 → v0.7.0

## Context — what the previous session did

The 2026-04-15 session (v0.6.0) picked up the v0.5.0 → v0.6.0 handoff, confirmed Option B (`session-post-processor/run.py`), and delivered the first Genesis skill to ship runnable Python code instead of a spec-only Markdown surface. Highlights:

1. **`skills/session-post-processor/run.py`** — 969 LOC single file, Python 3.10+ stdlib only (`json`, `re`, `os`, `pathlib`, `datetime`, `argparse`, `sys`, `unicodedata`). Implements the seven-step v0.5 spec end-to-end: locate → parse → redact → emit → halt-on-leak → INDEX → health card. CLI: `--project-root`, `--cwd` (worktree override), `--jsonl` (explicit source), `--inject-test-leak` (dogfood-only halt-gate probe).
2. **Dogfood run 2 of 3** — CLEAN verification on this session's own JSONL (49 683-byte archive, 93 records, 37 tool calls, 20 redactions across 13 patterns). Archive committed as `memory/project/sessions/2026-04-15_on-reprend-v0-6-0-project-genesis.md`.
3. **Halt-on-leak gate proven live** — `--inject-test-leak` appends a fake `github_pat_` after the redaction pass, triggers the gate, deletes the archive, emits RED card, non-zero exit. Gate is no longer theoretical.
4. **Slug rule corrected via live dogfood** — the 2026-04-15 research entry said `\`, `:`, and space map to `-`; empirical verification proved underscore also maps to `-`. First live-dogfood correction of an R8 entry. Captured in [`memory/journal/2026-04-15_slug-rule-live-dogfood-correction.md`](../../../../memory/journal/2026-04-15_slug-rule-live-dogfood-correction.md) as the project's first journal entry.
5. **Four granular commits** in the feat branch (run.py → sessions dogfood → plugin.json bump → CHANGELOG), fourth consecutive session with the discipline.
6. **PR #10** opened via `GH_TOKEN` env override, squash-merged at `68f832a`
7. **Tag `v0.6.0`** created on `68f832a` and pushed

## Current state at session handoff

- **Repo**: `C:\Dev\Claude_cowork\project-genesis\`
- **GitHub**: `myconciergerie-prog/project-genesis` (private)
- **main branch**: at `68f832a` (feat squash), then updated by the chore PR that ships this file
- **Latest tag**: `v0.6.0` on `68f832a`
- **Self-rating**: v0.6.0 = **8.6/10**. Target for v0.7.0 = **8.0/10 floor**. Target for v1.0.0 average = **8.5/10**
- **Running average**: (7.6 + 8.2 + 8.8 + 8.4 + 8.6) / 5 = **8.32/10** — on track for v1 target, but the last two milestones need to average 8.77 to reach 8.5
- **First journal entry**: the slug-rule correction moment (state: seed), may or may not be developed further depending on future dogfood surprises
- **Working tree**: expected clean at session open

## Anti-Frankenstein gate — still holding

Five skill artefacts shipped so far (4 skills + the run.py executable). **Two** remaining stubs:

- `pepite-flagging/` (independent — red-light discovery flagging with cross-project routing per `specs/v1_pepite_discovery_flagging.md`)
- `genesis-protocol/` (orchestrator — should land last, once every phase is implemented)

Plus the **v0.6 backlog**:

- Research entry refresh for the slug underscore rule (5-minute doc fix)
- Multi-slug collision YELLOW warning in `run.py`
- Test vector harness for `redaction-patterns.md`
- Allow-list for `generic_long_base64` false positives on known-safe path fragments
- Run 3 of the three-run dogfood gate (pending a future Genesis session or the first Aurum session after the freeze lifts)

## The next concrete action — v0.7.0: pick one of two

### Option A — `pepite-flagging` skill

The last **independent skill stub**. Canonical spec is `.claude/docs/superpowers/specs/v1_pepite_discovery_flagging.md`. Red-light criteria for flagging cross-project discoveries during research, with explicit consent before propagation to sibling projects.

**Why now**: it is the last independent skill. Once shipped, only the orchestrator (`genesis-protocol`) remains and v1 ships. Momentum argument: v0.6 landed clean and the running average is trending up; the cheaper target is easier to lock in before fatigue.

**Complexity**: medium. Heuristic classification is softer than the session-post-processor's deterministic pipeline, so the rating ceiling is lower. Target 8.0/10 floor, realistic ceiling 8.3/10.

### Option B — Maintenance version (v0.6 backlog clean-up)

Clear the four concrete backlog items from v0.6: research entry refresh, multi-slug warning, test vector harness, allow-list. No new skill — just debt paydown.

**Why now**: the backlog has been accumulating for three versions. A dedicated maintenance pass resets the slate before the final two skills, which is easier to rate high because every change maps to a pre-existing gap.

**Complexity**: low-to-medium. Each item is small in isolation; the batch is the point. Target 8.0/10 floor, realistic ceiling 8.5/10 because maintenance work on a frozen spec is the cleanest possible rating surface.

### Selection rubric for the next session

**Pick whichever pain is most concrete at the time**. As of the v0.6.0 handoff:

- `pepite-flagging`'s pain is the cross-project research-sharing rule in Layer 0 still has no implementation — research entries stay project-local by default. This is a **latent** pain, not an acute one. It is also the "momentum" target: v1 gets closer with every skill shipped.
- The maintenance backlog's pain is that every item is already known and has compounding cost the longer it waits. The research entry drift is the most embarrassing of the four — we have a code-verified fact contradicting a confidence-high doc. This is a **moderate** pain, felt every time the research entry is re-read.

**Suggested pick**: `pepite-flagging` because momentum toward v1 matters more than backlog tidiness at this stage, and the backlog items can ride along with v0.8 or v0.9. But a maintenance v0.7 is defensible if the user wants to reduce the known-gaps list before the final skill.

If the user wants both, they are bundleable: ship `pepite-flagging` as the headline feature with the 5-minute research-entry refresh as a side-fix. Do NOT bundle the test vector harness or the allow-list — those are scope creep on a skill implementation.

### Subtasks for the v0.7.0 session

1. **Sync main**: `git fetch origin && git pull --ff-only origin main`
2. **Pick the option** — apply the rubric above or justify a different choice in the commit message
3. **Create feat worktree**: `feat/2026-04-XX_<milestone>` under `.claude/worktrees/`
4. **For Option A specifically** (`pepite-flagging` skill):
   - Read `.claude/docs/superpowers/specs/v1_pepite_discovery_flagging.md` in full before writing any skill file
   - Structure follows the journal-system convention (speech-native, minimal consent surface, 1:1 mirror of the spec)
   - Expect 5-6 files: `SKILL.md`, `trigger-criteria.md` (the red-light rules), `pepite-format.md` (the frontmatter schema for `memory/pepites/*.md`), `cross-project-routing.md` (how to propagate to sibling project memory dirs), `install-manifest.yaml`, `verification.md`
   - Consider whether a `run.py` is needed. Probably not — heuristic classification is softer than the post-processor's deterministic pipeline, and v0.5's "spec first, code later" worked. Only add `run.py` if a concrete automation pain emerges during implementation
5. **For Option B specifically** (maintenance pass):
   - Refresh `research/stack/claude-code-session-jsonl-format_2026-04-15.md` to add underscore to the slug rule list (move the old one to `archive/` and write a 2026-04-XX successor, preserving the `supersedes:` chain)
   - Add the multi-slug YELLOW warning to `run.py` (check if multiple slug dirs match the cwd; if so, pick the most-recent-mtime across all of them and surface a YELLOW in the health card)
   - Add `tests/redaction_vectors.py` — a tiny harness that imports the `REDACTION_PATTERNS` list from `run.py` and asserts each `match` vector redacts and each `non-match` vector survives
   - Add the allow-list for `generic_long_base64` — a short list of known-safe path prefixes (e.g. `claude/docs/superpowers/`, `anthropic`, `github.com/`) that survive the pattern. Add test vectors for the allow-list
6. **Update `CHANGELOG.md`** with a v0.7.0 entry + 5-axis self-rating, floor **8.0/10**. Below 8.0 requires explicit justification in the notes section
7. **Bump `plugin.json`** to `0.7.0`
8. **Granular commits inside the feat branch** — one per file for Option A, one per backlog item for Option B
9. **Push, PR, squash merge, tag** — same flow as v0.4 through v0.6
10. **Memory entry**: `memory/project/session_v0_7_0_<milestone>.md`
11. **Resume prompt** for v0.8.0

### Reinforced disciplines (cumulative)

- **Granular commits inside the feat branch** — now fully default across four sessions, no longer a new discipline
- **R8 mid-session refresh** — if any R8 entry is expired or confidence `medium` at session open, refresh before depending on it. First commit in the feat branch is the research refresh (not the first skill file)
- **Live-dogfood R8 correction** — if the code proves a research entry wrong during execution, fix the code immediately, log the divergence in CHANGELOG under `### Fixed`, and add a research-entry-refresh task to the next session's resume prompt. The journal entry on the slug-rule correction captures the broader principle
- **Manual-only before hook wiring** — Run 3 of the three-run post-processor dogfood is pending. Hook wiring still deferred
- **Halt-on-leak as single privileged operation** — `run.py` is the only Genesis artefact that deletes files. Keep that privilege concentrated

### Acceptance criteria for v0.7.0

Common to both options:

- Every new file has an SPDX header
- `CHANGELOG.md` updated with v0.7.0 entry and self-rating ≥ 8.0/10 (or explicit justification)
- `plugin.json` version = `0.7.0`
- PR cleanly squash-merged to origin main
- Git tag `v0.7.0` on origin
- Worktree retained per R2.5
- Granular commits inside the feat branch

Option-specific:

- **Option A** (`pepite-flagging`): trigger criteria 1:1 with `v1_pepite_discovery_flagging.md`, every new file SPDX-headered, install-manifest creates `memory/pepites/` + `INDEX.md` idempotently. Cross-project routing documented but NOT automated in v0.7 (automation is a v2 target)
- **Option B** (maintenance): research entry refreshed with `supersedes:` chain intact, multi-slug warning fires under a synthetic multi-slug test, `tests/redaction_vectors.py` passes, allow-list vectors cover at least `.claude/docs/superpowers/` paths

### Things to NOT do in the v0.7.0 session

- **Do not implement both options in full** — Option A with a 5-minute research-entry side-fix is acceptable; the full backlog + pepite-flagging is anti-Frankenstein
- **Do not wire `SessionEnd` hooks** — Run 3 of the dogfood gate is pending
- **Do not rewrite `run.py`** — it is stable. Additive changes only (multi-slug warning, allow-list) if Option B is picked
- **Do not touch `phase-minus-one`, `phase-5-5-auth-preflight`, `journal-system`, or the `session-post-processor` spec files** — all stable
- **Do not drift `pepite-flagging` from its canonical spec** — 1:1 mirror discipline applies, same as journal-system
- **Do not skip the self-rating block** — R10.3 gate
- **If the user says `frankenstein`**, back out of the last proposal

## Open questions worth flagging before v0.7.0 starts

1. **Option A or B?** — rubric suggests A for momentum; B is defensible for debt paydown. Ask at the start
2. **Bundle the 5-minute research-entry refresh with Option A?** — recommended yes, it is directly on the critical path
3. **Keep the first journal entry as `state: seed`?** — yes, unless the user explicitly wants to resume it with `"reprends la pensée sur la correction slug-rule"`. Seed entries can rest indefinitely

These are not blockers — the next session decides on the fly.

## Exact phrase for the next session

Open Claude Code in `C:\Dev\Claude_cowork\project-genesis\` and say:

```
On reprend v0.7.0 Project Genesis. Lis le resume prompt le plus récent
dans .claude/docs/superpowers/resume/, puis choisis Option A
(pepite-flagging skill) ou Option B (maintenance pass backlog v0.6).
Suggested: Option A + refresh de l'entrée R8 underscore en side-fix.
Target v0.7.0 à 8.0/10 floor. Commits granulaires dans le feat branch.
```

Claude Code then runs R1.1, creates the worktree, reads this file + the previous session memory, confirms or overrides the option, and implements.

## References the next session will consume

- `memory/project/session_v0_6_0_runpy.md` — prior session memory with the run.py implementation, dogfood runs, slug rule correction, and forward map
- `memory/journal/2026-04-15_slug-rule-live-dogfood-correction.md` — the project's first journal entry, captures the live-dogfood R8 correction moment as a seed
- `.claude/docs/superpowers/research/stack/claude-code-session-jsonl-format_2026-04-15.md` — the research entry that needs the underscore fix (move to `archive/` and write a 2026-04-XX successor)
- `.claude/docs/superpowers/specs/v1_pepite_discovery_flagging.md` — canonical spec for Option A
- `skills/journal-system/` — structural model for Option A (speech-native skill, minimal consent surface)
- `skills/session-post-processor/run.py` — the first runnable module, structural model if pepite-flagging needs automation
- `skills/phase-5-5-auth-preflight/` + `skills/phase-minus-one/` — structural models for larger skills

## Farewell notes to the next session's Claude

v0.6.0 shipped at 8.6/10 — the highest single-version rating since v0.4.0's 8.8, and the first time Genesis shipped runnable code. The running average is 8.32/10 with two milestones to go. The v1 target of 8.5/10 needs the last two to average 8.77 — achievable but tight. `pepite-flagging`'s soft ceiling (heuristic classification) is 8.3ish; `genesis-protocol`'s ceiling as an orchestrator could be higher if it lands cleanly on top of proven components.

The first journal entry opened in this session: the slug-rule live-dogfood correction. It is a seed, not a resolved thought. A future dogfood surprise may layer onto it, or not. Let it rest.

The halt-on-leak gate is no longer a promise — it fired under a live probe and deleted a 54 616-byte archive before any index update. The security floor is real.

The recursive loop continues. Good luck.
