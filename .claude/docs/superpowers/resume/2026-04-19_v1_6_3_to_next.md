---
name: Resume prompt — 2026-04-19 v1.6.3 → v1.6.4 or v1.7.0
description: Handoff from v1.6.3 surgical hotfix for F5 (universal SPDX-frontmatter break affecting every marketplace install since v1.3.0). Plugin now load-clean — all 8 skills surface under `project-genesis:` namespace. Streak ≥ 9.0 restarts at 1 after v1.6.2's honest break. Running avg 8.92. Tag chain at 20 versions. Next candidates : v1.6.4 F2 multi-turn scripted / v1.7.0 new skill / v2 design conversation on F3.
type: resume
previous_session: v1.6.3 F5 fix
next_action: v1.6.4 F2 OR v1.7.0 new skill OR v2 F3 design
---

# Resume — 2026-04-19 v1.6.3 → next

## What v1.6.3 shipped

**Tag v1.6.3** (PR #47 squash-merged as `3a35fe5`). **Third PATCH on v1.6.x line.** Surgical hotfix for v1.6.2 F5.

**Fix scope (1 feat commit `eefba00`)** :
- 8 SKILL.md : SPDX `<!-- -->` relocated from top to trailing position (frontmatter parser needs `---` at line 1)
- `skills/pepite-flagging/SKILL.md` : description wrapped single-quote (collateral YAML parse error emerged once F5 was fixed)
- `skills/genesis-protocol/rules/v1_rules.md § R10.2` : YAML-frontmatter-Markdown exception added (prevents regression)
- `plugin.json` : 1.6.2 → 1.6.3

**Verification** : `claude plugin validate` ✔ passed ; runtime dispatch confirmed 8/8 skills surface (was 6, missing `genesis-drop-zone` + `promptor`).

**Blast radius fixed** : every marketplace install from v1.3.0 through v1.6.2 was silently loading zero skills. v1.6.3 unblocks.

**Self-rating 9.30/10 honest** — streak ≥ 9.0 restarts at 1. Running avg 8.92 (+0.02).

## Current state

- **Repo** : main at `<sync-target after chore merge>`. Tag chain 20 versions (… v1.6.2 → **v1.6.3**).
- **Worktrees retained** : `feat_2026-04-19_v1_6_3_f5_fix/` + `chore_2026-04-19_v1_6_3_session/` + prior forensic (v1.6.2, v1.6.1, v1.6.0, v1.5.x).
- **User state** : `~/.claude/skills/` personal-scope shadow (6 Genesis skills) unchanged per v1.6.2 session — still there, still stale. **Suggested v1.6.4 or next session** : user can now `claude plugin update project-genesis@project-genesis-marketplace` + verify all 8 skills surface from marketplace-scope alone (then optionally `rm ~/.claude/skills/<6 genesis-skills>/` to clean the personal-scope shadow).
- **R8 cache** : stack entries expire 2026-04-20 (tomorrow). SOTA entries still in TTL.
- **Runtime JSON archive** at `C:/tmp/v1.6.2-runs/` (7 runs) + `C:/tmp/v1.6.3-validate.json` (1 run) — forensic reference.

## Next session candidates

### Candidate A — v1.6.4 F2 multi-turn runtime dogfood (scripted stream-json)

v1.6.2 F2 deferred because `-p` single-shot can't exercise Phase 0.1 → 0.5 flows. Now that v1.6.3 has fixed F5 and skills dispatch properly, a scripted `--input-format=stream-json` multi-turn subprocess harness would capture H2 arbitration + H3 Phase 0.5 consent + H4 EXIT_NO_KEY halt-card runtime evidence.

**Scope** ~3-4h. Script design + test harness + 5 fixture re-runs + evidence capture. Pain-driven axis target 9.4+.

### Candidate B — v1.7.0 new skill

If user has a clear skill candidate (could emerge from Alexandre use case, from Aurum's product work, from anywhere), this is the cleanest slot for a MINOR bump. Plugin load-path is now verified clean, so new skills will surface immediately after ship.

### Candidate C — v2 design conversation on F3 (skill vs freelance value-add)

v1.6.2 F3 raised the product-positioning question : when does invoking `/genesis-drop-zone` actually improve over letting Claude freelance from Layer 0 context ? The alexandre_windows run showed freelance Claude extracted multi-source data coherently WITHOUT the skill. Worth an explicit design conversation before the next MINOR ship.

### Candidate D — user-scope personal-skills cleanup (housekeeping)

User can optionally `rm -rf ~/.claude/skills/<6-genesis-skills>/` now that v1.6.3 ships the canonical versions via marketplace. Not urgent ; not a ship.

## Reco for next session

**Mon vote : Candidate A (v1.6.4 F2)**. Reasons :
- Closes the v1.6.2 F2 defer which was explicit in the triage
- Plugin load-path is now clean so runtime evidence will be meaningful
- H2 + H3 + H4 unable-to-test become test-able
- Unblocks v1.7.0+ work with clean runtime baseline

**Auto-critique** : v1.6.4 piles on v1.6.x line work. If user has appetite for a new skill (Candidate B) or wants to zoom out to product questions (Candidate C), Candidate A can wait. Candidate D is trivial and can be bundled with any candidate.

## Exact phrase for next session

```
mode autonome  Je reviens après v1.6.3 hotfix F5 SPDX-frontmatter fix (8 SKILL.md SPDX → trailing + pepite-flagging YAML single-quote + R10.2 exception + plugin.json 1.6.2→1.6.3, claude plugin validate ✔, runtime dispatch 8/8 skills surface, self-rating 9.30 HONEST, streak restart à 1, running avg 8.92). Lis le resume v1.6.3 → next et le session trace v1.6.3. Candidats : (A) v1.6.4 F2 multi-turn scripted stream-json ~3-4h / (B) v1.7.0 new skill si candidat / (C) v2 design conversation F3 skill-vs-freelance / (D) cleanup ~/.claude/skills/ personal-scope shadow. Propose reco + auto-critique. /using-superpowers
```

**PowerShell launcher** (one-line) :

```
cd C:/Dev/Claude_cowork/project-genesis; claude --dangerously-skip-permissions "mode autonome  Je reviens après v1.6.3 hotfix F5 SPDX-frontmatter fix (8 SKILL.md relocated + pepite-flagging YAML + R10.2 exception, validate ✔, dispatch 8/8 skills, 9.30 HONEST streak restart, running avg 8.92). Lis resume v1.6.3→next + session trace. Candidats A v1.6.4 F2 multi-turn / B v1.7.0 new skill / C v2 F3 design / D personal-scope cleanup. Propose reco + auto-critique. /using-superpowers"
```

<!-- SPDX-License-Identifier: MIT -->
