<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-15 v0.4.0 → v0.5.0 next skill
description: Handoff from the 2026-04-15 session that shipped v0.4.0 (journal-system skill end-to-end) to the next session whose job is to implement one of the two remaining independent skill stubs (session-post-processor / pepite-flagging) in its own worktree and tag v0.5.0
type: resume
previous_session: 2026-04-15 v0.4.0 Journal system skill
next_action: Implement one of {session-post-processor, pepite-flagging} end-to-end in a feat worktree, target v0.5.0 at 8.0/10 floor
---

# Resume prompt — 2026-04-15 v0.4.0 → v0.5.0

## Context — what the previous session did

The 2026-04-15 session (the v0.4.0 one) picked up the v0.3.0 → v0.4.0 handoff and delivered, **after recovering from a lost PowerShell-window attempt at v0.4.0**:

1. **Verification of the lost v0.4.0 attempt** — reflog, fsck, stash, worktrees, local / remote branches all confirmed zero trace of any prior v0.4.0 commit. The prior attempt was working-tree edits that were never committed; nothing recoverable. Decision: restart clean from the v0.3 → v0.4 handoff.
2. **Feat worktree** at `.claude/worktrees/feat_2026-04-15_journal-system-skill/` off `feat/2026-04-15_journal-system-skill`
3. **5 new files** under `skills/journal-system/` — `SKILL.md` + `entry-format.md` + `amplification-rules.md` + `install-manifest.yaml` + `verification.md`. Every file SPDX-headered. Full list in `memory/project/session_v0_4_0_skill_journal_system.md`.
4. **1:1 mirror of Layer 0** — `~/.claude/CLAUDE.md` "Journal System — Universal Thought Capture" is the canonical spec, declared in three of the five files with explicit "drift must be corrected toward Layer 0, not the other way" language.
5. **plugin.json bumped** to `0.4.0`
6. **CHANGELOG v0.4.0 entry** with 5-axis self-rating averaging **8.8/10** (target floor 8.0/10; v1.0.0 soft ceiling 8.5/10). Above-ceiling rating is expected for small clean skills — larger surface skills (session-post-processor / pepite-flagging / genesis-protocol) will land lower by construction.
7. **PR #6** opened via `GH_TOKEN="$GH_TOKEN" gh pr create`, squash-merged at commit `c22d174` via `GH_TOKEN="$GH_TOKEN" gh pr merge 6 --squash` (no `--delete-branch` per R2.3)
8. **Tag `v0.4.0`** created on `c22d174` and pushed to origin
9. **Chore worktree** at `.claude/worktrees/chore_2026-04-15_v0_4_0-session-memory-resume/` off `chore/2026-04-15_v0_4_0-session-memory-resume` for the session memory entry + this resume prompt

## Current state at session handoff

- **Repo**: `C:\Dev\Claude_cowork\project-genesis\`
- **GitHub**: `myconciergerie-prog/project-genesis` (private)
- **main branch**: at `c22d174` (feat squash), then to be updated by the chore PR that ships this file
- **Latest tag**: `v0.4.0` on `c22d174`
- **Self-rating**: v0.4.0 = **8.8/10**. Target for v0.5.0 = **8.0/10 floor** (not ceiling — natural climb is fine, but larger surface means larger rooms for speculative creep). Target for v1.0.0 average = **8.5/10**.
- **Working tree**: expected clean at session open. Minor lag behind origin possible on the main fast-forward.

## Anti-Frankenstein gate — still holding

Three skills shipped so far. **Two** independent skill stubs remain, plus the orchestrator:

- `session-post-processor/` (independent — JSONL redaction + markdown archive)
- `pepite-flagging/` (independent — red-light discovery flagging with cross-project routing)
- `genesis-protocol/` (orchestrator — should land last, once every phase is implemented)

Each ships in its own worktree, its own PR, its own version bump, its own self-rating block.

## Known cosmetic issue (still unresolved)

The GitHub repo description artefact from v1 bootstrap (Étape 4b copy-paste garble) is still present. v0.3.0 shipped the skill (`phase-5-5-auth-preflight/pat-walkthrough.md` with `Administration: Read and write` in the canonical scope list) that teaches how to fix it, and v0.4.0 did not rotate the PAT. Still optional, still a one-command fix once a new PAT with `Administration RW` is issued:

```bash
GH_TOKEN="$NEW_PAT_WITH_ADMIN_RW" gh api --method PATCH repos/myconciergerie-prog/project-genesis \
  -f description='Project Genesis — recursive project bootstrap template, shipped as a Claude Code plugin'
```

Still cosmetic. Still non-blocking. Worth rotating before v1.0.0 for hygiene.

## The next concrete action — v0.5.0: pick one of two independent skills

**Why pick from `session-post-processor` / `pepite-flagging`**:

- Both are independent — no cross-skill state needed
- Both have canonical specs or research already (`research/sota/claude-code-session-jsonl-format.md` for session-post-processor, `specs/v1_pepite_discovery_flagging.md` for pepite-flagging)
- Neither depends on `genesis-protocol` which should land last

### Selection rubric for the next session

Pick the skill whose **pain point is most concrete right now**. Ranked by that lens:

1. **`session-post-processor`** — the JSONL transcripts already pile up under `~/.claude/projects/<slug>/*.jsonl` on this machine. Every Genesis session so far has required a manual "write the session memory entry" step that could be partially pre-generated from the transcript. Pain point is immediate: this very chore PR re-ran the same by-hand pattern that the skill would semi-automate. Medium complexity — redaction rules, secret-pattern matcher, markdown emitter, idempotent write to `memory/project/`. The `research/stack/claude-code-session-jsonl-format.md` entry's TTL expires 2026-04-15 (today) — the next session should re-verify the on-disk JSONL layout before depending on it. **Strong candidate.**

2. **`pepite-flagging`** — the spec `v1_pepite_discovery_flagging.md` defines the red-light criteria. The skill runs heuristics during research to flag discoveries with cross-project potential. Pain point: the cross-project research sharing rule in Layer 0 has no implementation, so every research entry in `research/sota/` stays project-local by default. Medium complexity; higher false-positive risk than session-post-processor because the red-light criteria are softer.

**Suggested pick**: `session-post-processor` — the pain is visible in this very session (writing session memory by hand from context), and the skill's scope is cleaner (deterministic transcript → markdown pipeline, not heuristic classification).

### Subtasks for the v0.5.0 session

1. **Sync main**: `git fetch origin && git pull --ff-only origin main`
2. **Pick a skill** — apply the rubric above, or justify a different choice in the commit message
3. **Create feat worktree**: `feat/2026-04-XX_<skill-name>-skill` under `.claude/worktrees/`
4. **For session-post-processor specifically**: verify `~/.claude/projects/C--Dev-Claude-cowork-project-genesis/*.jsonl` layout on disk. The `research/stack/claude-code-session-jsonl-format_2026-04-14.md` entry expires today — refresh it if the format has drifted, otherwise extend the TTL after confirmation.
5. **Read the canonical source** — `research/sota/` + `research/stack/` for session-post-processor, `specs/v1_pepite_discovery_flagging.md` for pepite-flagging
6. **Implement `skills/<skill-name>/`** with a self-contained set of files following the `journal-system` / `phase-minus-one` / `phase-5-5-auth-preflight` structural convention (one `SKILL.md` entry point + supporting runner / template / manifest / verification files, only as many as the skill actually needs — journal-system proved that 5 is enough for a small skill)
7. **Update `CHANGELOG.md`** with a v0.5.0 entry + 5-axis self-rating, floor **8.0/10** — if the skill is larger (session-post-processor likely will be) and the rating lands lower, justify in the notes section; 7.8 is still acceptable if the surface is genuinely larger
8. **Bump `plugin.json`** to `0.5.0` in the same PR
9. **Commit** `feat(<skill-name>): implement <skill purpose> end-to-end` — and **commit after each file is written**, not only at the end, to protect against session-host loss (new discipline from the v0.4.0 PowerShell incident)
10. **Push** the branch
11. **Open PR** via `GH_TOKEN="$GH_TOKEN" gh pr create`
12. **Squash merge** via `GH_TOKEN="$GH_TOKEN" gh pr merge <n> --squash` (no `--delete-branch`)
13. **Tag** `v0.5.0` on the squashed merge commit
14. **Memory entry**: write `memory/project/session_v0_5_0_skill_<skill-name>.md`
15. **Resume prompt** for the v0.6.0 session

### Reinforced discipline from the v0.4.0 incident

**Commit early, commit often**. The v0.4.0 PowerShell-window loss was unrecoverable because the prior attempt's work was in the working tree, never committed. Future sessions working on a skill of non-trivial size should `git commit` after each file is written, not only at the end of the skill. Squashing at merge time smooths the history, so there is no downside to granular commits inside a feat branch.

This is a reinforcement of existing R2.x worktree discipline, not a new rule. It is added to the v0.5.0 session's instruction list as a concrete reminder.

### Acceptance criteria for v0.5.0

- `skills/<chosen-skill>/` contains every component required by its spec
- Every new file has an SPDX header
- `CHANGELOG.md` updated with v0.5.0 entry and self-rating **≥ 8.0/10** (or explicit justification if lower)
- `plugin.json` version = `0.5.0`
- PR cleanly squash-merged to origin main
- Git tag `v0.5.0` on origin
- Worktree retained per R2.5
- Granular commits inside the feat branch (new discipline from v0.4.0 incident)

### Things to NOT do in the v0.5.0 session

- **Do not implement more than one skill**
- **Do not wire `SessionEnd` hooks** — still deferred (even if session-post-processor would be the natural consumer; wiring hooks is a separate v0.6+ concern)
- **Do not rewrite `phase-minus-one`, `phase-5-5-auth-preflight`, or `journal-system`** — all three are stable
- **Do not add features outside the spec** — fidelity first
- **Do not skip the self-rating block** — R10.3 gate
- **Do not drift the journal-system skill away from Layer 0** — the 1:1 mirror discipline is non-negotiable; if Layer 0 changes, update the skill, never the reverse
- **If the user says `frankenstein`** — back out of the last proposal

## Open questions worth flagging before v0.5.0 starts

1. **Which skill to pick** — the rubric suggests `session-post-processor` because the pain is visible in this very session, but `pepite-flagging` is also defensible. The next session asks at the start.
2. **JSONL format TTL** — the `research/stack/claude-code-session-jsonl-format_2026-04-14.md` entry expires 2026-04-15. Refresh or extend before depending on it.
3. **Secret redaction patterns for session-post-processor** — the skill must redact GH_TOKEN, PAT, SSH private keys, `.env.local` paste-backs, etc. The redaction rule set is non-trivial and should be its own file (`redaction-patterns.md`) with explicit regex + rationale per pattern, not a scattered list.
4. **Archive location** — where do processed markdown archives live? Options: `memory/project/sessions/<date>.md` (versioned), `~/.claude/projects/<slug>/archives/<date>.md` (local only), or both. The skill's spec should pick one explicitly at design time.

These are not blockers — the next session decides on the fly, but decisions go into the commit message.

## Exact phrase for the next session

Open Claude Code in `C:\Dev\Claude_cowork\project-genesis\` and say:

```
On reprend v0.5.0 Project Genesis. Lis le resume prompt le plus récent
dans .claude/docs/superpowers/resume/, puis implémente le prochain skill
dans un worktree feat/. Suggested: session-post-processor (pain concret
dans la session courante, spec JSONL déjà en cache). Target v0.5.0 à
8.0/10 floor, commits granulaires dans le feat branch.
```

Claude Code then runs R1.1 open ritual, creates the worktree, reads this file, picks or confirms the skill, and implements.

## References the next session will consume

- `memory/project/session_v0_4_0_skill_journal_system.md` — prior session context (incident + decisions)
- `.claude/docs/superpowers/research/stack/claude-code-session-jsonl-format_2026-04-14.md` — JSONL schema (TTL expires today; refresh before use)
- `.claude/docs/superpowers/research/sota/claude-code-session-jsonl-format.md` — if present, or re-research
- `.claude/docs/superpowers/specs/v1_pepite_discovery_flagging.md` — red-light criteria (for pepite-flagging branch)
- `skills/phase-minus-one/` + `skills/phase-5-5-auth-preflight/` + `skills/journal-system/` — structural models, varying complexity
- `~/.claude/CLAUDE.md` Layer 0 — cross-project research sharing rule (relevant to pepite-flagging)

## Farewell notes to the next session's Claude

v0.4.0 shipped at 8.8/10 after recovering from a lost-PowerShell-window v0.4.0 attempt. The restart-clean decision was the right call — the unrecoverable working-tree loss means granular commits are the new default inside feat branches. Apply that discipline in v0.5.0.

The self-rating climb (7.6 → 8.2 → 8.8) is natural and will slow as the remaining skills have larger surfaces. Do not force a climb. If `session-post-processor` lands at 8.0 because it has more files and more edge cases, that is a healthy rating for a larger skill, not a failure. The v1 average target is 8.5/10 — we can absorb a few sub-8.5 ratings on larger skills without missing the ceiling.

The recursive loop continues. Good luck.
