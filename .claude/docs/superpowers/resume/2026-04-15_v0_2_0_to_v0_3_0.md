<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-15 v0.2.0 → v0.3.0 phase-5-5-auth-preflight
description: Handoff from the 2026-04-15 session that shipped v0.2.0 (phase-minus-one skill end-to-end) to the next session whose job is to implement phase-5-5-auth-preflight in its own worktree and tag v0.3.0
type: resume
previous_session: 2026-04-15 v0.2.0 Phase -1 skill
next_action: Implement skills/phase-5-5-auth-preflight/ end-to-end in a feat worktree, target v0.3.0 at 7.8/10
---

# Resume prompt — 2026-04-15 v0.2.0 → v0.3.0

## Context — what the previous session did

The 2026-04-15 session (this one) picked up Étape 5 from the v1 bootstrap handoff prompt and delivered:

1. **Feat worktree** at `.claude/worktrees/feat_2026-04-15_phase-minus-one-skill/` off `feat/2026-04-15_phase-minus-one-skill`
2. **12 new files** under `skills/phase-minus-one/` — `SKILL.md` + `detect.sh` + `install-manifest.yaml` + 9 template / runner markdown files. Every file SPDX-headered. Full list in `memory/project/session_v0_2_0_skill_phase_minus_one.md`.
3. **Spec fidelity** — all three stratified refinement layers of `specs/v1_phase_minus_one_first_user_bootstrap_flow.md` honoured, no silent divergence
4. **Live-tested detection** — `bash skills/phase-minus-one/detect.sh` emits 25 well-formed `KEY=VALUE` lines on this Windows 11 host
5. **plugin.json bumped** to `0.2.0`
6. **CHANGELOG v0.2.0 entry** appended above v0.1.0 with a 5-axis self-rating averaging **7.6/10** (target 7.0/10; v1.0.0 ceiling 8.5/10)
7. **PR #2** opened via `GH_TOKEN="$GH_TOKEN" gh pr create`, squash-merged at commit `15e4be9` via `GH_TOKEN="$GH_TOKEN" gh pr merge 2 --squash` (no `--delete-branch` per R2.3)
8. **Tag `v0.2.0`** created on `15e4be9` and pushed to origin
9. **Chore worktree** at `.claude/worktrees/chore_2026-04-15_session-memory-resume/` off `chore/2026-04-15_session-memory-resume` for the session memory entry + this resume prompt
10. **This file** — written in the chore worktree before being committed and merged via a small chore PR per R2.1

## Current state at session handoff

- **Repo**: `C:\Dev\Claude_cowork\project-genesis\`
- **GitHub**: `myconciergerie-prog/project-genesis` (private)
- **main branch**: at `15e4be9` (feat squash) then to be updated by the chore PR that ships this file. Local root will lag until the next session runs `git fetch && git pull --ff-only origin main`.
- **Latest tag**: `v0.2.0` on `15e4be9`
- **Self-rating**: v0.2.0 = **7.6/10**. Target for v0.3.0 = **7.8/10**. Target for v1.0.0 = **8.5/10**.
- **Working tree**: expected clean at session open (R1.1 step 3). Minor lag behind origin possible on the main fast-forward.

## Anti-Frankenstein gate — still holding

The v0.2.0 session shipped only one skill. Five skill stubs remain in `skills/`:

- `genesis-protocol/` (orchestrator — likely last, once every phase is implemented)
- `phase-5-5-auth-preflight/` (**the v0.3.0 target**)
- `journal-system/` (independent; reuses Layer 0 journal spec)
- `session-post-processor/` (independent; JSONL redaction + markdown archive)
- `pepite-flagging/` (independent; reuses the pépite spec)

Each is its own worktree, its own PR, its own version bump, its own self-rating block.

## Known cosmetic issue (carried over from v1 bootstrap)

The GitHub repo description still contains the Étape 4b copy-paste artefact. Fix options (both non-blocking, both documented in the previous resume prompt):

1. **Manual via web UI**: open `https://github.com/myconciergerie-prog/project-genesis` → About → pencil → replace with `Project Genesis — recursive project bootstrap template, shipped as a Claude Code plugin`
2. **PAT rotation** with `Administration: Read and write` scope, then PATCH via API

Still cosmetic. Still non-blocking.

## The next concrete action — v0.3.0: implement `phase-5-5-auth-preflight`

**Why `phase-5-5-auth-preflight` is the right choice**:

- The spec `specs/v1_phase_5_5_auth_preflight_learnings.md` exists, populated from the live dogfooding of Étape 4a-c in v1 bootstrap (SSH keygen + fine-grained PAT creation + empty repo + three-probe pre-flight)
- It closes the loop from "Phase -1 set up the machine" to "Phase 5.5 is ready to push a first repo"
- It reuses Phase -1's `memory/reference/automation-stack.md` state — the integration surface is small
- Every subtask is already documented in the learnings spec (the session that shipped v1 bootstrap wrote them as they were encountered)

### Subtasks for the v0.3.0 session

1. **Sync main**: `git fetch origin && git pull --ff-only origin main`
2. **Create feat worktree**: `feat/2026-04-XX_phase-5-5-auth-preflight-skill` under `.claude/worktrees/`
3. **Read the spec** — `specs/v1_phase_5_5_auth_preflight_learnings.md` in full
4. **Implement `skills/phase-5-5-auth-preflight/`** with:
   - `SKILL.md` — skill entry point + flow overview
   - `ssh-keygen.md` — per-project ed25519 template + `~/.ssh/config` alias insertion pattern
   - `pat-walkthrough.md` — fine-grained PAT creation checklist with the full scope list (Contents RW, Metadata R, Pull Requests RW, Workflows RW, Administration RW — the missing Administration scope was the Étape 4b blocker)
   - `empty-repo-create.md` — web-UI paste-back pattern (fine-grained PATs cannot create user repos via API — hard lesson from Aurum v0_init)
   - `three-probe-test.md` — the canonical three-probe pre-flight (`ssh -T git@github.com-<project>`, `GH_TOKEN=... gh api user`, `GH_TOKEN=... gh api repos/<owner>/<repo>`)
   - `playwright-automation.md` — conditional branch: if Playwright MCP is present from Phase -1, use it to drive the PAT creation form; else fall back to paste-back
   - `consent-card.md` — opt-in on which project is getting the auth pre-flight, what scopes, what PAT expiration, etc.
   - `verification.md` — final health card
5. **Update `CHANGELOG.md`** with a v0.3.0 entry + 5-axis self-rating targeting **7.8/10**
6. **Bump `plugin.json`** to `0.3.0` in the same PR
7. **Commit** `feat(phase-5-5-auth-preflight): implement Auth Pre-flight skill end-to-end`
8. **Push** the branch
9. **Open PR** via `GH_TOKEN="$GH_TOKEN" gh pr create`
10. **Squash merge** via `GH_TOKEN="$GH_TOKEN" gh pr merge <n> --squash` (no `--delete-branch`)
11. **Tag** `v0.3.0` on the squashed merge commit
12. **Memory entry**: write `memory/project/session_v0_3_0_skill_phase_5_5_auth_preflight.md`
13. **Resume prompt + goodbye card** for the v0.4.0 session

### Acceptance criteria for v0.3.0

- `skills/phase-5-5-auth-preflight/` contains every component above
- Every new file has an SPDX header
- `CHANGELOG.md` updated with v0.3.0 entry and self-rating **≥ 7.8/10**
- `plugin.json` version = `0.3.0`
- PR cleanly squash-merged to origin main
- Git tag `v0.3.0` on origin
- Worktree retained per R2.5

### Things to NOT do in the v0.3.0 session (anti-Frankenstein reminders)

- **Do not implement more than one skill** — `phase-5-5-auth-preflight` only
- **Do not wire `SessionEnd` hooks** — still deferred
- **Do not add features outside the spec** — fidelity to `specs/v1_phase_5_5_auth_preflight_learnings.md`
- **Do not skip the self-rating block** — R10.3 gate
- **Do not rewrite `phase-minus-one`** unless a concrete bug is found — it shipped at 7.6/10 and is stable
- **If the user says `frankenstein`** — back out of the last proposal

## Open questions worth flagging before v0.3.0 starts

1. **Playwright MCP automation of the GitHub PAT form** — is this worth the implementation complexity now, or defer to v0.4.0? The Aurum v0_init session lost time on PAT scope iterations; Playwright could eliminate that, but only if the form selectors are stable enough to automate reliably. **Suggested**: ship a paste-back-only v0.3.0, log the automation as a v0.4.0 candidate pépite.
2. **Where does the SSH key generation happen** — is it the same skill, or does `phase-5-5-auth-preflight` delegate to a separate `skills/ssh-identity/` skill? **Suggested**: same skill for v0.3.0, because the SSH key is per-project and its lifecycle is tied to the project's first push, so the coupling is justified.
3. **Three-probe test location** — standalone template file, or inlined in the skill's main runner? **Suggested**: standalone, because Phase 5.5 reruns it on every session open (per R1.1 auth health check) and a separate file makes it cheap to call from other skills too.

These are not blockers — the next session can decide on the fly.

## Exact phrase for the next session

Open Claude Code in `C:\Dev\Claude_cowork\project-genesis\` and say:

```
On reprend v0.3.0 Project Genesis. Lis le resume prompt le plus récent
dans .claude/docs/superpowers/resume/, puis implémente le skill
phase-5-5-auth-preflight end-to-end dans un worktree feat/. Target
v0.3.0 à 7.8/10.
```

Claude Code then runs R1.1 open ritual, creates the worktree, reads this file, reads `specs/v1_phase_5_5_auth_preflight_learnings.md` in full, and implements.

## References the next session will consume

- `specs/v1_phase_5_5_auth_preflight_learnings.md` — the canonical spec
- `skills/phase-minus-one/install-manifest.yaml` — for stack state expectations
- `skills/phase-minus-one/verification.md` — for the `memory/reference/automation-stack.md` schema
- `memory/reference/ssh_genesis_identity.md` — existing SSH pattern for Genesis itself (model for downstream projects)
- `memory/reference/github_genesis_account.md` — existing GitHub binding pattern
- `~/.claude/CLAUDE.md` Layer 0 — per-project SSH identity + GH_TOKEN env override + fine-grained PAT scope checklist
- `memory/project/session_v0_2_0_skill_phase_minus_one.md` — prior context (from this session)

## Farewell notes to the next session's Claude

v0.2.0 shipped at 7.6/10, a half-point above target. The phase-minus-one skill is faithful to the spec, live-tested for the detect script, and anti-Frankenstein on every axis. Do not try to "polish" it in the v0.3.0 session — that's feature creep against a stable surface. Ship the next skill cleanly and let the overall rating climb naturally as the stack grows.

The recursive loop continues. Good luck.
