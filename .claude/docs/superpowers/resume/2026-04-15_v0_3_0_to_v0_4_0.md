<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-15 v0.3.0 → v0.4.0 next skill
description: Handoff from the 2026-04-15 session that shipped v0.3.0 (phase-5-5-auth-preflight skill end-to-end) to the next session whose job is to implement one of the three independent skill stubs (journal-system / session-post-processor / pepite-flagging) in its own worktree and tag v0.4.0
type: resume
previous_session: 2026-04-15 v0.3.0 Phase 5.5 Auth Pre-flight skill
next_action: Implement one of {journal-system, session-post-processor, pepite-flagging} end-to-end in a feat worktree, target v0.4.0 at 8.0/10
---

# Resume prompt — 2026-04-15 v0.3.0 → v0.4.0

## Context — what the previous session did

The 2026-04-15 session (the v0.3.0 one) picked up the v0.2.0 → v0.3.0 handoff and delivered:

1. **Feat worktree** at `.claude/worktrees/feat_2026-04-15_phase-5-5-auth-preflight-skill/` off `feat/2026-04-15_phase-5-5-auth-preflight-skill`
2. **8 new files** under `skills/phase-5-5-auth-preflight/` — `SKILL.md` + 7 step / runner markdown files. Every file SPDX-headered. Full list in `memory/project/session_v0_3_0_skill_phase_5_5_auth_preflight.md`.
3. **Spec fidelity** — all five learnings of `specs/v1_phase_5_5_auth_preflight_learnings.md` honoured with zero silent divergence
4. **plugin.json bumped** to `0.3.0`
5. **CHANGELOG v0.3.0 entry** with 5-axis self-rating averaging **8.2/10** (target 7.8/10; v1.0.0 ceiling 8.5/10)
6. **PR #4** opened via `GH_TOKEN="$GH_TOKEN" gh pr create`, squash-merged at commit `1f964e0` via `GH_TOKEN="$GH_TOKEN" gh pr merge 4 --squash` (no `--delete-branch` per R2.3)
7. **Tag `v0.3.0`** created on `1f964e0` and pushed to origin
8. **Chore worktree** at `.claude/worktrees/chore_2026-04-15_v0_3_0-session-memory-resume/` off `chore/2026-04-15_v0_3_0-session-memory-resume` for the session memory entry + this resume prompt

## Current state at session handoff

- **Repo**: `C:\Dev\Claude_cowork\project-genesis\`
- **GitHub**: `myconciergerie-prog/project-genesis` (private)
- **main branch**: at `1f964e0` (feat squash), then to be updated by the chore PR that ships this file
- **Latest tag**: `v0.3.0` on `1f964e0`
- **Self-rating**: v0.3.0 = **8.2/10**. Target for v0.4.0 = **8.0/10** (floor, not ceiling — natural climb is fine). Target for v1.0.0 = **8.5/10**
- **Working tree**: expected clean at session open. Minor lag behind origin possible on the main fast-forward.

## Anti-Frankenstein gate — still holding

Two skills shipped so far. Four independent skill stubs remain:

- `genesis-protocol/` (orchestrator — likely last)
- `journal-system/` (independent — reuses Layer 0 journal spec)
- `session-post-processor/` (independent — JSONL redaction + markdown archive)
- `pepite-flagging/` (independent — reuses pépite spec)

Each ships in its own worktree, its own PR, its own version bump, its own self-rating block.

## Known cosmetic issue (still unresolved)

The GitHub repo description artefact from v1 bootstrap (Étape 4b copy-paste garble) is still present. The irony is that the v0.3.0 session just shipped the skill that teaches how to fix it:

1. Rotate the PAT with `Administration: Read and write` scope (now in the canonical scope list per `skills/phase-5-5-auth-preflight/pat-walkthrough.md`)
2. PATCH via `GH_TOKEN="$GH_TOKEN" gh api --method PATCH repos/myconciergerie-prog/project-genesis -f description='Project Genesis — recursive project bootstrap template, shipped as a Claude Code plugin'`

**Opportunity for v0.4.0**: dogfood the `phase-5-5-auth-preflight` skill on the Genesis repo itself and fix the description as a side effect. It's a one-line PAT rotation and would validate the skill in a real context.

Still cosmetic. Still non-blocking.

## The next concrete action — v0.4.0: pick one of three independent skills

**Why pick from `journal-system` / `session-post-processor` / `pepite-flagging`**:

- All three are independent — no cross-skill state needed
- All three reuse existing Layer 0 or spec content as the primary source
- Each would close a distinct value loop on its own
- None depend on `genesis-protocol` which should land last

### Selection rubric for the next session

Pick the skill whose **pain point is most concrete right now**. Ranked by that lens:

1. **`journal-system`** — Layer 0 already defines the trigger phrases and the stratified dialogue format. The skill is essentially a thin layer that watches for trigger phrases and writes journal files with the right frontmatter. Pain point: every project on this machine can capture thoughts once shipped. Low implementation complexity, high cross-project leverage. **Strong candidate.**

2. **`session-post-processor`** — The JSONL format is already researched (`research/sota/claude-code-session-jsonl-format.md`). The skill parses session transcripts, redacts secrets, and emits a readable Markdown archive per session. Pain point: every Genesis session currently requires a manual "write the session memory entry" step. Medium complexity.

3. **`pepite-flagging`** — The spec `v1_pepite_discovery_flagging.md` defines the red-light criteria. The skill runs heuristics during research to flag discoveries with cross-project potential. Pain point: cross-project research sharing is a Layer 0 rule but has no implementation. Medium complexity; higher false-positive risk than the others.

**Suggested pick**: `journal-system` — the smallest clean win, highest reuse of Layer 0 content, opens the door for every future project to capture thoughts in the same format.

### Subtasks for the v0.4.0 session

1. **Sync main**: `git fetch origin && git pull --ff-only origin main`
2. **Pick a skill** — apply the rubric above, or justify a different choice in the commit message
3. **Create feat worktree**: `feat/2026-04-XX_<skill-name>-skill` under `.claude/worktrees/`
4. **Read the canonical source** — Layer 0 section for `journal-system`, or the matching spec for the other two
5. **Implement `skills/<skill-name>/`** with a self-contained set of files following the `phase-minus-one` / `phase-5-5-auth-preflight` structural convention (one `SKILL.md` entry point + supporting runner / template files)
6. **Update `CHANGELOG.md`** with a v0.4.0 entry + 5-axis self-rating, floor **8.0/10** — if the skill is small and the rating lands lower, justify in the notes section
7. **Bump `plugin.json`** to `0.4.0` in the same PR
8. **Commit** `feat(<skill-name>): implement <skill purpose> end-to-end`
9. **Push** the branch
10. **Open PR** via `GH_TOKEN="$GH_TOKEN" gh pr create`
11. **Squash merge** via `GH_TOKEN="$GH_TOKEN" gh pr merge <n> --squash` (no `--delete-branch`)
12. **Tag** `v0.4.0` on the squashed merge commit
13. **Memory entry**: write `memory/project/session_v0_4_0_skill_<skill-name>.md`
14. **Resume prompt** for the v0.5.0 session

### Optional bonus for v0.4.0 — dogfood the new skill

If the v0.4.0 session's chosen skill is not `phase-5-5-auth-preflight` (and it shouldn't be — that already shipped), the session can optionally use the just-shipped `phase-5-5-auth-preflight/pat-walkthrough.md` to rotate Genesis's own PAT with Administration RW scope and fix the repo description. This would:

- Validate the skill in a real context before downstream users hit it
- Close the cosmetic follow-up that has been open since v1 bootstrap
- Take < 10 minutes

This is optional and should not balloon the v0.4.0 session. If the PAT rotation is attempted, it goes in its own tiny commit `chore(repo): rotate PAT and fix description via phase-5-5-auth-preflight dogfood`, separate from the main skill PR.

### Acceptance criteria for v0.4.0

- `skills/<chosen-skill>/` contains every component required by its spec
- Every new file has an SPDX header
- `CHANGELOG.md` updated with v0.4.0 entry and self-rating **≥ 8.0/10** (or explicit justification if lower)
- `plugin.json` version = `0.4.0`
- PR cleanly squash-merged to origin main
- Git tag `v0.4.0` on origin
- Worktree retained per R2.5

### Things to NOT do in the v0.4.0 session

- **Do not implement more than one skill**
- **Do not wire `SessionEnd` hooks** — still deferred
- **Do not rewrite `phase-minus-one` or `phase-5-5-auth-preflight`** — both are stable
- **Do not add features outside the spec** — fidelity first
- **Do not skip the self-rating block** — R10.3 gate
- **If the user says `frankenstein`** — back out of the last proposal

## Open questions worth flagging before v0.4.0 starts

1. **Which skill to pick** — the rubric suggests `journal-system` but the user may have a different preference. The next session asks at the start rather than assuming.
2. **Dogfood PAT rotation** — yes or no for the bonus description fix. Either answer is fine; if no, the cosmetic issue stays for v0.5.0.
3. **`three-probe-test.md` reuse** — should Phase 5's future `genesis-protocol` skill call it directly, or re-implement? The standalone extraction in v0.3.0 was explicitly so the reuse is available; future sessions should honour it.

These are not blockers — the next session decides on the fly.

## Exact phrase for the next session

Open Claude Code in `C:\Dev\Claude_cowork\project-genesis\` and say:

```
On reprend v0.4.0 Project Genesis. Lis le resume prompt le plus récent
dans .claude/docs/superpowers/resume/, puis implémente le prochain skill
dans un worktree feat/. Suggested: journal-system (plus petit, plus de
réutilisation Layer 0). Target v0.4.0 à 8.0/10.
```

Claude Code then runs R1.1 open ritual, creates the worktree, reads this file, picks or confirms the skill, and implements.

## References the next session will consume

- `memory/project/session_v0_3_0_skill_phase_5_5_auth_preflight.md` — prior session context
- `~/.claude/CLAUDE.md` Layer 0 — journal system trigger phrases and stratified format (for `journal-system`)
- `.claude/docs/superpowers/research/sota/claude-code-session-jsonl-format.md` — JSONL schema (for `session-post-processor`)
- `.claude/docs/superpowers/specs/v1_pepite_discovery_flagging.md` — red-light criteria (for `pepite-flagging`)
- `skills/phase-minus-one/` — first skill, structural model
- `skills/phase-5-5-auth-preflight/` — second skill, latest structural model

## Farewell notes to the next session's Claude

v0.3.0 shipped at 8.2/10 — the highest rating yet. The climb is natural, not forced. Keep the discipline: one skill per PR, one PR per worktree, one self-rating block per version, zero speculative features. Each independent skill adds value without dragging the others with it.

The anti-Frankenstein curve is still on the healthy side of the inflection point. Target 8.0 floor for v0.4.0 is a minimum, not a ceiling — a small clean skill can easily land higher just by being small and clean. Do not pad files to "feel complete" — sparse is better than speculative.

The recursive loop continues. Good luck.
