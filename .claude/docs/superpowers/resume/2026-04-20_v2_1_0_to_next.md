<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-20 v2.1.0 → next
description: Handoff from v2.1.0 small-scope MINOR (marketplace validate warning fix + v2.0.0 runtime evidence capture). Closes two v2.0.0 loose ends. 4/4 runtime observables PASS on first try, verbatim captured in new spec. Honest 8.92/10 — STREAK ≥ 9.0 BROKEN at 2 (v1.6.3 9.30 + v2.0.0 9.20 run ends here). Running avg 8.93 flat over 22 tagged versions. Tag chain at 22 versions (… v1.6.3 → v2.0.0 → v2.1.0). Next candidates : v2.2.0 multi-modal runtime evidence / v3.0 web mode kickoff (genesis-web sibling repo already at v0.6.0) / v2.0.2 minor cleanups / Aurum unlock review.
type: resume
previous_session: v2.1.0 cosmetic + runtime evidence
next_action: v2.2.0 multi-modal evidence OR v3.0 web mode kickoff OR Aurum unlock review
---

# Resume — 2026-04-20 v2.1.0 → next

## What v2.1.0 shipped

**Tag v2.1.0** (PR #51 squash-merged as `3911a22`). Small-scope MINOR bundling two v2.0.0 loose ends that the v2.0.0→next resume had flagged as deferred :

- **Piste A (cosmetic, ~15 min)** — `.claude-plugin/marketplace.json` gains `metadata.description` field. Closes the last outstanding `claude plugin validate` warning from v2.0.0's collateral fix `0f69522`. Validator output now clean : `✔ Validation passed` (zero warnings).
- **Piste B (runtime evidence, ~2-3 h)** — new spec `.claude/docs/superpowers/specs/v2_1_0_runtime_evidence_log.md` captures verbatim 4/4 observables on a real end-to-end `/project-genesis:genesis-drop-zone` invocation in `C:/tmp/genesis-v2-test/`. User-driven live test : plugin bumped v1.6.3 → v2.0.0 via `claude plugin update`, then fresh `claude` session in the fixture, slash trigger, typed-text idea, decline consent, `/exit`, full scrollback pasted back. All 4 observables (A Phase 0.0 sub-skill first / B exact auth confirmation line / C FR welcome box / D zero API key friction) PASS on first try. Bonus : v1.3.1 mirror + v1.3.2 consent + v1.3.3 FR decline bridge all render intact post-v2 refactor.
- **R8 hygiene** — 2 stack entries (`claude-code-plugin-structure_2026-04-19.md` + `claude-code-session-jsonl-format_2026-04-19.md`) moved to `archive/` after their 1-day TTL elapsed. In-skill templates remain live.
- **`.gitignore` extension** — `.claude/settings.local.json` + `.playwright-mcp/` ignored (local-only Claude Code state that was polluting `git status`).

**3 commits in feat tranche** (squash-merged) : fix(marketplace) + test(runtime) + chore(hygiene). Chore tranche follows as separate PR with CHANGELOG + session trace + resume + MEMORY + plugin.json 2.0.0 → 2.1.0 bump (caught as feat-tranche omission, documented honestly in self-rating).

**Self-rating 8.92/10 HONEST** — streak ≥ 9.0 BROKEN at 2 (v1.6.3 9.30 + v2.0.0 9.20 → v2.1.0 8.92). Running avg flat at 8.93 over 22 tagged versions.

## Current state

- **Repo** : main at `3911a22` (feat) + chore commit TBD on next push. Tag chain 22 versions (… v1.6.3 → v2.0.0 → **v2.1.0**).
- **Worktrees retained** : `feat_2026-04-20_v2_1_0_cosmetic_and_runtime_evidence/` + prior forensic.
- **User state** : local install bumped to v2.0.0 during v2.1.0 runtime test (`claude plugin update`). Will need another `claude plugin update project-genesis@project-genesis-marketplace` next session to pull v2.1.0 when testing / dogfooding.
- **R8 cache** : all sota entries TTL OK. Stack dir empty (both entries archived this session). Next expirations : sota entries 2026-04-21 (`open-source-license-for-dev-tooling`, `claude-code-plugin-distribution`, `spdx-headers`) ; sota `claude-in-ide-tools_2026-04-15` + `claude-ecosystem-cross-os_2026-04-15` expire 2026-04-22.
- **V3 vision** : captured in `memory/master.md § "What v3 vision is"` (2026-04-19, extended 2026-04-20 discussion in this session). **`genesis-web` sibling repo already exists** at `C:/Dev/Claude_cowork/genesis-web/`, v0.6.0 tagged (auth modal + welcome router + sign-out shipped). Physical split between `project-genesis` (MIT plugin) and `genesis-web` (proprietary SaaS platform) is real. **Consumption mechanism** between the two repos is the open architectural question (npm publish vs git submodule vs git subtree / direct vendoring) — deferred to v3.0 kickoff.

## Next session candidates

### Candidate A — v2.2.0 multi-modal runtime evidence (~2-3 h)

v2.1.0 closed the Phase 0.0 runtime evidence gap for the **typed-text** input modality. The attachment drag-and-drop modality (PDF, image, audio, URL, mixed media) is still documentary. v2.2.0 would extend the runtime evidence coverage : prepare a fixture with sample PDFs / images, walk user through `/genesis-drop-zone` with an actual drag-and-drop attachment, verify the 9-field mirror renders correctly with `attaches` populated and `langue_detectee` from attachment content. Would fully close the v2 runtime validation axis.

**Pros** : natural continuation of the v2.1.0 evidence axis ; closes the full drop-zone input modality coverage ; user already has the muscle memory for the test protocol.
**Cons** : scope limited to evidence, no new user-visible feature ; would tag as MINOR (v2.2.0) or PATCH (v2.1.1) depending on whether it surfaces any code drift that needs a fix.

### Candidate B — v3.0 web mode kickoff — consumption mechanism decision + master.md split formalization (~1 session fresh brainstorming)

Per the 2026-04-20 discussion captured in this session's memory, the `genesis-web` sibling repo already exists physically at v0.6.0. The v3.0 kickoff needs to : (a) formalize the split in `memory/master.md` (currently captures the v3 vision but not the real `C:/Dev/Claude_cowork/genesis-web/` sibling state), (b) decide the consumption mechanism for how `genesis-web` imports `project-genesis` skill code (npm publish vs git submodule vs direct vendoring), (c) optionally set up the first CI wire between the two repos. Would be the first cross-repo coupling work of the Genesis project.

**Pros** : moves toward the v3 vision strategically ; formalizes an architectural decision that's been deferred through several ships ; unblocks future `genesis-web` development from consuming plugin updates reliably.
**Cons** : requires fresh brainstorming session (no plan exists yet) ; cross-repo coordination adds complexity ; consumption mechanism decision is load-bearing and better made with design review than in-flight.

### Candidate C — Aurum frozen scope lock review + formal unlock (~30 min)

Per `memory/project/aurum_frozen_scope_lock.md`, the Aurum scope lock formally lifted at v1.0.0. v2.0.0 and v2.1.0 have both shipped beyond the original lock scope. A short next session could : (a) review the lock rule for closure, (b) archive the rule file as "lock history" forensic, (c) open the Aurum v1 kickoff as the immediately-next strategic axis.

**Pros** : honors the lock-scope agreement ; clears the formal rule out of active memory ; unblocks Aurum work which has been pending since 2026-04-14.
**Cons** : Aurum work is a different project context ; would shift focus away from Genesis as the active codebase.

### Candidate D — v2.0.2 cosmetic cleanups (~30 min)

Two small items surfaced during v2.1.0 but weren't in scope : (a) `plugin.json description` has mojibake (`â€"` instead of em-dash `—`) — a legacy encoding issue latent since v1.0.0 ; (b) MEMORY.md Project section is chronologically out-of-order (entries appended in ship-commit order, not tag-version order — v1.4.2 at the bottom follows v2.0.0 because v1.4.2 was back-inserted as a late pointer). Neither is user-visible harmful. Could be bundled into a PATCH ship as v2.1.1.

**Pros** : cleans up latent nits before they compound ; trivial effort.
**Cons** : no pain-driven value ; cosmetic-only ; bundling with A or B would be more efficient than a dedicated PATCH.

## Reco for next session

**Mon vote : Candidate B — v3.0 web mode kickoff (consumption mechanism decision + master.md split formalization)**. Reasons :

- The 2026-04-20 discussion in this session surfaced that `genesis-web` already exists physically and the consumption mechanism is the load-bearing architectural question blocking further v3 work.
- v2.1.0 cleanly closed the v2.0.0 tail (cosmetic warning + runtime evidence) — there's no pending v2 axis pulling scope back toward v2.x anymore.
- Candidate A is lower-value-for-effort (closes a narrow evidence sub-axis) compared to the strategic weight of v3 kickoff.
- Candidate C and D can wait ; they're cheap cleanups that can bundle into any later ship.

**Auto-critique** : If the user prefers evidence-first discipline (v2 architecture still needs multi-modal validation before v3 kickoff), Candidate A IS the right call — closing the full v2 runtime evidence surface before moving to v3 is a defensible anti-Frankenstein stance. And if the user has no strong v3 timeline pressure, Candidate C (Aurum unlock review) could naturally precede any of A/B/D since the Aurum axis has been frozen since 2026-04-14 and the lock formally lifted 6 sessions ago.

## Exact phrase for next session

```
mode autonome  Je reviens après v2.1.0 small-scope MINOR (marketplace validate warning fix + v2.0.0 runtime evidence capture). 3 commits feat tranche, PR #51 squash 3911a22, tag v2.1.0. 4/4 runtime observables PASS verbatim on fresh-project /genesis-drop-zone. 8.92 HONEST streak ≥ 9.0 BROKEN at 2 (v1.6.3 9.30 + v2.0.0 9.20 → v2.1.0 8.92), running avg 8.93 flat. Lis resume v2.1.0→next + session trace. Candidats : (A) v2.2.0 multi-modal runtime evidence attachments / (B) v3.0 web mode kickoff consumption mechanism + master.md split formalization genesis-web v0.6.0 existing / (C) Aurum unlock review / (D) v2.0.2 cosmetic cleanups plugin.json mojibake + MEMORY.md ordering. Propose reco + auto-critique. /using-superpowers
```

**PowerShell launcher** (one-line, copy-paste ready) :

```
cd C:/Dev/Claude_cowork/project-genesis; claude --dangerously-skip-permissions "mode autonome  Je reviens après v2.1.0 small-scope MINOR marketplace validate fix + v2.0.0 runtime evidence. PR #51 squash 3911a22 tag v2.1.0. 4/4 observables PASS verbatim. 8.92 HONEST streak BROKEN at 2, running avg 8.93 flat. Lis resume v2.1.0→next + session trace. Candidats A v2.2.0 multi-modal / B v3.0 web mode kickoff consumption mechanism genesis-web v0.6.0 existant / C Aurum unlock / D v2.0.2 cleanups. Propose reco + auto-critique. /using-superpowers"
```
