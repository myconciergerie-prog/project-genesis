<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-17 v1.2.3 → v1.2.4 or v2 Étape 0
description: Handoff from v1.2.3 F34 ship. The P1 queue from the v1.2.0 self-dogfood is now closed — F23/F27/F29/F30 in v1.2.1, F20/F21/F22 in v1.2.2, F34 in v1.2.3. The next natural boundary is either a v1.2.4 cycle on P2 doc work / rule-level extensions, or the v2 Étape 0 drop-zone pivot while the research cache is still fresh.
type: resume
previous_session: v1.2.3 gh active-account pre-flight (PR #?, tag v1.2.3)
next_action: Pick v1.2.4 scope (P2 doc work OR rule-level mirror of Step 6.0) OR pivot to v2 Étape 0 drop-zone
---

# Resume — 2026-04-17 v1.2.3 → v1.2.4 or v2 Étape 0

## What v1.2.3 shipped

**Tag v1.2.3** (PR #? merged). One P1 friction (F34) landed as a 3-commit bundle in one PR:

- **F34 — Step 6.0 itself** — `### Step 6.0 — gh active-account pre-flight` inserted at the opening of `skills/genesis-protocol/phase-6-commit-push.md` before Step 6.1 pre-commit review. Runs `gh api user --jq .login`, compares against a target owner resolved from the git remote URL (first-match) / `bootstrap_intent.md` (fallback) / Step 0 consent card (final fallback). On mismatch, attempts `gh auth switch -h github.com -u <owner>`; on switch-failure, halts with `gh auth login --web` remediation and full `gh auth status` output.
- **F34 — SKILL.md sync** — `## Mode dispatch` Category A "Structural stops" row now lists Step 6.0 alongside paradox guards, nested-repo halts, halt-on-leak, and failed probes. 1:1 mirror between the canonical catalogue and the runbook.
- **Chore** — version bump `1.2.2` → `1.2.3`, CHANGELOG, session trace, R8 archive of two expired stack entries (`claude-code-plugin-structure_2026-04-14.md`, `claude-code-session-jsonl-format_2026-04-15.md`).

Self-rating **9.18/10**. Running average **8.71/10** (up from 8.68). Three consecutive ≥ 9.0 versions (v1.2.1 9.26, v1.2.2 9.14, v1.2.3 9.18) — the surgical-commit-per-root-cause / bundle-in-one-PR discipline continues to hold.

Full narrative at `memory/project/session_v1_2_3_f34_gh_preflight.md`.

## Current state

- Repo: `myconciergerie-prog/project-genesis` on main, tag `v1.2.3`, commit `<merge-sha>`.
- Tags chain: `v1.0.0` → `v1.1.0` → `v1.2.0` → `v1.2.1` → `v1.2.2` → `v1.2.3`.
- git status: clean, all pushed.
- `gh auth`: still two accounts (`myconciergerie-prog` + `myconciergerieavelizy-cloud`); **Step 6.0 from v1.2.3 now automates the active-account check for Phase 6 bootstraps**, but the same discipline has NOT yet been mirrored to v0.2.0+ post-bootstrap PR sessions — **always verify active account with `gh api user --jq .login` before `gh pr create`** in any v0.2.0+ session until v1.3 lands the rule-level extension.
- P1 queue from the v1.2.0 friction log: **CLOSED**.

## v1.2.4 candidates (if staying in the v1.2.x refinement loop)

### Candidate A — rule-level mirror of Step 6.0 for v0.2.0+ PR sessions

Add an R10-adjacent rule in `skills/genesis-protocol/rules/v1_rules.md` that mandates a `gh api user --jq .login` vs git-remote-owner check before any `gh pr create` invocation. This extends the v1.2.3 bootstrap-only fix to every downstream session, closing the same pain in every Genesis-bootstrapped project. Scope: ~15–25 lines of rule prose + a short checklist. Est. 1 short session.

**Why this next**: F34 was live-reproduced during v1.2.1's own PR creation — which was NOT a bootstrap run, it was Genesis working on itself in v0.2.0+ mode. The v1.2.3 fix covers the bootstrap, but the original failure surface (Genesis repo's own PR work) is still uncovered by anything but a verbal reminder. This candidate closes that gap.

### Candidate B — F25/F31 config.txt canonical examples

Ship `templates/config-minimal.txt.example` (3-line free-form) and `templates/config-complete.txt.example` (all 8 canonical fields, YAML-ish), plus a pin in README Quick Start. P2 doc work. Scope: 2 template files + a few README lines. Est. ≤ 1 session.

**Why this next**: with the P1 queue closed, P2 doc work becomes the natural cleanup pass. The config.txt format ambiguity (F25 observed in atelier-playmobil bootstrap, F31 re-observed in the selfdogfood run) is the most-quoted P2 in the friction log.

### Candidate C — F26 full wire-up (passthrough fields in bootstrap_intent.md)

Complete what v1.2.2 half-fixed: edit `skills/genesis-protocol/phase-0-seed-loading.md` to explicitly write `## Non-canonical fields (passed through)` into `bootstrap_intent.md` at Phase 0.5 when non-canonical kvp args are observed. Scope: ~20 lines in the Phase 0.5 write-template section.

**Why not this**: F26 is borderline — the convention is documented, the actual write is implicit but would benefit from an explicit example. Prioritize below A and B.

## v2 Étape 0 — the alternative pivot

Same pivot as the v1.2.1 → v1.2.2 and v1.2.2 → v1.2.3 resume prompts. The research cache `v2_promptor_fusion_landscape_2026-04-17.md` expires **2026-04-24** (7 days from today — about 5–6 days of runway remain after a hypothetical v1.2.4 ship).

**Arguments for Path B now vs after v1.2.4**:
- **Now**: mental mode switch is cleaner with the P1 queue closed; no half-finished v1.x work to context-switch from; research cache is fresh.
- **After v1.2.4**: one more surgical ship extends the streak (potential 4ᵗʰ ≥ 9.0), and v1.2.4 candidate A (rule-level Step 6.0 mirror) genuinely closes a user-experience pain that the self-rating framing flagged as important.

**Suggested order if the user wants both**: v1.2.4 candidate A (rule-level mirror, ~1 session) → v2 Étape 0 (bigger scope, 2+ sessions). This takes the last concrete P1-adjacent pain off the table before opening the v2 canvas.

**Counter-suggestion if the streak is fatiguing**: go straight to v2 Étape 0. The score-as-user-experience framing from the v1.2.3 session open still applies — if the mental mode switch makes the user more productive on v2, that IS user experience. The streak is not an end in itself.

## Files to read at session open

1. `memory/MEMORY.md`
2. `memory/master.md`
3. `memory/project/session_v1_2_3_f34_gh_preflight.md`
4. `memory/project/selfdogfood_friction_log_v1_2_0_2026-04-17.md` (deferred-list context, no remaining P1)
5. This resume prompt
6. For Path B (v2 Étape 0): `specs/v2_vision_promptor_fusion.md` + `.claude/docs/superpowers/research/sota/v2_promptor_fusion_landscape_2026-04-17.md`

## R8 cache state

- Stack entries `claude-code-plugin-structure` + `claude-code-session-jsonl-format` archived by v1.2.3 (TTL expired 2026-04-17). Archive stanzas in INDEX.md note they were fresh-at-archival against v1.2.2 session data; refresh if drift suspected before any session-post-processor change or plugin structure edit.
- `v2_promptor_fusion_landscape_2026-04-17.md` fresh, expires **2026-04-24** — usable for Path B. Refresh window closes before Path B can be fully completed if Path B is picked on 2026-04-23 or later; consider either a fresh snapshot at session open for Path B, or acceptance that Path B's first session runs partly on near-expired research.
- `gh-cli-single-click-auth_2026-04-16.md` fresh, expires 2026-04-23 — relevant for any v1.2.4 rule-level gh auth work.
- No new research entries required for v1.2.4 candidate A (rule-level Step 6.0 mirror reuses the same gh CLI primitives already verified in v1.2.3).

## Scope lock

Aurum remains at `0b1de3d` per the v1.0.0 freeze-lift conditions (the freeze was lifted at v1.0.0 ship, so no active lock). Verify at session open by reading `memory/project/aurum_frozen_scope_lock.md`.

## Exact phrase for the next session

Open Claude Code in `C:/Dev/Claude_cowork/project-genesis/` and paste:

```
mode autonome  Je reviens après le ship v1.2.3 (F34 Step 6.0 gh
active-account pre-flight, self-rating 9.18/10, running avg 8.71,
trois ships consécutifs ≥ 9.0 — P1 queue fermée). Lis le resume
v1.2.3 → v1.2.4/v2-étape-0 et le session trace v1.2.3. Propose-moi
Path A (v1.2.4 candidat A rule-level mirror de Step 6.0 pour sessions
v0.2.0+) vs Path B (pivot v2 Étape 0 drop-zone) avec ta reco, puis
on attaque.
/using-superpowers
```
