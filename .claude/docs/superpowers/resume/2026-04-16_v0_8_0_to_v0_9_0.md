<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-16 v0.8.0 → v0.9.0 Path A polish toward v1.0.0
description: Handoff from the 2026-04-16 session that shipped v0.8.0 (genesis-protocol orchestrator at 9.0/10, running average 8.49/10) to the v0.9.0 polish session whose job is to pull the running average cleanly above 8.5 before the v1.0.0 tag, leveraging the cumulated memory + meta-memory context from v1 bootstrap through v0.8. Path A chosen explicitly over Path B direct-ship. Aurum freeze lifts only at v1.0.0.
type: resume
previous_session: 2026-04-16 v0.8.0 genesis-protocol orchestrator
next_action: Run the v0.9.0 polish pass — README update, phase-file trim, dry-run validation against a tmp folder, Layer 0 inheritance documentation, optionally one v0.5/v0.6 gap (dogfood run 3 is the strongest candidate). Target rating 8.5–9.0 to push running average cleanly above 8.5.
---

# Resume prompt — 2026-04-16 v0.8.0 → v0.9.0 (Path A polish toward v1.0.0)

## Context — what the previous session did

The 2026-04-16 session (v0.8.0) picked up the v0.7.0 → v0.8.0 handoff, confirmed **Option A (pure Markdown)** over Option B (Markdown + Python driver) and Option C (hybrid), and delivered the genesis-protocol orchestrator in a single feat branch:

1. **`skills/genesis-protocol/`** — eight files, ~1,400 lines total, 1:1 mirror of `memory/master.md`'s 7-phase table. Conductor that composes the five sibling skills (`phase-minus-one`, `phase-5-5-auth-preflight`, `journal-system`, `session-post-processor`, `pepite-flagging`) into a single `/genesis-protocol` invocation. No Python, no hooks, no shared state.
2. **Concentrated-privilege map** enumerated formally in SKILL.md — six data points: `phase-minus-one` (installers, 3-mode ladder), `phase-5-5-auth-preflight` (SSH/PAT/repos, paste-back default), `journal-system` (none), `session-post-processor` (archives, halt-on-leak), `pepite-flagging` (cross-project pointers, per-target consent), and **`genesis-protocol` itself** (writing an entire new project directory outside the Genesis repo, top-level consent card + per-phase confirmation).
3. **Third 1:1 spec mirror** shipped (after journal-system v0.4 and pepite-flagging v0.7). SKILL.md's 7-phase master table mirrors `memory/master.md` directly — drift is a merge-blocker.
4. **File folding justification** — the resume prompt suggested 5-7 files; the implementation landed at 8 with phases 2, 4, 7 folded into adjacent runbooks (Phase 2 into `phase-1-rules-memory.md`, Phase 4 into `phase-3-git-init.md`, Phase 7 into `phase-6-commit-push.md`). Each phase still has a clear home in SKILL.md's master table.
5. **11 granular commits** in the feat branch, squashed at `0d2616f`.
6. **Tag `v0.8.0`** created on `0d2616f` and pushed to origin.

**Six skills shipped. Zero stubs remaining.** The full v1 skill surface is complete.

## Running average and the v1 arithmetic

Self-rating v0.8.0 = **9.0/10**. Clears the session floor (8.5) by 0.5. Five-axis breakdown: pain-driven 9.0, prose cleanliness 8.8, best-at-date 8.8, self-contained 9.2, anti-Frankenstein 9.2.

Running average v0.2 → v0.8:

```
(7.6 + 8.2 + 8.8 + 8.4 + 8.6 + 8.8 + 9.0) / 7 = 8.49/10
```

This is **0.01 below** the v1 target of 8.5/10 — the smallest possible formal miss. Below the 9.1/10 threshold for direct v1.0.0 tag, so v0.8.0 shipped cleanly and the v1.0.0 path required a user decision.

## Path A chosen — why, and what it means for v0.9.0 scope

The user picked **Path A (v0.9.0 polish → v1.0.0)** over Path B (direct v1.0.0 from v0.8.0) with the framing *"en tenant compte de toutes les avancées dans la mémoire et dans la préparation de méta memory"*. This is a deliberate nudge that v0.9.0 should not be a mechanical number-chase — it should leverage the cumulated memory + meta-memory context from v1 bootstrap through v0.8 to land a cleaner v1.0.0.

**Implication for scope**: the polish should touch the parts of the plugin where the meta-memory architecture has most evolved since v1 bootstrap. Six areas surface naturally:

1. **1:1 spec mirror discipline** — now applied three times. v0.9.0 should document it as a named pattern in the plugin (short section in `memory/master.md` or a new `specs/patterns_spec_mirror.md`), not leave it implicit in each skill's prose
2. **Concentrated-privilege map** — now with six data points. The orchestrator's SKILL.md has the table; v0.9.0 could promote it to a plugin-level pattern doc or at least cross-reference it from `memory/master.md`
3. **Journal system as 6th memory type** — in use since v0.4, one real entry written so far (the slug rule live-dogfood correction at v0.6-v0.7 boundary). v0.9.0 could add a second intentional journal entry capturing the Option-A-by-default epiphany from v0.8
4. **Pépite system as 7th memory type** — shipped v0.7, zero real pépites flagged yet (implementation work, not research, for the last two sessions). v0.9.0 has an opportunity if the polish work surfaces any cross-project insight
5. **Layer 0 ↔ project memory inheritance** — the pattern is documented in `~/.claude/CLAUDE.md` and used throughout, but Genesis's own `CLAUDE.md` + `memory/master.md` only reference it indirectly. v0.9.0 could add a short "How project memory inherits from Layer 0" section that makes the pattern discoverable without reading Layer 0 first
6. **Meta-Memory Path C** — confirmed as primary path 2026-04-15 (Wave 2 addendum). Genesis v1 is the vehicle. v0.9.0 should make the Path C decision visible in `memory/master.md` as a stable pointer, not just implicit in the Aurum frozen-scope-lock rule

These six items form a theme: **make the meta-memory architecture visible in the plugin's own docs, not just implicit in the skill implementations**. That's the Path-A-with-memory-context scope.

## The next concrete action — v0.9.0 polish pass

### Target

- **Rating floor**: 8.5/10 (polish sessions can rate lower than feature sessions, but this one needs at least 8.5 to pull the running average above 8.5)
- **Running average after v0.9.0**: `(7.6 + 8.2 + 8.8 + 8.4 + 8.6 + 8.8 + 9.0 + 8.5) / 8 = 8.49/10` if v0.9 = 8.5 (same rounding!), so the polish actually needs to rate at ≥ 8.6 to cleanly clear 8.5. Math: `8 × 8.5 - 8.49 × 7 = 68.0 - 59.43 = 8.57`. So **v0.9.0 rating ≥ 8.57 ⟺ running average ≥ 8.50 after v0.9.0**
- **Practical floor**: **8.6/10** for a cleanly-above-target running average, with 8.8+ giving some headroom

### Subtasks for the v0.9.0 session (ordered by rating impact)

1. **Sync main**: `git fetch origin && git pull --ff-only origin main` — should be at `0d2616f` (v0.8.0 tag) plus whatever chore PR shipped this file
2. **Read in order** (memory-context-first, not file-list-first):
   - `memory/MEMORY.md` + `memory/master.md` (R1.1 discipline)
   - `memory/project/session_v0_8_0_genesis_protocol.md` (prior session memory — **especially the "Path A decision" and "Gaps logged" sections**)
   - `memory/project/session_v1_bootstrap.md` (origin session — scope the v1 narrative fresh)
   - `memory/journal/INDEX.md` + the one entry there (journal is dormant; surfacing its one entry is worth the minute)
   - `memory/pepites/INDEX.md` (pépites are still empty — v0.9.0 is a chance if research surfaces anything)
   - `~/.claude/CLAUDE.md` — **skim the Layer 0 index, pay attention to `addendum_2026-04-15_wave2_session_discipline.md` for Meta-Memory Path C confirmation**
   - Each of the six shipped skills' `SKILL.md` **at a skim level** — the goal is to have the composition surface in working memory, not to re-read them in detail
3. **Create chore worktree**: `chore/2026-04-16_v0_9_0-polish` under `.claude/worktrees/` (this is a polish session, not a feature session — `chore/` prefix is correct)
4. **The polish subtasks, in priority order**:

   **Priority 1 — Dry-run validation (highest rating lift, highest risk-reduction)**
   - Create `C:\tmp\genesis-dryrun\` — a fresh empty folder
   - Write a minimal `config.txt` inside it with all mandatory fields (name, slug, vision, license, is-a-plugin: no, plan tier)
   - Walk through the genesis-protocol phase runbooks **on paper** against that folder — don't actually run installers or create SSH keys or touch GitHub, but trace every file the orchestrator would write and every sibling it would invoke
   - Surface any friction, path escape issue, Windows-specific gotcha, or missing step as a v0.9 patch
   - Do NOT create real SSH keys, do NOT create a real GitHub repo, do NOT run installers. This is a **validation walkthrough**, not an execution
   - If the dry-run uncovers fixes, land them as granular commits in the chore branch
   - Expected rating impact: +0.3 on pain-driven, +0.1 on best-at-date (validated-by-execution partial-credit)

   **Priority 2 — README public-facing polish (highest user-visible lift)**
   - Current README says "Genesis v1 is coming soon" — replace with real narrative
   - Sections to add: (a) what Genesis does in one paragraph; (b) the 7-phase protocol in one table; (c) the six skills with one-line descriptions each; (d) "How to bootstrap a project with Genesis" 5-step quickstart; (e) pointer to `memory/master.md` for the full vision; (f) pointer to `CHANGELOG.md` for history and self-rating discipline; (g) MIT license + SPDX note
   - Keep it concise — 80-120 lines total. Not a full doc site, a landing README
   - Expected rating impact: +0.2 on prose cleanliness, +0.1 on self-contained (the plugin becomes discoverable without reading CLAUDE.md first)

   **Priority 3 — Meta-memory visibility in plugin docs**
   - Add a short "Layer 0 inheritance" section to `memory/master.md` explaining how project memory inherits universal rules and patterns from `~/.claude/CLAUDE.md`. Three paragraphs maximum
   - Add a "Cross-skill patterns" section to `memory/master.md` naming the three patterns that have emerged: 1:1 spec mirror discipline (journal-system + pepite-flagging + genesis-protocol), concentrated-privilege map (all six skills), granular-commits-inside-feat-branch (every session since v0.5)
   - Optionally, promote these to a new `specs/` file if they feel big enough (`specs/v1_cross_skill_patterns.md`?)
   - Expected rating impact: +0.2 on pain-driven (visibility turns implicit patterns into debuggable explicit ones), +0.1 on anti-Frankenstein (patterns named = patterns defended)

   **Priority 4 — Phase-file length trimming** (only if the prior three leave time)
   - `phase-1-rules-memory.md` (280 lines), `phase-3-git-init.md` (298 lines), `phase-6-commit-push.md` (318 lines) are longer than the 5-skill median
   - Target ~10-15% trim without content loss. Areas to compress: redundant anti-Frankenstein reminders (keep one per file, remove the per-step echoes), over-enumerated exit conditions (keep the critical ones, drop the restatements), verbose "common failures" sections (keep the ones that actually happen, drop the theoretical ones)
   - Expected rating impact: +0.1 on prose cleanliness (only worth doing if the three above land; otherwise this is mechanical and dilutes the "memory context" framing)

   **Priority 5 — Dogfood run 3 for `session-post-processor`** (if the polish yields a clean bootstrap session archive)
   - The v0.9.0 session's own JSONL archived via the sibling skill = dogfood run 3
   - If the archive lands CLEAN (halt-on-leak GREEN), the skill has now passed three successful manual runs and **hook wiring becomes permissible for v1.0.0** (this is the original v0.5 discipline threshold)
   - This is high-leverage because it unlocks v1.0.0's hook wiring option, but it depends on the session running long enough to archive
   - Expected rating impact: +0.2 on self-contained (proven pipeline), +0.1 on pain-driven (closes the v0.5 gap)

5. **Update `CHANGELOG.md`** with a v0.9.0 entry + 5-axis self-rating. Be honest about which priorities landed and which didn't — partial polish is fine as long as the rating justifies
6. **Bump `plugin.json`** to `0.9.0`
7. **Granular commits** inside the chore branch — one per priority item that actually shipped
8. **Push, PR, squash merge, tag `v0.9.0`** — same flow as v0.2 → v0.8
9. **Memory entry**: `memory/project/session_v0_9_0_polish.md` following the v0.8 structure
10. **Resume prompt**: `v0.9.0 → v1.0.0 ship` — the next session after this one is the actual v1.0.0 tag session, Aurum freeze lifts, public announcement starts

### The v1.0.0 ship gate at end of v0.9.0

After v0.9.0 merges:

- Compute the new running average: `(sum_through_v0_8 + v0_9_rating) / 8`
- If ≥ 8.50 → propose v1.0.0 tag at session close with explicit user confirmation (same pattern as today's Path A/B decision)
- If < 8.50 → honest assessment, propose v0.9.1 tiny-polish OR accept the gap and ship v1.0.0 anyway with documented gap
- Do NOT auto-tag v1.0.0. The user confirms every major tag

### Reinforced disciplines (cumulative)

- **Granular commits inside the chore branch** — eighth session. Default discipline
- **Speech-native memory triggers are live** — journal system can be invoked mid-session with "ouvre une pensée sur X"; pépites are detected automatically during any research operation
- **Concentrated-privilege map as precedent** — v0.9.0 should not introduce any new privileged operation. If a polish requires one, it's out of scope for v0.9
- **R2.1 worktree discipline** — chore worktree first, before any Edit/Write
- **R8 TTL scan at session open** — same as every session. If anything in `sota/` or `stack/` has expired, archive first
- **Path A / Path B framing at close** — if the rating lands below the v1 target, present both paths to the user, don't auto-decide
- **"Leverage memory context" as an explicit scope anchor** — the user's framing for Path A. Every polish decision should trace back to something learned in v1-v0.8, not to generic code hygiene

### Acceptance criteria for v0.9.0

- Every new or modified file has an SPDX header (existing files keep theirs)
- `CHANGELOG.md` updated with v0.9.0 entry and 5-axis self-rating
- `plugin.json` version = `0.9.0`
- PR cleanly squash-merged to origin main
- Git tag `v0.9.0` on origin
- Worktree retained per R2.5
- Granular commits inside the chore branch (at least one per shipped priority)
- **At least Priority 1 (dry-run validation) OR Priority 2 (README polish) landed** — these are the two highest-rating-impact items and anchor the session's value
- **Self-rating ≥ 8.6/10** to justify the running average reaching ≥ 8.50
- User confirms v1.0.0 tag (next session) explicitly, or picks a v0.9.1 path

### Things to NOT do in the v0.9.0 session

- **Do not add any new skill** — the six skills are stable. v0.9.0 is polish, not feature
- **Do not modify any sibling skill's core flow** — if a dry-run uncovers a bug, fix it in the sibling; but don't rewrite the six-step flow of `phase-5-5-auth-preflight` "because the polish session is the chance to revisit"
- **Do not run the genesis-protocol orchestrator end-to-end for real** — the dry-run is a **walkthrough**, not an execution. Running it for real means creating actual SSH keys, real PATs, real GitHub repos, and that's v1.1 territory (first real downstream bootstrap)
- **Do not wire any hook yet** — even if dogfood run 3 lands CLEAN, hook wiring is a **v1.0.0 candidate** to be decided at ship time, not a v0.9.0 action
- **Do not skip the self-rating block** — R10.3 gate, eighth consecutive session
- **Do not auto-tag v1.0.0 at the end of v0.9.0** — ship v0.9.0 first, then explicit user confirmation for v1.0.0 in the following session
- **Do not treat the polish as "trim to make numbers work"** — the user's framing is "leverage memory context". If a decision doesn't trace to something learned in v1-v0.8, defer it
- **If the user says `frankenstein`**, back out of the last polish and simplify. A polish session that over-engineers is the classic Frankenstein trap — the floor is "what did we learn, how do we make it visible", not "what else could be better"

## Open questions worth flagging before v0.9.0 starts

1. **Priority ordering** — the resume suggests dry-run > README > meta-memory visibility > phase trim > dogfood run 3. The user may want a different order, or a subset. Ask at the start
2. **Dogfood run 3** — if the v0.9.0 session generates enough transcript to be worth archiving, should `session-post-processor` run against it mid-session? This is the cleanest way to hit run 3 inside v0.9. Ask at the start
3. **README scope** — "real narrative" is vague. Does the user want a 80-line landing README, a 200-line README with examples, or a README pointing to a future GitHub Pages site (deferred to v2)? Ask at the start
4. **Meta-memory visibility location** — add to `memory/master.md`, or promote to a new `specs/` file? The `specs/` promotion is bigger scope but more discoverable. Ask if the session has time for both
5. **v1.1 backlog versus v0.9.0 scope** — the known gaps (hook wiring, multi-slug warning, test harness, allow-list) are all v1.1 candidates. None of them should land in v0.9 unless they fall out naturally from the polish. Confirm at start

These are not blockers — the next session decides on the fly.

## Exact phrase for the next session

Open Claude Code in `C:\Dev\Claude_cowork\project-genesis\` and say:

```
On reprend v0.9.0 Project Genesis — Path A polish vers v1.0.0. Lis
le resume prompt le plus récent dans .claude/docs/superpowers/resume/,
puis scope le polish en priorité : dry-run validation contre un tmp
folder, README public-facing, visibilité meta-memory dans master.md,
trim optionnel des phase files. Target rating ≥ 8.6/10 pour clean la
running average au-dessus de 8.5. Leverage le contexte mémoriel cumulé
— c'est ça le point de Path A, pas un number-chase. Commits granulaires
dans le chore branch.
```

Claude Code then runs R1.1, creates the chore worktree, reads this file + the v0.8.0 session memory + master.md's 7-phase table + the six shipped SKILL.md files, confirms the priority ordering, and implements.

## References the next session will consume

- `memory/project/session_v0_8_0_genesis_protocol.md` — prior session memory with the concentrated-privilege map, the Path A decision, gaps logged, forward map
- `memory/master.md` — **the 7-phase protocol canonical description** — source of truth for the orchestrator's structure and the anchor for v0.9.0's meta-memory visibility additions
- `memory/project/session_v1_bootstrap.md` — origin session for scoping the v1 narrative fresh
- `memory/journal/2026-04-15_slug-rule-live-dogfood-correction.md` — the project's one journal entry, relevant if v0.9.0 wants to add a second entry on the Option-A-by-default epiphany from v0.8
- `memory/pepites/INDEX.md` — still empty; live opportunity if v0.9.0 research surfaces anything
- `~/.claude/CLAUDE.md` — Layer 0 index; specifically the `addendum_2026-04-15_wave2_session_discipline.md` for Meta-Memory Path C confirmation, and `workflow_research_and_memory.md` for R8 + memory scope routing
- `~/.claude/memory/layer0/rules_hard_discipline.md` — Layer 0 hard rules; relevant for any decision touching auth, windows, language, or worktree
- `skills/genesis-protocol/SKILL.md` — for the 7-phase master table and concentrated-privilege map (anchor for meta-memory visibility work)
- `skills/genesis-protocol/phase-{0,1,3,6}-*.md` — for the phase-file trim priority
- `skills/session-post-processor/SKILL.md` + `run.py` — for the dogfood run 3 priority
- `CHANGELOG.md` — for the v0.8.0 entry, self-rating, and "Next version target" paths A/B

## Farewell notes to the next session's Claude

v0.8.0 shipped at 9.0/10 — Option A (pure Markdown) confirmed empirically, third 1:1 spec mirror landed, file folding justified in both docs and session memory, concentrated-privilege map now has six data points. The orchestrator closes the recursive loop structurally — Genesis can now describe how to bootstrap a new project using the same skills it was bootstrapped with — but the loop is not yet empirically closed because the orchestrator has not been run against a real downstream project.

v0.9.0 is the **polish session that makes the memory/meta-memory context visible** and validates the runbooks via dry-run. The user picked Path A with the explicit framing that this is about leveraging cumulated context, not chasing numbers. Honor that framing — every polish decision should trace back to something learned in v1-v0.8. If a decision feels mechanical, defer it or reframe it.

The running average of 8.49 is **0.01 below** the v1 target. This is the tiniest possible formal miss. A polish rating of 8.6 or higher pulls the running average to ≥ 8.50 cleanly. That's the arithmetic target, but the real target is "make v1.0.0 worth shipping" — which means the polish should feel like a natural closing move, not a last-mile grind.

v1.0.0 ships in the session **after** v0.9.0. Aurum unfreeze is at v1.0.0, not at v0.9. The chore PR that ships this resume prompt is the handoff — when the user reads this file at the next session open, they should feel the continuity with v0.8 and the clarity of scope.

Good luck. The recursive loop is one polish session away from being genuinely complete.
