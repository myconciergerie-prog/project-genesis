<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-16 v0.9.0 → v1.0.0 ship
description: Handoff from the v0.9.0 Path A polish session (running average 8.54/10, target cleared) to the v1.0.0 ship session. All six skills shipped. Running average above target. Dogfood run 3 GREEN — hook wiring eligible. The next session tags v1.0.0 with explicit user confirmation, lifts the Aurum freeze, and starts the public announcement.
type: resume
previous_session: 2026-04-16 v0.9.0 Path A polish
next_action: Tag v1.0.0 with user confirmation. Lift Aurum freeze. Decide on SessionEnd hook wiring. Write the public announcement. The first real downstream bootstrap is a v1.1 milestone.
---

# Resume prompt — 2026-04-16 v0.9.0 → v1.0.0 ship

## Context — what the v0.9.0 session did

The v0.9.0 session was a **Path A polish** — leveraging the cumulated memory/meta-memory context from v1 bootstrap through v0.8 to land a cleaner v1.0.0, not a mechanical number-chase. Every change traces back to a specific lesson from v1 → v0.8 shipping.

1. **Dry-run walkthrough** against `C:\tmp\genesis-dryrun\` — paper trace of the 7-phase orchestrator against a synthetic target folder with a minimal `config.txt` (slug `dryrun-demo`, is-a-plugin no). 10 findings surfaced (5 medium-severity fixed, 5 low-severity deferred to v1.1). First end-to-end trace of the orchestrator against a non-Genesis target. Full details: `memory/project/dryrun_walkthrough_2026-04-16.md`.
2. **`master.md` meta-memory sections** — two new sections: "Layer 0 inheritance" (what `~/.claude/CLAUDE.md` owns vs what Genesis owns locally) and "Cross-skill patterns" (1:1 spec mirror discipline, concentrated-privilege map, granular-commits-inside-feat-branch). Makes the architecture visible in the plugin's own docs, not implicit in skill implementations.
3. **README public-facing rewrite** — 129-line bilingual EN+FR landing page replacing the v0.1.0 scaffold-era prose. 7-phase table, six skills with one-liners, 5-step quickstart, pointers to `master.md` and `CHANGELOG.md`.
4. **R8 housekeeping** — two expired stack entries refreshed.
5. **Dogfood run 3 GREEN** — `session-post-processor` against this session's own JSONL. 312 records, 21 minutes, 113 tool calls, halt-on-leak gate CLEAN (14/14 patterns, 5 base64 false-positives). Third consecutive successful manual run. **Hook wiring is now eligible per the v0.5 discipline.**
6. **Phase-file trim explicitly skipped** — the dry-run added precision content to the same phase files; mechanical trimming would partially undo the additions. Documented in CHANGELOG with rationale.

**Self-rating**: 8.92/10. **Running average**: v0.2 → v0.9 = **(7.6 + 8.2 + 8.8 + 8.4 + 8.6 + 8.8 + 9.0 + 8.92) / 8 = 8.54/10**. **0.04 above** the v1.0.0 target of 8.5. Path A succeeded — clean clearance with headroom.

## The v1.0.0 ship gate — now open

All preconditions are met:

- **Running average ≥ 8.50** — 8.54/10. Headroom of 0.04.
- **All six skills shipped** — `phase-minus-one`, `phase-5-5-auth-preflight`, `journal-system`, `session-post-processor`, `pepite-flagging`, `genesis-protocol`. Zero stubs. Zero reimplementations.
- **README is public-facing** — real landing-page narrative, not "v1 is coming soon".
- **Meta-memory architecture is documented** — Layer 0 inheritance and cross-skill patterns visible in `master.md`.
- **Orchestrator validated** — paper trace surfaced 10 findings, 5 fixed. The structure is sound.
- **Dogfood run 3 GREEN** — hook wiring is an option, not a blocker.

The v1.0.0 tag is the **user's call**. Do NOT auto-tag.

## What the v1.0.0 session should do

### Core (required for the tag)

1. **Read this resume prompt + the v0.9.0 session memory** (`memory/project/session_v0_9_0_polish.md`). Follow R1.1 open ritual.
2. **Decide on hook wiring** — `SessionEnd` hook for `session-post-processor` auto-invocation. Three options:
   - **Wire it now** (v1.0.0 ships with auto-archiving) — highest-value, dogfood run 3 GREEN justifies it
   - **Defer to v1.1** — safest, keeps v1.0.0 as a pure no-runtime ship
   - **Wire it but gated behind a flag** — middle ground, hook exists but disabled by default in `hooks.json`
3. **Tag v1.0.0** on the merge commit of the chore PR that ships this resume prompt + the v0.9.0 session memory. Explicit user confirmation. Push the tag.
4. **Lift the Aurum freeze** — update `memory/project/aurum_frozen_scope_lock.md` to record the unfreeze at v1.0.0 + the unfreeze date. This is the signal that the next session after v1.0.0 can be an Aurum session.
5. **Session memory entry** — `memory/project/session_v1_0_0_ship.md` following the v0.9 structure.

### Optional but high-value

6. **Public announcement prep** — draft the GitHub release notes for v1.0.0 (can use `gh release create`). The CHANGELOG v0.2 → v0.9 entries are the raw material.
7. **First downstream bootstrap plan** — write a brief plan in `.claude/docs/superpowers/plans/` for the v1.1 first-real-run: which project to bootstrap, which `config.txt`, which consent-card values. Aurum.ai is the natural first candidate (it was the Genesis progenitor, and the freeze has just lifted).
8. **v1.1 backlog consolidation** — pull together the 5 deferred dry-run findings, the multi-slug YELLOW, the test vector harness, the allow-list, and any v1.0.0 decisions into a single v1.1 scope document.

### Things to NOT do in the v1.0.0 session

- **Do not add any new skill** — six skills, stable.
- **Do not polish further** — Path A is complete. The running average is above target. Any further polish is feature creep against the "ship v1.0.0" scope.
- **Do not run the genesis-protocol orchestrator for real** — that is v1.1, not v1.0.0. The paper trace is validated; real execution has its own scope.
- **Do not merge any change to the orchestrator runbooks** — v0.9.0 precision fixes are shipped. Runbook changes are v1.1+.
- **Do not auto-tag** — the user confirms every major version tag.

## Reinforced disciplines (cumulative from v0.2)

- Granular commits inside the chore branch (ninth consecutive session).
- 1:1 spec mirror discipline (three applications shipped, pattern named in `master.md`).
- Concentrated-privilege map (six data points, pattern named).
- Worktree discipline R2.1 — chore worktree first, all edits inside.
- R8 TTL scan at session open.
- Self-rating per R10.3 at every version bump.
- PAT via `GH_TOKEN` env override (ninth consecutive session, additive auth preserved).
- Path A / Path B user-decision framing at session close (applied at v0.8.0 close and confirmed at v0.9.0 open).
- Dogfood run threshold met — 3 successful manual runs before any hook wiring.

## References the v1.0.0 session will consume

- `memory/project/session_v0_9_0_polish.md` — prior session memory (this session's self-rating, dry-run summary, forward map)
- `memory/master.md` — stable vision; **specifically the new "Layer 0 inheritance" and "Cross-skill patterns" sections** added in v0.9.0
- `memory/project/dryrun_walkthrough_2026-04-16.md` — the 10 findings, 5 fixed, 5 deferred
- `memory/project/aurum_frozen_scope_lock.md` — the Aurum freeze to lift
- `CHANGELOG.md` — the complete v0.2 → v0.9 history with self-ratings (raw material for release notes)
- `~/.claude/CLAUDE.md` — Layer 0 for R1.1 open ritual

## Exact phrase for the next session

Open Claude Code in `C:\Dev\Claude_cowork\project-genesis\` and say:

```
On lance v1.0.0 Project Genesis — ship session. Lis le resume prompt
v0.9.0→v1.0.0, confirme le gate (running average 8.54 ≥ 8.5, six skills,
dogfood run 3 GREEN), décide du hook wiring SessionEnd, tag v1.0.0,
lift Aurum freeze. C'est le closing move.
```

## Farewell notes to the next session's Claude

v0.9.0 shipped at 8.92/10. Path A was the right call — it turned a 0.01 formal miss into a 0.04 clean clearance, and it did so by making the meta-memory architecture visible rather than by trimming files. The three named cross-skill patterns in `master.md` are the v0.9.0 session's contribution to the project's long-term coherence — they document what six skills already do, not what some future skill should aspire to. That's the "leverage memory context" framing honored.

The dogfood run 3 GREEN is the quietly high-leverage result. Hook wiring for `SessionEnd` is now an option the user can choose to ship at v1.0.0 — it turns session-post-processor from "Claude remembers to invoke it" to "it runs automatically at session close". That's a significant UX upgrade if the user opts in. But the choice is theirs — the three manual runs are the threshold, not a mandate.

The Aurum freeze has been in effect since genesis session 1 on 2026-04-14. Lifting it at v1.0.0 is the signal that Genesis has shipped and Aurum development can resume. The first Aurum session after the freeze lifts is the immediately-next workstream. If the user wants to bootstrap Aurum afresh using Genesis v1.0.0, that's the v1.1 dogfood candidate — the strange loop completing for real, not just on paper.

Good luck. v1.0.0 is one tag away.
