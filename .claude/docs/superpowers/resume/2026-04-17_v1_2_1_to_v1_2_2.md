<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-17 v1.2.1 → v1.2.2
description: Handoff from v1.2.1 paradox-guards ship session. Three P0 fixes landed surgically from v1.2.0 self-dogfood. v1.2.2 picks from the P1 queue (F20/F22 mode-auto semantics most urgent, F34 gh active-account second) or pivots to v2 Étape 0 drop-zone build.
type: resume
previous_session: v1.2.1 paradox guards (PR #24, tag v1.2.1)
next_action: Pick v1.2.2 scope from P1 queue or start v2 Étape 0
---

# Resume — 2026-04-17 v1.2.1 → v1.2.2

## What v1.2.1 shipped

**Tag v1.2.1 on `0a1d138`** (PR #24 merged). Three P0 fixes surgically landed:

- **F29** — `v1_rules.md` relocated inside the `genesis-protocol` skill. Plugin self-contained across all install modes. F18 workaround no longer has a downstream break.
- **F30** — Phase 3.1 now uses `git rev-parse --show-toplevel` three-way dispatch. Nested-repo silent `git init` impossible. Follow-up commit documents the git-bash MSYS/POSIX path format divergence.
- **F23 + F27** — Step 0 paradox guards (Guard A target-inside-orchestrator + Guard B self-collision-slug) refuse strange-loop targets structurally. No override flag.

Self-rating **9.26/10** — new single-version high. Running average **8.65/10**. Full narrative at `memory/project/session_v1_2_1_paradox_guards.md`.

**Live F34 reproduction during PR creation** (`gh auth switch` required): documents P1 priority for v1.2.2, not fixed in this cycle.

## Current state

- Repo: `myconciergerie-prog/project-genesis` on main, tag `v1.2.1`, commit `0a1d138`.
- Tags chain: `v1.0.0` → `v1.1.0` → `v1.2.0` → `v1.2.1`.
- git status: clean, all pushed.
- `gh auth`: two accounts (`myconciergerie-prog` + `myconciergerieavelizy-cloud`); **always verify active account with `gh api user --jq .login` before `gh pr create`** until F34 is fixed.

## Deferred v1.2.2 candidates (P1 first)

| Priority | Friction | Fix sketch |
|---|---|---|
| P1 | F20, F22 | `mode` argument (detailed / semi-auto / auto) as first-class orchestrator parameter; consent cards become informational-only in auto mode except for true security floor (OAuth click, PAT creation) |
| P1 | F21 | `## Arguments` section in `SKILL.md` documenting `mode`, `target`, `seed`, `consent` with defaults |
| P1 | F34 | Phase 6 Step 6.0 pre-flight: `gh api user --jq .login` compared to resolved `<owner>`; `gh auth switch -u <owner>` attempt before halt |
| P2 | F24 | Phase 0.1 `git rev-parse --show-toplevel` probe (same probe as F30, earlier in the flow) |
| P2 | F25, F31 | `templates/config-minimal.txt.example` + `templates/config-complete.txt.example` + README Quick Start |
| P2 | F26 | Non-canonical fields passed through to `bootstrap_intent.md` under a "## Non-canonical (passed through)" audit section |
| P3 | F28 | `genesis-cleanup` sibling skill (delete SSH key pair, remove `~/.ssh/config` block, offer to delete target folder) |
| P3 | F33 | R8 cache scope disambiguation note for orchestrator-about-itself vs orchestrator-about-target |
| v1.3 | F32 | Lightweight Python driver for the orchestrator — argument parsing, state tracking, re-entrancy |

## Two natural paths forward

### Path A — continue the v1.2.x refinement loop (P1 cluster)

Bundle F20/F22/F21 into a single v1.2.2 PR: add mode-auto as a proper orchestrator argument with documented semantics, along with the `## Arguments` section. Then F34 in v1.2.3 (its own focused PR). Est. 2 sessions.

**Arguments for Path A**: smaller diffs, each PR has clear scope, preserves the one-fix-per-PR discipline that scored 9.26 in v1.2.1.

### Path B — pivot to v2 Étape 0 (Le Dépôt drop-zone)

The v1.2.0 session ended with Étape 0 prepended to `specs/v2_vision_promptor_fusion.md`. Research cache has the 2026 UX canon ready (`v2_promptor_fusion_landscape_2026-04-17.md`). Start building the drop-zone surface that sits in front of the orchestrator — intent-first unified box, Files API + Citations, relationship-language privacy.

**Arguments for Path B**: the v2 story is what the Victor test ultimately depends on. The P1 cluster is internal plumbing; v2 Étape 0 is the visible product shift. Also the research TTL is fresh (expires 2026-04-24).

## Files to read at Session open

1. `memory/MEMORY.md`
2. `memory/master.md`
3. `memory/project/session_v1_2_1_paradox_guards.md`
4. `memory/project/selfdogfood_friction_log_v1_2_0_2026-04-17.md` (the P1 queue context)
5. This resume prompt
6. For Path B: `specs/v2_vision_promptor_fusion.md` + `.claude/docs/superpowers/research/sota/v2_promptor_fusion_landscape_2026-04-17.md`

## R8 cache state

- 2 `stack/` entries refreshed 2026-04-16, TTL expires 2026-04-17 (today at session open). Scan INDEX.md at R1.1 step 5 and either refresh or archive before any new research.
- `v2_promptor_fusion_landscape_2026-04-17.md` fresh, expires 2026-04-24 — usable for Path B.

## Scope lock

Aurum remains frozen at `0b1de3d` per original v1.0.0 freeze-lift conditions — the freeze was lifted at v1.0.0 ship, so no active lock. Verify at session open by reading `memory/project/aurum_frozen_scope_lock.md`.

## Exact phrase for the next session

Open Claude Code in `C:/Dev/Claude_cowork/project-genesis/` and paste:

```
mode autonome  Je reviens après le ship v1.2.1 (F29 + F30 + F23/F27
paradox guards, self-rating 9.26/10, running avg 8.65). Lis le
resume v1.2.1→v1.2.2, le session trace v1.2.1, et le friction log
v1.2.0 P1 queue. Propose-moi Path A (P1 cluster F20/F22/F21 + F34)
vs Path B (v2 Étape 0 drop-zone) avec ta reco, puis on attaque.
/using-superpowers
```
