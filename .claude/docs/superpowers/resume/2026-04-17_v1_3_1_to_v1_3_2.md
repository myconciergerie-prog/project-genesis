<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-17 v1.3.1 → v1.3.2
description: Handoff from the v1.3.1 genesis-drop-zone extraction mirror ship. First PATCH on the v1.3.x conversational-layer line. Three candidates for v1.3.2 — (A) bootstrap_intent.md write + handoff (first Layer A concentrated privilege), (B) Path A Citations API upgrade (audit-trail on extracted fields), (C) runtime locale detection.
type: resume
previous_session: v1.3.1 genesis-drop-zone extraction mirror
next_action: Pick v1.3.2 scope (A write + handoff / B API Citations / C locale) OR another direction
---

# Resume — 2026-04-17 v1.3.1 → v1.3.2

## What v1.3.1 shipped

**Tag v1.3.1** (PR #? merged). First PATCH on the v1.3.x conversational-layer line (v1.2.4 → v1.3.0 had been the previous MINOR). Six-commit bundle in one PR (spec + spec polish + plan + plan polish + feat + chore).

- **feat** — `skills/genesis-drop-zone/phase-0-welcome.md` gets FR + EN mirror templates (9-row aligned-column tables revealed row-by-row) plus the v1.3.1 bridge text ("Création du projet (GitHub, fichiers, mémoire) arrive bientôt. Pour l'instant, j'ai lu et compris — reviens à Claude Code normalement."). `skills/genesis-drop-zone/SKILL.md` gets `In scope (v1.3.1)` sub-block, `Phase 0 — acknowledgement` → `Phase 0 — mirror` rename + rewrite, schema table (9 fields), null-class convention (3 classes), truncation rules, updated concentrated-privilege forward note (points to v1.3.2 as the first Layer A privilege ship). `plugin.json` bumped 1.3.0 → 1.3.1. `memory/master.md` privilege-map entry freshened. Net diff: 4 files, +150 / −54.
- **chore** — CHANGELOG v1.3.1 + session trace + MEMORY.md pointer + this resume prompt.

Self-rating **9.30/10**. Running average **8.81/10** (up from 8.78). **Six consecutive ships ≥ 9.0** (v1.2.1 9.26, v1.2.2 9.14, v1.2.3 9.18, v1.2.4 9.16, v1.3.0 9.34, v1.3.1 9.30). Best-at-date held at 9.2 via the same R8 entry (`v2_promptor_fusion_landscape_2026-04-17.md`, still fresh).

Full narrative at `memory/project/session_v1_3_1_extraction_mirror.md`.

## Current state

- Repo: `myconciergerie-prog/project-genesis` on main, tag `v1.3.1`, commit `<merge-sha>`.
- Tags chain: `v1.0.0` → `v1.1.0` → `v1.2.0` → `v1.2.1` → `v1.2.2` → `v1.2.3` → `v1.2.4` → `v1.3.0` → **`v1.3.1`**.
- git status: clean, all pushed.
- `gh auth`: still two accounts (`myconciergerie-prog` + `myconciergerieavelizy-cloud`); R2.3.1 pre-flight holds at every gh write op (proven again in v1.3.1 push / PR create / PR merge).
- Skill count: 7 (six Layer B + one Layer A). Unchanged from v1.3.0 — v1.3.1 extends, doesn't add.
- Cross-skill patterns: 4 named (1:1 spec mirror / concentrated-privilege map / granular-commits-inside-feat-branch / Layer A / Layer B stratification). Unchanged from v1.3.0.
- Concentrated privilege of `genesis-drop-zone` remains `none` across both v1.3.0 and v1.3.1. First Layer A privilege ship queued for v1.3.2.
- P1 queue: empty at both bootstrap and downstream-rule levels (closed v1.2.3 and v1.2.4). v1.3.x is a feature-line, not a P-fix line.
- Living-spec pattern now proven: `v2_etape_0_drop_zone.md` evolves across versions with version-scoped sections (`## Scope — v1.3.0 vertical slice` + `## Scope — v1.3.1 extraction`). First time this pattern runs at full discipline.

## Scenario #1 runtime replay — still owed (unchanged from v1.3.0)

Runtime replay of Scenario #1 (fresh-dir `/genesis-drop-zone` → welcome prints → mirror streams → bridge prints) still requires invoking Claude Code in an empty directory — externally-launched, not executable from inside this session. Artefact-level verification passed (SKILL.md dispatch coherent; FR + EN mirror templates exactly 9 rows each; R9 accent discipline respected inside the main table block; bridge keeps accents; all scenarios #3/#6/#9/#12 pass). The same −0.2 Pain-driven deduction that v1.3.0 carried applies to v1.3.1; it rolls forward until a fresh Claude Code instance runs the scenario.

## v1.3.2 candidates

### Candidate A — bootstrap_intent.md write + target-dir resolution + handoff

Ship the "write the extracted intent to disk + hand off to `genesis-protocol`" increment:

1. Consent prompt before any write — bilingual card with target directory resolution (cwd / subdir / new project dir), overwrite protection.
2. `bootstrap_intent.md` file write — UTF-8, the 9-field schema serialized in a human-readable format (YAML frontmatter or Markdown table — decide at brainstorming).
3. Handoff to `genesis-protocol` Phase 0 — invoke with `bootstrap_intent.md` as the Layer B seed (replaces `config.txt` in v2).

**Why this next**: the resume prompt from v1.3.0 scoped v1.3.2 as exactly this. v1.3.1 made the schema real in-context; v1.3.2 persists it + hands off. Closes the full Étape 0 surface — from drop to handoff.

**Concentrated privilege**: introduces the first Layer A concentrated privilege (`bootstrap_intent.md` write). Precedent-setting ship — consent card, overwrite protection, bilingual confirmation, per-target consent floor (mirrors `pepite-flagging`'s discipline). Worth a careful privilege-declaration section in the spec.

**R8 runway**: `v2_promptor_fusion_landscape_2026-04-17.md` still usable for the handoff pattern (Stage 3 conversational briefing + Layer B phase table deltas). Expires 2026-04-24. If the next session opens on 2026-04-23 or later, refresh the entry first.

**Estimated scope**: probably bigger than v1.3.1 (real plumbing — write, consent, resolution, handoff). Could be split into v1.3.2 (write + consent) + v1.3.3 (handoff) if the ship feels heavy; keep coherent if it doesn't.

### Candidate B — Path A Citations API upgrade

Upgrade v1.3.1's in-context extraction with an Anthropic API call using `citations: {enabled: true}` per `document` block. Each extracted field gets `cited_text` + `document_index` + location traceability. The mirror can optionally show `[page N]` annotations.

**Concentrated privilege**: introduces the first "external API call" privilege for `genesis-drop-zone` — `ANTHROPIC_API_KEY` environment usage + network call + model selection.

**Why pick B over A**: if audit-trail credibility is the immediate blocker (e.g. an engineering consumer of `bootstrap_intent.md` needs source attribution). **But**: without the write + handoff of Candidate A, the audit-trail has no downstream reader. Anti-Frankenstein argues B should sequence AFTER A, or be bundled with A as a two-privilege ship.

**R8 runway**: Stage 2 of `v2_promptor_fusion_landscape_2026-04-17.md` documents the recipe in full. Fresh until 2026-04-24.

**Estimated scope**: smaller-per-field than A, but requires API-call plumbing (subagent dispatch or Python). Could be a v1.3.3 refinement on top of A's v1.3.2 ship.

### Candidate C — runtime locale detection (FR/EN switch)

Wire up the FR/EN variant switching at render time for both welcome box AND mirror. Currently v1.3.1 extracts `langue_detectee` into the schema but always renders FR. v1.3.2 could close the loop — detect user language from trigger match + message content, render in that language.

**Why interesting**: small scope, clean ship, closes a real R9-adjacent gap (EN-native users would see the bilingual bridge but an FR-only welcome + mirror).

**Why not first**: Candidate A is the structural next step (write + handoff). C is refinement of v1.3.1's surface without adding new capability. Better as v1.3.3 or later.

## Files to read at session open

1. `memory/MEMORY.md`
2. `memory/master.md` (no v1.3.1 changes to cross-skill patterns)
3. `memory/project/session_v1_3_1_extraction_mirror.md`
4. This resume prompt
5. `.claude/docs/superpowers/specs/v2_etape_0_drop_zone.md` § "Deferred to v1.3.2+" (for candidate A or B scoping)
6. `.claude/docs/superpowers/specs/v2_vision_promptor_fusion.md` § "Extraction choice" (for candidate B context)
7. `.claude/docs/superpowers/research/sota/v2_promptor_fusion_landscape_2026-04-17.md` Stage 2 (Citations) + Stage 3 (conversational briefing / handoff)

## R8 cache state

- `v2_promptor_fusion_landscape_2026-04-17.md` — fresh, expires **2026-04-24**. Usable for A (handoff), B (Citations), C (locale detection backing). Refresh at session open if opened on 2026-04-23 or later; consider accepting partial-near-expiry if the first v1.3.2 session lands on that window edge.
- `gh-cli-single-click-auth_2026-04-16.md` — fresh, expires 2026-04-23. Usable for any `GH_BROWSER` wire-up in v1.3.2 if the auth revolution piece surfaces.
- `zero-friction-bootstrap-ux_2026-04-16.md` — fresh, expires 2026-04-23. Background for v1.3.2 write-UX polish.
- No new R8 research entries required for any of the three candidates at this point.

## Scope lock

Aurum remains at `0b1de3d`. The v1.0.0 freeze was lifted on v1.0.0 ship, so no active lock. Verify at session open by reading `memory/project/aurum_frozen_scope_lock.md`.

## Exact phrase for the next session

Open Claude Code in `C:/Dev/Claude_cowork/project-genesis/` and paste:

```
mode autonome  Je reviens après le ship v1.3.1 (genesis-drop-zone
extraction mirror, 9-field schema in-context, bullet-list ack remplacé
par table à colonnes alignées, bridge mis à jour, self-rating 9.30/10,
running avg 8.81, sixième ship consécutif ≥ 9.0, privilège toujours
`none`). Lis le resume v1.3.1 → v1.3.2 et le session trace v1.3.1.
Propose-moi v1.3.2 candidat A (bootstrap_intent.md write + target-dir +
handoff, first Layer A privilege), candidat B (Path A Citations API
upgrade pour audit-trail), candidat C (runtime locale detection) avec
ta reco, puis on attaque.
/using-superpowers
```
