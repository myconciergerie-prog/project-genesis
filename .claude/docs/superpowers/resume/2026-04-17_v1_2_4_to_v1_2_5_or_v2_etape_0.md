<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-17 v1.2.4 → v1.2.5 or v2 Étape 0
description: Handoff from v1.2.4 R2.3.1 ship. F34 is now covered at both bootstrap (Step 6.0, v1.2.3) and v0.2.0+ downstream-rule (R2.3.1, v1.2.4) levels — the 1:1 mirror is complete. The next natural boundary is either a v1.2.5 cycle on P2 doc work, or the v2 Étape 0 drop-zone pivot while the research cache is still fresh.
type: resume
previous_session: v1.2.4 R2.3.1 rule-level gh pre-flight (F34 mirror)
next_action: Pick v1.2.5 scope (P2 config examples OR R8 scope disambiguation) OR pivot to v2 Étape 0 drop-zone
---

# Resume — 2026-04-17 v1.2.4 → v1.2.5 or v2 Étape 0

## What v1.2.4 shipped

**Tag v1.2.4** (PR #? merged). One rule-level sub-rule landed as a 2-commit bundle in one PR:

- **R2.3.1 — rule-level mirror of Step 6.0** — `### R2.3.1 gh active-account pre-flight (before any gh write — added v1.2.4)` inserted under `### R2.3 PR & merge` in `skills/genesis-protocol/rules/v1_rules.md`. Resolves target owner from `git remote get-url origin` (single source, no fallbacks needed in v0.2.0+ sessions), runs `gh api user --jq .login`, on mismatch attempts `gh auth switch` with re-verification. Halt / auto-switch / re-verification branches are byte-for-byte structurally identical to Step 6.0 (only `❌ R2.3.1 …` / `❌ Step 6.0 …` differs). Scope: every `gh` write op (pr create / pr merge / release create / repo write-verbs); read-only calls explicitly exempt. Declared a structural stop in every mode. The "Relation to bootstrap Step 6.0" paragraph inside R2.3.1 itself is the anti-drift hook that commits future maintainers to reviewing both when either is touched.
- **R2.3 cross-refs** — two bullet-line parentheticals "(precede with R2.3.1 pre-flight)" added to the R2.3 PR and merge bullets.
- **Chore** — version bump `1.2.3` → `1.2.4`, CHANGELOG, session trace, MEMORY.md pointer, this resume prompt.

Self-rating **9.16/10**. Running average **8.74/10** (up from 8.71). **Four consecutive ships scoring ≥ 9.0** (v1.2.1 9.26, v1.2.2 9.14, v1.2.3 9.18, v1.2.4 9.16) — the surgical-commit-per-root-cause / bundle-in-one-PR discipline continues to hold, and the 1:1 mirror discipline extended cleanly from spec pairs to rule/runbook pairs.

Full narrative at `memory/project/session_v1_2_4_rule_gh_preflight.md`.

## Current state

- Repo: `myconciergerie-prog/project-genesis` on main, tag `v1.2.4`, commit `<merge-sha>`.
- Tags chain: `v1.0.0` → `v1.1.0` → `v1.2.0` → `v1.2.1` → `v1.2.2` → `v1.2.3` → `v1.2.4`.
- git status: clean, all pushed.
- `gh auth`: still two accounts (`myconciergerie-prog` + `myconciergerieavelizy-cloud`); **F34 now covered at both levels** — Step 6.0 (v1.2.3) for bootstrap Phase 6, R2.3.1 (v1.2.4) for every v0.2.0+ PR session in every Genesis-bootstrapped project. The verbal reminder that lived in sessions v1.2.3 and earlier ("always verify active account with `gh api user --jq .login` before `gh pr create`") is now encoded as a rule.
- P1 queue from the v1.2.0 friction log: **CLOSED** at both bootstrap and downstream-rule levels.

## v1.2.5 candidates (if staying in the v1.2.x refinement loop)

### Candidate A — F25/F31 config.txt canonical examples

Ship `templates/config-minimal.txt.example` (3-line free-form) and `templates/config-complete.txt.example` (all 8 canonical fields, YAML-ish), plus a pin in README Quick Start. P2 doc work. Scope: 2 template files + a few README lines. Est. ≤ 1 session.

**Why this next**: with the P1 queue closed at both levels, P2 doc work becomes the natural cleanup pass. The config.txt format ambiguity (F25 observed in atelier-playmobil bootstrap, F31 re-observed in the selfdogfood run) is the most-quoted P2 in the friction log. Small scope, clean ship.

### Candidate B — F33 R8 cache scope disambiguation

Formalize the `project-specific | transverse | universal` scope tagging that Layer 0 already references in `workflow_research_and_memory.md` but that Genesis's local R8 INDEX.md doesn't explicitly surface yet. Frontmatter addition `scope: project|transverse|universal` on every active entry + a one-line column in the INDEX.md Active table + a session-open note in R1.1 step 5. Scope: ~15 lines of INDEX.md + ~7 frontmatter edits + a rules pointer. Est. 1 short session.

**Why this next**: the scope tagging is the mechanism by which Layer 0 Path C (Genesis-as-meta-memory-reference-implementation) becomes self-evidencing. Without it, the universal-scope SOTA convention from Layer 0 addendum 2026-04-15 is enforced by discipline alone. Small, mechanical, low-risk.

### Candidate C — F26 full wire-up (passthrough fields in bootstrap_intent.md)

Complete what v1.2.2 half-fixed: edit `skills/genesis-protocol/phase-0-seed-loading.md` to explicitly write `## Non-canonical fields (passed through)` into `bootstrap_intent.md` at Phase 0.5 when non-canonical kvp args are observed. Scope: ~20 lines in the Phase 0.5 write-template section.

**Why not first**: F26 is borderline — the convention is documented, the actual write is implicit but would benefit from an explicit example. Candidate A closes a more-quoted pain with the same effort.

## v2 Étape 0 — the alternative pivot

Same pivot as the v1.2.2 → v1.2.3 and v1.2.3 → v1.2.4 resume prompts. The research cache `v2_promptor_fusion_landscape_2026-04-17.md` expires **2026-04-24** — about **6 days of runway** remain after v1.2.4 ship.

**Arguments for Path B now vs after v1.2.5**:
- **Now**: mental mode switch is cleaner with P1 queue closed at both levels; no half-finished v1.x work; 1:1 mirror discipline extended cleanly (rule/runbook pairs now a first-class pattern, ready to be named in master.md's cross-skill-patterns section during v2 canvas opening if it earns its stripe); research cache fresh.
- **After v1.2.5**: one more surgical ship extends the streak to a **fifth** ≥ 9.0 (depending on candidate), and v1.2.5 candidate A (config examples) or candidate B (R8 scope tagging) both close small but real pains.

**Suggested order if the user wants both**: v1.2.5 candidate A (config examples, ~1 session) → v2 Étape 0. Takes the most-quoted P2 off the table before opening v2 canvas. Candidate B is also viable as the pre-v2 ship because it's mechanical and closes a Layer 0 evidencing gap.

**Counter-suggestion if the streak is fatiguing**: go straight to v2 Étape 0. The score-as-user-experience framing from the v1.2.3 and v1.2.4 session opens still applies — if the mental mode switch makes the user more productive on v2, that IS user experience. Four consecutive ≥ 9.0 ships is already a meaningful rhythm signal; extending for its own sake is vanity discipline.

## Files to read at session open

1. `memory/MEMORY.md`
2. `memory/master.md`
3. `memory/project/session_v1_2_4_rule_gh_preflight.md`
4. `memory/project/selfdogfood_friction_log_v1_2_0_2026-04-17.md` (deferred-list context, no remaining P1)
5. This resume prompt
6. For Path B (v2 Étape 0): `specs/v2_vision_promptor_fusion.md` + `.claude/docs/superpowers/research/sota/v2_promptor_fusion_landscape_2026-04-17.md`

## R8 cache state

- Stack entries `claude-code-plugin-structure` + `claude-code-session-jsonl-format` archived by v1.2.3 (TTL expired 2026-04-17). Still archived; refresh if drift suspected before any session-post-processor change or plugin structure edit.
- `v2_promptor_fusion_landscape_2026-04-17.md` fresh, expires **2026-04-24** — usable for Path B. Refresh window closes before Path B can be fully completed if Path B is picked on 2026-04-23 or later; consider either a fresh snapshot at session open for Path B, or acceptance that Path B's first session runs partly on near-expired research.
- `gh-cli-single-click-auth_2026-04-16.md` fresh, expires 2026-04-23 — broader prior-art reference for R2.3.1 / Step 6.0 territory; no additional refresh needed for either v1.2.5 candidate.
- No new research entries required for v1.2.5 candidate A (config examples reuse existing seed from bootstrap) or candidate B (R8 scope is Layer 0 convention, no web research needed).

## Scope lock

Aurum remains at `0b1de3d` per the v1.0.0 freeze-lift conditions (the freeze was lifted at v1.0.0 ship, so no active lock). Verify at session open by reading `memory/project/aurum_frozen_scope_lock.md`.

## Exact phrase for the next session

Open Claude Code in `C:/Dev/Claude_cowork/project-genesis/` and paste:

```
mode autonome  Je reviens après le ship v1.2.4 (R2.3.1 rule-level
mirror de Step 6.0 pour sessions v0.2.0+, self-rating 9.16/10,
running avg 8.74, quatrième ship consécutif ≥ 9.0 — P1 couvert aux
deux niveaux bootstrap + downstream). Lis le resume v1.2.4 →
v1.2.5/v2-étape-0 et le session trace v1.2.4. Propose-moi v1.2.5
candidat A (config examples) vs candidat B (R8 scope tagging) vs
Path B (pivot v2 Étape 0 drop-zone) avec ta reco, puis on attaque.
/using-superpowers
```
