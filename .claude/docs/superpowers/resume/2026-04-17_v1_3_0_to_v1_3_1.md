<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-17 v1.3.0 → v1.3.1
description: Handoff from the v1.3.0 genesis-drop-zone welcome vertical slice ship. First MINOR bump since v1.2.0, Layer A conversational front door exists as surface-only. Next natural increments are the v1.3.1 extraction + bootstrap_intent.md write + handoff to genesis-protocol; or a brief v1.2.5-style P2 chore ship if a streak-extending quick-win is preferred before the Path-A Citations wiring.
type: resume
previous_session: v1.3.0 genesis-drop-zone welcome slice
next_action: Pick v1.3.1 scope (extraction + write + handoff) OR a v1.2.5-style P2 chore interlude
---

# Resume — 2026-04-17 v1.3.0 → v1.3.1

## What v1.3.0 shipped

**Tag v1.3.0** (PR #? merged). First MINOR bump since v1.2.0. Two-commit bundle in one PR:

- **feat** — new sibling skill `skills/genesis-drop-zone/` (Layer A conversational front door of Genesis v2) with `SKILL.md` (124 lines, 9 sections mirroring the spec's 8-row Mirrored map) and `phase-0-welcome.md` (97 lines, FR + EN welcome boxes, ack template, bilingual bridge). New implementation-grade spec `.claude/docs/superpowers/specs/v2_etape_0_drop_zone.md` (268 lines) with inline R8 citations at each UX choice. Vision doc (`v2_vision_promptor_fusion.md`) receives one pointer line near § Étape 0 (hybrid-C strategy). `memory/master.md` cross-skill-patterns section gains **pattern #4 = Layer A / Layer B stratification**; concentrated-privilege map extends to 7 entries including `genesis-drop-zone (none)` (journal-system precedent). `skills/README.md` gets the 7th entry. `plugin.json` bumped 1.2.4 → 1.3.0.
- **chore** — CHANGELOG v1.3.0 + session trace + MEMORY.md pointer + this resume prompt.

Self-rating **9.34/10**. Running average **8.78/10** (up from 8.74). **Five consecutive ships ≥ 9.0** (v1.2.1 9.26, v1.2.2 9.14, v1.2.3 9.18, v1.2.4 9.16, v1.3.0 9.34). Best-at-date cleared the 8.6–8.8 PATCH-cycle structural ceiling — inline R8 citations of fresh research was the lever.

Full narrative at `memory/project/session_v1_3_0_drop_zone_welcome.md`.

## Current state

- Repo: `myconciergerie-prog/project-genesis` on main, tag `v1.3.0`, commit `<merge-sha>`.
- Tags chain: `v1.0.0` → `v1.1.0` → `v1.2.0` → `v1.2.1` → `v1.2.2` → `v1.2.3` → `v1.2.4` → **`v1.3.0`**.
- git status: clean, all pushed.
- `gh auth`: still two accounts (`myconciergerie-prog` + `myconciergerieavelizy-cloud`); R2.3.1 pre-flight holds at every gh write op (proven in v1.3.0 push / PR create / PR merge).
- Skill count: 7 (six Layer B + one Layer A).
- Cross-skill patterns: 4 named (1:1 spec mirror / concentrated-privilege map / granular-commits-inside-feat-branch / **Layer A / Layer B stratification**).
- P1 queue: empty at both bootstrap and downstream-rule levels (closed v1.2.3 and v1.2.4). v1.3.x is a feature-line, not a P-fix line.

## Scenario #1 runtime replay — owed

v1.3.0 self-rating Pain-driven axis (-0.2) deducted for the fact that **Scenario #1** (fresh-dir `/genesis-drop-zone` → welcome prints) was not runtime-replayed inside the ship session — it requires a fresh Claude Code invocation in an empty directory, which the ship session could not execute. Artefact-level verification passed (SKILL.md dispatch coherent, phase-0-welcome.md templates syntactically valid, bilingual day-1 confirmed). **The first post-merge action should be this replay**, before any v1.3.1 code work. If the replay fails, the v1.3.0 tag stands but a v1.3.0.1 hotfix (or a small patch in the early v1.3.1 phase) is warranted.

## v1.3.1 candidates

### Candidate A — full Path-A Citations extraction + handoff (the natural next increment)

Ship the v1.3.1 scope declared in the v1.3.0 spec § "Deferred to v1.3.1+":

1. Structured extraction of user intent via Path A (Citations-audited) — `citations: {enabled: true}` per `document` block on dropped files, per-bullet `cited_text` + `document_index` traceability for the mirror screen at Étape 2.
2. `bootstrap_intent.md` file write — consent prompt, target-directory resolution (user-designated fresh project dir), UTF-8 encoding, overwrite protection.
3. Handoff to `genesis-protocol` — invoke with `bootstrap_intent.md` as the Layer B seed, replacing `config.txt` in v2 for the conversational path.

**Why this next**: the v1.3.0 ship demonstrated the surface; v1.3.1 makes it useful. This is the increment the spec's deferred list scoped exactly for next. Concentrated privilege moves from `none` to "one write to user-designated project directory" (first concentrated-privilege introduction for a Layer A skill — setting the precedent for v1.3.x and beyond).

**Research runway**: `v2_promptor_fusion_landscape_2026-04-17.md` is still fresh, expires **2026-04-24** — ~6 days of runway after v1.3.0 ship. Path A Citations recipe is fully documented in the R8 entry Stage 2 section. Refresh at session open if opened on 2026-04-23 or later.

**Estimated scope**: one vertical increment, one PR, 2 commits (feat + chore). Probably slightly bigger than v1.3.0 because extraction logic + file-write consent UX are real plumbing, not pure surface. Concentrated-privilege declaration + mitigation section to be written carefully.

### Candidate B — v1.2.5-style P2 chore interlude (config examples)

Ship the F25/F31 config.txt canonical examples (`templates/config-minimal.txt.example` + `templates/config-complete.txt.example`) as a pure doc ship. This was Candidate A of the v1.2.4 → v1.2.5 resume prompt. Scope: 2 template files + README pin. ≤1 session.

**Why pick this over A**: if the v1.3.1 extraction scope feels heavy and a sixth consecutive streak-ship ≥9.0 on a pure-doc PATCH would prefer the streak-extension. Patch version becomes v1.3.1 with zero functional change to `genesis-drop-zone`, then v1.3.2 is the extraction.

**Why not pick over A**: extending the streak for the streak = vanity discipline (the same warning that applied to v1.2.5 Candidate A originally). The running average +0.04-per-ship cadence is already a meaningful rhythm signal — artificially prolonging it is not what the discipline is for. If A feels heavy, split A into two smaller ships (v1.3.1 = extraction only, v1.3.2 = write + handoff) rather than dodging with a doc PATCH.

### Candidate C — runtime locale detection (small, bounded)

Wire up the FR/EN welcome-box switch via runtime locale detection (from trigger-phrase language match + message content). Currently FR is printed by default and EN is mirror-ready but unused. Scope: add a locale-picker step in SKILL.md + a section in phase-0-welcome.md that selects the variant. ≤1 session.

**Why interesting**: closes a real R9-related surface gap (EN-native users see the bilingual bridge but not the welcome box in their language). Small scope, clean ship.

**Why not first**: Candidate A is the bigger increment and the structural next step; Candidate C is a refinement of v1.3.0's surface without adding new capability. Better as v1.3.2 or later.

## Files to read at session open

1. `memory/MEMORY.md`
2. `memory/master.md` (note the new cross-skill-pattern #4)
3. `memory/project/session_v1_3_0_drop_zone_welcome.md`
4. This resume prompt
5. `.claude/docs/superpowers/specs/v2_etape_0_drop_zone.md` (for the deferred list if picking Candidate A or C)
6. `.claude/docs/superpowers/specs/v2_vision_promptor_fusion.md` § "Extraction choice — Citations (Path A) vs Structured Outputs (Path B)" (for Candidate A)
7. `.claude/docs/superpowers/research/sota/v2_promptor_fusion_landscape_2026-04-17.md` Stage 2 (for Candidate A Citations recipe)

## R8 cache state

- `v2_promptor_fusion_landscape_2026-04-17.md` fresh, expires **2026-04-24** — usable for Candidate A. Refresh at session open if opened on 2026-04-23 or later; consider accepting partial-near-expiry if the first Candidate A session lands on that window edge.
- `gh-cli-single-click-auth_2026-04-16.md` fresh, expires 2026-04-23 — usable for any `GH_BROWSER` wire-up or gh-related v1.3.x work.
- `zero-friction-bootstrap-ux_2026-04-16.md` fresh, expires 2026-04-23 — background for v1.3.x UX polish.
- No new research entries required for any of the three candidates.

## Scope lock

Aurum remains at `0b1de3d`. The v1.0.0 freeze was lifted on v1.0.0 ship, so no active lock. Verify at session open by reading `memory/project/aurum_frozen_scope_lock.md`.

## Exact phrase for the next session

Open Claude Code in `C:/Dev/Claude_cowork/project-genesis/` and paste:

```
mode autonome  Je reviens après le ship v1.3.0 (genesis-drop-zone
welcome vertical slice, first MINOR since v1.2.0, self-rating
9.34/10, running avg 8.78, cinquième ship consécutif ≥ 9.0,
Best-at-date ceiling brisé à 9.4). Lis le resume v1.3.0 → v1.3.1
et le session trace v1.3.0. Replay d'abord Scenario #1 (fresh dir
/genesis-drop-zone → welcome) qui était différé. Puis propose-moi
v1.3.1 candidat A (extraction + bootstrap_intent.md + handoff)
vs candidat B (P2 chore config examples) vs candidat C (runtime
locale detection) avec ta reco, puis on attaque.
/using-superpowers
```
