<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-18 v1.4.0 → v1.4.1
description: Handoff from the v1.4.0 genesis-drop-zone Citations API ship. Ninth consecutive ship ≥ 9.0. First MINOR bump since v1.3.0 opened the v1.3.x conversational-layer line. Second concentrated privilege class (network) live for genesis-drop-zone. Cross-skill-pattern #2 refined to "one privilege per operation class, per skill". Four candidates for v1.4.1 — (A) Layer B citation surfacing, (B) UX toolkit polish (@clack/prompts + Charm Gum + cli-spinners), (C) error-handling refinements on filesystem side, (D) Files API beta adoption.
type: resume
previous_session: v1.4.0 Citations API extraction
next_action: Pick v1.4.1 scope (A Layer B surfacing / B UX toolkit / C error handling / D Files API) or another direction
---

# Resume — 2026-04-18 v1.4.0 → v1.4.1

## What v1.4.0 shipped

**Tag v1.4.0** (PR TBD, merge pending). **MINOR** bump on the v1.3.x conversational-layer line — first MINOR since v1.3.0 opened the line. Six-commit rhythm (fifth consecutive application).

- **spec** (`c76227a`) + **spec polish** (`1a064ac`, 4 advisories from spec-document-reviewer) — `.claude/docs/superpowers/specs/v2_etape_0_drop_zone.md` gains `## Scope — v1.4.0 Citations API extraction` section + new design section `## Citations API — signal + dispatch (v1.4.0)` (three-gate lifecycle + extractor I/O contract + fallback triggers + citation object shape + env vars + modification-loop interaction + zero Layer B ripple note + typed-text citation wrapping subsection added in polish). Concentrated privilege declaration rewritten as per-class table. 1:1 mirror map + R9 tier additions. Deferred renamed v1.3.4+ → v1.4.1+ (Path A Citations CLOSED, removed). Verification scenarios #28-#39. `## Rationale for v1.4.0 route` section. +287 / -26 lines + 42 / -7 polish.
- **plan** (`69ae2dd`) + **plan polish** (`c94b570`, 4 advisories from plan-document-reviewer) — 13-task implementation plan, 422 lines. Task 0 gate through Task 13 PR+merge+tag. Task 1 Python extractor split into 7 sub-steps with R9 decomposed probes. Fixture diff probe corrected from 2 → 4 lines + CRLF preservation precondition. Scenario #37 functional YAML-parse probe added.
- **feat** (`9df758f`) — v1.4.0 ship. 11 files, +793 / -32 lines.
  - `skills/genesis-drop-zone/scripts/extract_with_citations.py` new (356 lines).
  - `skills/genesis-drop-zone/SKILL.md` new `## Citations API dispatch (v1.4.0)` + `### In scope (v1.4.0)` + `## Concentrated privilege` per-class rewrite + `## Deferred scope` 12-item v1.4.1+ list + all stale v1.3.4+ refs swept.
  - `skills/genesis-drop-zone/phase-0-welcome.md` new `### Citation annotation format (v1.4.0)` subsection.
  - Three fixtures — `_fr_with_citations`, `_en_with_citations` (text_char_range), `_fallback` (CRLF-preserved byte-identical to v1.3.3 EN modulo `skill_version`).
  - `memory/master.md` cross-skill-pattern #2 refined to "at most one per operation class, per skill"; cross-skill-pattern #4 extended with v1.4.0 additive-frontmatter discipline.
  - `.claude-plugin/plugin.json` 1.3.3 → 1.4.0.
  - `.claude/docs/superpowers/research/stack/anthropic-python_2026-04-18.md` new R8 stack entry pinning `anthropic>=0.40.0`.
  - `INDEX.md` updated with stack row + v2_promptor_fusion row summary updated to include v1.4.0 Citations extractor.
- **chore** (pending) — this resume + CHANGELOG + session trace + MEMORY pointer.

Self-rating **9.10/10**. Running average **≈ 8.88/10** (+0.01). **Ninth consecutive ship ≥ 9.0** (9 of 9 since v1.2.1).

Full narrative at `memory/project/session_v1_4_0_citations_api.md`.

## Current state

- Repo: `myconciergerie-prog/project-genesis`. Branch `feat/v1.4.0-citations` ready to PR.
- Tags chain pre-merge: v1.0.0 → v1.1.0 → v1.2.0 → v1.2.1 → v1.2.2 → v1.2.3 → v1.2.4 → v1.3.0 → v1.3.1 → v1.3.2 → v1.3.3 → **v1.4.0 (pending)**.
- Worktree clean on `feat/v1.4.0-citations`, 5 commits ahead of main (spec + spec polish + plan + plan polish + feat). Chore commit pending as part of this chore landing.
- `gh auth`: two accounts (`myconciergerie-prog` + `myconciergerieavelizy-cloud`); R2.3.1 pre-flight required before `gh pr create`.
- Skill count: 7 (six Layer B + one Layer A). Unchanged structurally. `genesis-drop-zone` now has two privilege classes.
- **Cross-skill patterns**: 4 named. Pattern #2 **refined** from "at most one per skill" to "at most one per operation class, per skill". Pattern #4 extended with v1.4.0 additive-frontmatter discipline.
- **Concentrated privilege map**: `genesis-drop-zone` now declares two orthogonal classes — disk (v1.3.2 write, unchanged) + network (v1.4.0 API call with silent fallback).
- P1 queue: empty at both bootstrap and downstream-rule levels. v1.3.x was feature-line PATCH cycle; v1.4.0 opens the audit-trail + external-integration line.
- Living-spec pattern: `v2_etape_0_drop_zone.md` now covers v1.3.0 + v1.3.1 + v1.3.2 + v1.3.3 + v1.4.0 in one file across five version-scoped sections (fifth consecutive application).

## v1.4.1 candidates

### Candidate A — Layer B citation surfacing

Extend `genesis-protocol`'s Phase 0 Step 0.4 intent card and Step 0.5 `bootstrap_intent.md` template to **display** the `[page N]` / `[lines X-Y]` annotations that Layer A now persists in `drop_zone_intent.md` frontmatter. v1.4.0 writes the data; v1.4.1 would render it end-to-end.

**Scope**: small-to-moderate. `genesis-protocol` Phase 0 `phase-0-seed-loading.md` Step 0.4 card template gains per-field origin annotation (` [page N]` suffix next to Vision, Stack hints, etc. when `<field>_source_citation` is present). Step 0.5 `bootstrap_intent.md` template's `## Conversational context from drop zone` section or new `## Source attribution from drop zone` section.

**Why pick A**: closes the audit-trail loop end-to-end. Victor in detailed mode sees "Vision: ... [page 3 of brief]" on the Phase 0 card — visible, not just persisted. Makes v1.4.0's Citations value loop-closed rather than half-baked.

**Why it might not be first**: no concrete user pain has emerged yet asking for the Phase 0 card to surface citations. Victor has seen the annotations in the Layer A mirror (v1.4.0); Layer B's card in detailed mode is a secondary surface. Wait for pain.

### Candidate B — UX toolkit polish

`@clack/prompts` structural skeleton, Charm Gum for select prompts, cli-spinners for the `◐` animation, cross-platform completion chime. Now that R9 is closed (v1.3.3) and citations have shipped (v1.4.0), the surface is complete enough for polish without re-fragmenting.

**Scope**: small-to-moderate. Introduce toolkit gradually — spinner on mirror first, prompt skeleton second, chime third.

**Why pick B**: the surface is feature-complete. Polish lands cleanly. Keeps the ≥9.0 streak with minimal risk.

**R8 runway**: `zero-friction-bootstrap-ux_2026-04-16.md` is the primary source. Fresh until 2026-04-23. Refresh at session open.

### Candidate C — Error handling refinements (filesystem side)

Permission-denied / disk-full / symlink-pointing-to-directory edge cases currently let `OSError` bubble up naturally via the harness stack. v1.4.1+ adds halt + remediation if real user pain in v1.3.2 / v1.3.3 / v1.4.0 usage emerges.

**Scope**: small if pain-driven. Moderate if preemptive + comprehensive (catches all three filesystem classes).

**Why pick C**: no concrete pain observed yet across v1.3.2 / v1.3.3 / v1.4.0 shipping period. Worth deferring until a real OSError surfaces. Remains on the list so it stays visible.

### Candidate D — Files API beta adoption

Migrate from inline base64 document blocks to the Anthropic Files API beta (`anthropic-beta: files-api-2025-04-14`). Enables dedup across sessions (create-once / use-many) and larger file limits (500 MB/file, 500 GB/org). Useful when a concrete user drops the same brief multiple times or hits the 32 MB × 600-page inline limit.

**Scope**: moderate. Adds beta header handling + Files API create/retrieve logic + file-ID caching discipline + TTL-aware cleanup. Stack entry refresh needed.

**Why pick D**: performance/cost lever. Not yet justified — no user has hit the 32 MB limit or paid repeatedly for the same document upload.

## Reco

**Candidate B (UX toolkit polish)** is the cleanest next ship for preserving the ≥ 9.0 streak. The surface is now feature-complete after v1.4.0 (welcome + mirror with annotations + consent + write + Layer B handoff + locale dispatch). Polish can land cleanly. Narrow scope feasible: introduce spinner-only as a minimal PATCH v1.4.1.

**Candidate A (Layer B citation surfacing)** is the next structural ship if the user wants to close the end-to-end audit-trail loop. Moderate scope — `genesis-protocol` Phase 0 touches — but preserves the additive-contract discipline that v1.4.0 established.

**Candidate C** waits for real OSError pain. **Candidate D** waits for inline-base64 limit pain.

If the session is tight on time or energy, Candidate B (scope-reduced to spinner-only) is the risk-minimising default.

## Files to read at session open

1. `memory/MEMORY.md`
2. `memory/master.md` (v1.4.0 changes: cross-skill-pattern #2 refined + #4 extended; privilege map first multi-class declaration)
3. `memory/project/session_v1_4_0_citations_api.md`
4. This resume prompt
5. `.claude/docs/superpowers/specs/v2_etape_0_drop_zone.md § "Deferred to v1.4.1+"` (for candidate scoping)
6. CHANGELOG.md v1.4.0 entry (5-axis self-rating detail)
7. If picking A or D: `skills/genesis-drop-zone/scripts/extract_with_citations.py` to understand the extractor's output shape before extending Layer B.

## R8 cache state

- `v2_promptor_fusion_landscape_2026-04-17.md` — fresh, expires **2026-04-24**. Primary source for A (Path A Citations Stage 2), B (Stage 1 UX), D (Files API in Stage 2). Refresh at session open if opened on 2026-04-23 or later.
- `gh-cli-single-click-auth_2026-04-16.md` — fresh, expires 2026-04-23.
- `zero-friction-bootstrap-ux_2026-04-16.md` — fresh, expires 2026-04-23. Primary source for Candidate B.
- `anthropic-python_2026-04-18.md` (stack, TTL 1 day) — **expires 2026-04-19**. If the next session opens on 2026-04-19 or later AND touches the extractor, refresh this stack entry before any code change.
- No new R8 research required for any of the four candidates at this point beyond the anthropic-python stack refresh.

## Exact phrase for the next session

Open Claude Code in `C:/Dev/Claude_cowork/project-genesis/` and paste:

```
mode autonome  Je reviens après le ship v1.4.0 (genesis-drop-zone
Citations API extraction, seconde classe de privilège network via
subprocess Python, silent graceful fallback vers v1.3.3 in-context,
additive frontmatter keys préservant zero Layer B ripple, cross-skill-
pattern #2 raffiné "per operation class per skill", 3 fixtures CRLF-
préservés, self-rating 9.10/10 [Self-contained 8.9 honnête MINOR-bump
surface growth], running avg 8.88, neuvième ship consécutif ≥ 9.0,
MINOR bump premier depuis v1.3.0). Lis le resume v1.4.0 → v1.4.1 et
le session trace v1.4.0. Refresh R8 stack entry anthropic-python si
on est à 2026-04-19 ou après ET que le scope touche l'extractor.
Propose-moi v1.4.1 candidat A (Layer B citation surfacing sur le
Phase 0 card), candidat B (UX toolkit polish @clack/prompts + Charm
Gum + cli-spinners + chime), candidat C (error-handling filesystem
side), candidat D (Files API beta adoption) avec ta reco, puis on
attaque.
/using-superpowers
```

**PowerShell launcher** (one-line, per the end-of-session resume convention):

```
cd C:/Dev/Claude_cowork/project-genesis; claude --dangerously-skip-permissions "mode autonome  Je reviens après le ship v1.4.0 (genesis-drop-zone Citations API extraction, seconde classe de privilège network via subprocess Python, silent graceful fallback vers v1.3.3 in-context, additive frontmatter keys préservant zero Layer B ripple, cross-skill-pattern #2 raffiné 'per operation class per skill', 3 fixtures CRLF-préservés, self-rating 9.10/10, running avg 8.88, neuvième ship consécutif ≥ 9.0, MINOR bump premier depuis v1.3.0). Lis le resume v1.4.0 → v1.4.1 et le session trace v1.4.0. Refresh R8 stack entry anthropic-python si on est à 2026-04-19 ou après ET que le scope touche l'extractor. Propose-moi v1.4.1 candidat A (Layer B citation surfacing), candidat B (UX toolkit polish), candidat C (error-handling filesystem), candidat D (Files API beta) avec ta reco, puis on attaque. /using-superpowers"
```
