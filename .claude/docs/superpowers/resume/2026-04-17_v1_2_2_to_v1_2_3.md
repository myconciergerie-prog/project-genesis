<!-- SPDX-License-Identifier: MIT -->
---
name: Resume prompt — 2026-04-17 v1.2.2 → v1.2.3
description: Handoff from v1.2.2 mode-argument ship session. P1 cluster (F20 + F22 + F21) landed as a 2-commit PR. v1.2.3 picks F34 (gh active-account pre-flight at Phase 6.0) as the focused next surgical fix, or pivots to v2 Étape 0 drop-zone if research cache freshness drives the call.
type: resume
previous_session: v1.2.2 mode-argument (PR #?, tag v1.2.2)
next_action: Pick v1.2.3 scope — F34 focused, or pivot to v2 Étape 0
---

# Resume — 2026-04-17 v1.2.2 → v1.2.3

## What v1.2.2 shipped

**Tag v1.2.2** (PR #? merged). Three P1 frictions landed as a 2-commit bundle in one PR:

- **F21** — `## Arguments` section in `skills/genesis-protocol/SKILL.md` documenting `mode` / `target` / `seed` with defaults + passthrough convention (`context=...`, `strange-loop=...`, etc. are accepted and captured in `bootstrap_intent.md`).
- **F20** — `mode` is now a first-class orchestrator argument with three values (`detailed` | `semi-auto` | `auto`). Mode is sticky through the run, propagates into `phase-minus-one`'s own 3-mode ladder, and never overrides structural stops (paradox guards, nested-repo halts, halt-on-leak, security floor).
- **F22** — Step 0 top-level consent card dispatches per mode: block in `detailed`/`semi-auto`, informational-log in `auto`. Three phase runbooks (Phase 0.4, Phase 3.2/3.3/3.5, Phase 6.1) get a short mode-dispatch paragraph pointing to SKILL.md's canonical table.

Self-rating **9.14/10**. Running average **8.68/10** (up from 8.65). Three consecutive ≥ 9.0 versions (v1.2.0 8.88, v1.2.1 9.26, v1.2.2 9.14) — the surgical-commit-per-root-cause discipline is holding.

Full narrative at `memory/project/session_v1_2_2_mode_auto_args.md`.

## Current state

- Repo: `myconciergerie-prog/project-genesis` on main, tag `v1.2.2`, commit `<merge-sha>`.
- Tags chain: `v1.0.0` → `v1.1.0` → `v1.2.0` → `v1.2.1` → `v1.2.2`.
- git status: clean, all pushed.
- `gh auth`: still two accounts (`myconciergerie-prog` + `myconciergerieavelizy-cloud`); **still always verify active account with `gh api user --jq .login` before `gh pr create`** — this is exactly what F34 is about, and v1.2.3 is the PR that finally automates the check.

## v1.2.3 candidate — F34 as its own focused PR

**Fix sketch** (from the v1.2.0 friction log):

> Phase 6 Step 6.0 pre-flight: `gh api user --jq .login` compared to resolved `<owner>` from `bootstrap_intent.md`. If mismatch, attempt `gh auth switch -u <owner>` automatically; if that fails, surface the mismatch with `gh auth status` output and halt until resolved.

**Expected shape**: one commit adding Step 6.0 to `phase-6-commit-push.md` before the existing Step 6.1 (pre-commit review). Possibly a second commit updating `SKILL.md`'s phase master table to list the new step. Diff: ~40 insertions, zero deletions. Single-PR, single-tag — the v1.2.1 and v1.2.2 rhythm.

**Why F34 next and not one of the other P1s**:
- F34 was **live-reproduced during v1.2.1's PR creation** on this very session's chain — the signal is fresh and the context is fully loaded. Waiting any longer dilutes the root-cause evidence.
- F24 (Phase 0.1 git-aware inspection) is P2, not P1, and F30 already covers the Phase 3 blocker case. F24 alone is cosmetic polish.
- F26 (non-canonical fields audit UI) is half-fixed by v1.2.2 — the passthrough CONVENTION is documented in SKILL.md, the actual `bootstrap_intent.md` template write can ride along with any other v1.2.x touch-up.
- F25/F31 (config.txt canonical examples) are P2 doc work — best shipped as a chore PR rather than a fix PR.
- F32 (Python driver) remains the v1.3 target. v1.2.2 proved the Markdown ceiling was above the mode-dispatch scope; v1.2.3 with F34 also stays in the Markdown lane.

## Two natural paths forward

### Path A — continue the v1.2.x refinement loop (F34 in v1.2.3)

Single-commit PR adding Step 6.0 pre-flight. Est. 1 session, probably shorter than v1.2.2. Running average should cross 8.70 on a clean ship.

**Arguments for Path A**: fourth consecutive surgical ship, maintains the 9.0+ streak, F34 is the last live-reproduced P1 in the queue.

### Path B — pivot to v2 Étape 0 (Le Dépôt drop-zone)

Same Path B as the v1.2.1 → v1.2.2 resume. The research cache `v2_promptor_fusion_landscape_2026-04-17.md` expires **2026-04-24** (7 days from today) — still plenty of runway to do v1.2.3 first, but the window is closing.

**Arguments for Path B**: v2 is the visible product shift. The P1 queue is nearly empty — only F34 remains as a P1; after that the next-severity band is P2 doc work. Starting v2 after v1.2.3 means one more v1.2.x cycle before the mental-mode switch.

**Suggested order if the user wants both**: v1.2.3 (F34, ~1 session) → v2 Étape 0 (bigger scope, 2+ sessions). The P1 queue closes cleanly, then the mode shifts to v2.

## Files to read at Session open

1. `memory/MEMORY.md`
2. `memory/master.md`
3. `memory/project/session_v1_2_2_mode_auto_args.md`
4. `memory/project/selfdogfood_friction_log_v1_2_0_2026-04-17.md` (F34 details at the very end of the frictions list)
5. This resume prompt
6. For Path B: `specs/v2_vision_promptor_fusion.md` + `.claude/docs/superpowers/research/sota/v2_promptor_fusion_landscape_2026-04-17.md`

## R8 cache state

- Stack entries `claude-code-plugin-structure` + `claude-code-session-jsonl-format` expired 2026-04-17 (today). Not refreshed during v1.2.2 because the change was skill-internal. **v1.2.3 F34 work does depend on `gh` CLI behaviour** — archive the expired stack entries first, then consider a fresh `gh auth` behaviour snapshot if anything in the auth UX looks unfamiliar during the fix.
- `v2_promptor_fusion_landscape_2026-04-17.md` fresh, expires **2026-04-24** — usable for Path B.

## Scope lock

Aurum remains at `0b1de3d` per the v1.0.0 freeze-lift conditions (the freeze was lifted at v1.0.0 ship, so no active lock). Verify at session open by reading `memory/project/aurum_frozen_scope_lock.md`.

## Exact phrase for the next session

Open Claude Code in `C:/Dev/Claude_cowork/project-genesis/` and paste:

```
mode autonome  Je reviens après le ship v1.2.2 (F21 Arguments + F20/F22
mode dispatch, self-rating 9.14/10, running avg 8.68, trois ships
consécutifs ≥ 9.0). Lis le resume v1.2.2→v1.2.3, le session trace v1.2.2,
et la section F34 du friction log v1.2.0. Propose-moi Path A (F34 en
v1.2.3 focused) vs Path B (pivot v2 Étape 0 drop-zone) avec ta reco,
puis on attaque.
/using-superpowers
```
